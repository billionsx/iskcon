/**
 * ЗКН-Ф021 — ШТАМП СБОРКИ: ДЕПЛОЙ ОБЯЗАН ДОКАЗАТЬ СЕБЯ, А НЕ ОТРАПОРТОВАТЬ.
 *
 * ПОЧЕМУ ЭТОТ ФАЙЛ ПОЯВИЛСЯ.
 *
 * 13.07.2026 деплой прошёл «успешно», гейт свежести был ЗЕЛЁНЫЙ — а прод (и apex,
 * и workers.dev, то есть МИМО всех кэшей) отдавал СТАРЫЙ бандл. Человек открывал
 * сайт и видел вчерашний код. Дважды подряд.
 *
 * Гейт сверял ХЭШ ФАЙЛА: `/assets/index-<hash>.js` из свежей `dist/index.html`
 * против того, что отдаёт сайт. У этой проверки две дыры:
 *
 *   1. ХЭШ МОЖЕТ СОВПАСТЬ. Правка в лениво-подгружаемом чанке НЕ меняет хэш
 *      входного чанка. Тогда «ожидаемый» равен «живому» — и гейт зелёный,
 *      даже если на прод не уехало НИЧЕГО.
 *
 *   2. ХЭШ НИЧЕГО НЕ ГОВОРИТ О СОДЕРЖИМОМ. Совпал — и ладно. А внутри может
 *      лежать сборка любой давности.
 *
 * Штамп закрывает обе. В бандл вшивается SHA КОММИТА, и гейт ищет ИМЕННО ЕГО
 * в теле живого файла. Совпасть случайно нечему: нет SHA в бандле на проде —
 * значит эта сборка туда не доехала. Точка.
 *
 * Значение подставляется в CI (шаг «Штамп сборки»). Локально остаётся "dev" —
 * это нормально и ничего не ломает.
 */
export const BUILD_SHA = "dev";
