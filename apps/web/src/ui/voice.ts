/**
 * ЗКН-Д013 · ЧУЖОЙ ГОЛОС — КУРСИВНЫМ GEORGIA.
 * ─────────────────────────────────────────────────────────────────────────
 * ЕДИНЫЙ ИСТОЧНИК ПРАВДЫ. Всё, что является ОТКРОВЕНИЕМ ИЛИ ЦИТАТОЙ —
 * стих писания (санскрит · транслитерация · ПЕРЕВОД), куплет бхаджана,
 * любая цитата в карточке личности, стих дня, цитата в PDF — набирается
 * шрифтом писания (Georgia) КУРСИВОМ. Без исключений.
 *
 * Наша собственная проза — комментарий в читалке, житие, пояснение, подпись —
 * прямым UI-шрифтом.
 *
 * Читатель обязан ВИДЕТЬ ГЛАЗАМИ, где говорит шастра, а где говорим мы.
 * Это не украшение: смешать голос откровения с голосом редактора — значит
 * присвоить себе авторитет писания.
 *
 * Правило переноса встроено сюда же (ЗКН-Д012): транслитерация состоит из
 * длинных составных слов через дефис («кришна-прештхайа»), и без него строка
 * уезжает за край блока.
 */
import type { CSSProperties } from "react";

export const SCRIPTURE_VOICE: CSSProperties = {
  fontFamily: "var(--font-scripture)",
  fontStyle: "italic",
  overflowWrap: "anywhere",
  wordBreak: "break-word",
  hyphens: "auto",
};

/** Тот же голос для PDF/печати, где CSS-переменные недоступны. */
export const SCRIPTURE_VOICE_PRINT: CSSProperties = {
  fontFamily: "Georgia, 'Gentium Book Plus', 'Times New Roman', serif",
  fontStyle: "italic",
  overflowWrap: "anywhere",
};
