// Единая версия кэша серверных PDF. Используется в ДВУХ местах:
//   • worker.ts — как сегмент синтетического edge-ключа (caches.default),
//     чтобы бамп орфанил ранее закэшированные book/lila-PDF (они immutable на год);
//   • bookPdf.tsx / BookDetailPage.tsx — как ?v= в URL запроса PDF, чтобы тот же
//     бамп пробивал и браузерный/CDN-кэш ответа (book/lilamerged отдаются immutable).
// Поднимать ОДНО это значение при смене обложек книг или вёрстки печати —
// и серверный, и клиентский слой кэша инвалидируются синхронно.
export const PDF_CACHE_REV = "c2";
