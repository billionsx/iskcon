/**
 * ЗКН-Пф001 — ВЕРСИЯ КЭША PDF ВЫВОДИТСЯ ИЗ СБОРКИ, А НЕ ИЗ РУКИ.
 *
 * PDF отдаются с `max-age=31536000, immutable` (год, без перепроверки). Это верно
 * ТОЛЬКО если у нового содержимого — новый ключ кэша. Раньше ключ был ручной
 * строкой («c3»), которую надо было помнить и бампать при каждой правке контента.
 * Никто не помнил → PDF во всём приложении оставались старыми навсегда.
 *
 * Теперь значение подставляется на сборке из SHA коммита (шаг в deploy-web.yml).
 * Каждый деплой = новый ключ = PDF пересобираются с актуальным содержимым.
 * Ручная правка этого файла бессмысленна: он перезаписывается при деплое.
 */
export const PDF_CACHE_REV = "dev";
