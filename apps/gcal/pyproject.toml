[project]
name = "iskcon-gcal"
version = "0.1.0"
description = "On-demand Gaudiya Vaisnava (Gaurabda) calendar compute — Cloudflare Python Worker"
requires-python = ">=3.12"

[dependency-groups]
dev = ["workers-py"]
