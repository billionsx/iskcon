# СВОД ЗАКОНОВ ПРИЛОЖЕНИЯ (Конституция)

> **Единственный индекс всех законов приложения.** Один файл. Прежде чем трогать
> текст, карточку, дизайн, навигацию, данные или скрипт — сверься с этим сводом.
> Если закон установлен здесь — он применяется ко **всему** приложению.
>
> Этот документ — **реестр и процесс**. Полнотекстовые формулировки живут в
> файлах-источниках истины (колонка «Источник»); свод их **индексирует**, а не
> дублирует. Правило одного места: деталь правится в источнике, свод хранит ссылку.

---

## 0. Зачем этот свод

Законы в проекте уже были — но **раздроблены** по десятку документов и, главное,
**не принуждались**: инфраструктура принуждения существовала, но осиротела
(`tokens.ts`/`primitives.tsx` — 0 импортов; санитайзер `cleanCardText` подключён к
2 карточкам из 13; статического линтера законов в CI нет вообще). Поэтому один и
тот же закон приходилось повторять устно снова и снова — а устная форма забывается.

**Корень проблемы не в отсутствии законов, а в отсутствии принуждения.** Свод
чинит это одним правилом: *каждый повторяющийся закон обязан подняться по лестнице
принуждения до уровня, на котором нарушение технически невозможно.*

---

## 1. Лестница принуждения (У0 → У5)

Сила закона = уровень, на котором он живёт. Чем выше — тем невозможнее нарушение.

| Ур. | Форма | Кто гарантирует | Слабость |
|----|-------|-----------------|----------|
| **У0** | Память / устно | никто | забывается — **корень боли** |
| **У1** | Документ (`docs/…`) | добрая воля | не читается |
| **У2** | Общий модуль (токен/примитив/компонент) | принятие в коде | можно не импортировать |
| **У3** | Правка данных в D1 | одноразовая | регрессирует при новом контенте |
| **У4** | Санитайзер в рантайме (`cleanCardText`) | код | покрывает только то, что через него прогнали |
| **У5** | Гейт в CI (grep / SQL, fail сборки) | конвейер | — нарушение не может попасть в прод |

**ЗАКОН ЗАКОНОВ (мета-закон).** Терминальное состояние повторяющегося закона —
**У4 или У5**. Закон, оставленный на У0/У1, считается *незакрытым* (баг процесса).
При сомнении выбираем более высокий уровень.

Иерархия при конфликте: **У5 > У4 > У3 > У2 > У1 > У0.** Гейт/санитайзер главнее
документа; документ главнее памяти.

---

## 2. Жизненный цикл закона (создание → утверждение → установление)

```
ПРЕДЛОЖЕНИЕ → УТВЕРЖДЕНИЕ → УСТАНОВЛЕНИЕ → ПРОВЕРКА → ЗАКРЫТИЕ
```

1. **ПРЕДЛОЖЕНИЕ.** Формулировка + домен + *почему* (какая повторяющаяся боль) +
   целевой уровень принуждения.
2. **УТВЕРЖДЕНИЕ.** Основатель утверждает (одним словом). Закон получает ID
   (`ЗКН-<домен><NNN>`) и строку в §4.
3. **УСТАНОВЛЕНИЕ.** Реализуется на **максимально возможном** уровне:
   - механическое, контекст-независимое → санитайзер (У4) **и/или** CI-grep (У5);
   - смысловое, контекст-зависимое → правка данных в D1 (У3) **+** по возможности
     CI-grep/SQL, ловящий регрессию (У5);
   - визуальное → перенос в общий токен/примитив (У2) **+** линтер против «сырых»
     значений (У5).
4. **ПРОВЕРКА.** Команда проверки (`grep`/SQL/аудит) возвращает **0 нарушений**.
   Идеал — команда вшита в CI (`check-web.yml` / отдельный `laws-lint.yml`).
5. **ЗАКРЫТИЕ.** Закон закрыт, только если: проверка = 0 **И** уровень ≥ У4/У5.
   Иначе — остаётся в §5 «Долги» до механизации.

Отмена/правка закона — тем же циклом (правится строка в §4 и источник).

---

## 2-бис. ПРАВКА ОСНОВАТЕЛЯ = ЗАКОН (обязательный рефлекс)

> **Это самый важный процессный закон свода.** Он существует потому, что основатель
> вынужден был повторять одно и то же по многу раз: правка исполнялась, но не
> становилась законом — и повторялась снова в следующем шаге.

**ЗАКОН ЗКН-Ц005.** Любая правка/замечание основателя ОБЯЗАТЕЛЬНО и НЕМЕДЛЕННО
проходит через этот рефлекс — до того, как работа считается сделанной:

| # | Шаг | Что делаю |
|---|-----|-----------|
| 1 | **Исполнить** | сделать саму правку |
| 2 | **Обобщить** | спросить себя: это частный случай или ПРАВИЛО? Почти всегда правило |
| 3 | **Записать в свод** | новый закон в `LAWS.md §4` + человеческим языком в `LAWS_plain.md` + в источник истины домена (`STANDARD_*.md`) |
| 4 | **Механизировать** | поднять на максимальный уровень: санитайзер (У4) или линтер/гейт (У5). Документ без механизма = НЕЗАКРЫТО |
| 5 | **Найти ВСЕ такие же** | правка в одном месте → grep/SQL по всему приложению: где ещё то же нарушение? Чинить все разом |
| 6 | **Доложить** | в ответе: что исправлено, каким законом это стало, где механизировано |

**Признак того, что закон нарушен мной:** основатель повторяет замечание второй раз.
Это баг процесса, а не его недовольство.

**Триггеры-маркеры** (любой из них = запускать рефлекс немедленно):
«не работает», «не так», «должно быть», «нужно», «почему», «опять», «я же говорил»,
присланный скриншот с дефектом, присланный ассет.


---

## 3. Домены законов

| Код | Домен | Источник истины (файл/функция) |
|----|-------|--------------------------------|
| **И** | Имена и канонические формы | `docs/STANDARD_canonical_names.md`, `cardText.ts` |
| **Т** | Текст карточек | `docs/LAWS_cards.md`, `apps/web/src/cardText.ts::cleanCardText` |
| **Д** | **ISKCON Design** — дизайн-система (шрифт, цвет, отступ, радиус) | **`docs/STANDARD_design.md`**, `ui/tokens.ts`, `ui/primitives.tsx`, `ui/globals.css` |
| **К** | Карточки (4 формата: ВБК·ВСК·ВМК·ПК) | **`docs/STANDARD_cards.md`**, `CardCover.tsx`, `cardActions.tsx` |
| **Н** | Навигация (стек истории, «назад») | `docs/STANDARD_navigation.md`, `apps/web/src/nav.ts` |
| **С** | Санскрит / терминология | `docs/STANDARD_scripture_terms.md`, `ui/Skt.tsx`, `ui/scripture.ts` |
| **Б** | Книги (названия, чипы) | `docs/STANDARD_book_titles.md`, `STANDARD_book_chips.md`, `apps/web/src/books.ts` |
| **Р** | Реестр / данные / уровни профиля | `docs/entity-registry/GOLD_STANDARD_PROFILE.md`, **`tools/goldforge/`** |
| **П** | ПКЛ (карточки личностей) | `docs/pkl-hero-playbook.md` |
| **Ф** | Инфраструктура / деплой / инженерные гейты | `.github/workflows/`, `apps/api/migrations/`, `docs/INFRASTRUCTURE_SETUP.md` |
| **Пр** | Продуктовая дисциплина (стратегические НЕТ) | `docs/STRATEGY.md` |
| **БТ** | Достоверность / BBT (ноль фабрикации, цитаты, копирайт) | `docs/pkl-hero-playbook.md`, `docs/doctrine/`, `docs/STANDARD_canonical_names.md` |
| **Сд** | Сиддханта (доктринальные инварианты) | `docs/SPINE_personalism.md`, `docs/doctrine/gauranga-lila.md` |
| **Пл** | Пайплайны наполнения (автоскрипты) | `.github/workflows/` (календарь/лента/TG→IA), `apps/gcal/`, `tools/` |
| **Ц** | Скрипты / CI-гигиена | этот файл, §6 |

---

## 4. Реестр законов

> **Утверждение доменов (пошагово):** Сд ✅ · БТ ✅ · Пр 🟠 · И ✅ · Т ✅ · С 🟠 · К ✅ · Д ✅ · Н ✅ · Б ✅ · Р ✅ · П ✅ · Пл ✅ · Ф ✅ · Ц ✅ — **15 / 15 · СВОД РАТИФИЦИРОВАН 11.07.2026** (Пр, С — с оговорками).

Уровень = **текущий → целевой**. Статус: 🟢 закрыт (≥У4) · 🟡 частично · 🔴 долг (У0/У1).

### Домен Сд — Сиддханта (доктрина — верховный слой) · ✅ УТВЕРЖДЁН 10.07.2026
| ID | Закон | Ур. | Проверка | Статус |
|----|-------|-----|----------|--------|
| ЗКН-Сд001 | *kṛṣṇas tu bhagavān svayam* (ШБ 1.3.28): Кришна — изначальная Верховная Личность Бога; хаб всего графа (`svayam-bhagavan`), всё центрируется на Нём  Это не украшение свода, а **инвариант данных**: у `krishna` категория `svayam-bhagavan` и ≥20 связей. Потеряется — рухнет ось таксономии | **У5** | `data-audit.py` | 🟢 |
| ЗКН-Сд002 | Гауранга Махапрабху = Сам Кришна в настроении и золотом сиянии любви Шримати Радхарани (тождество `gauranga-lila-identity`) | У3 | `doctrine/gauranga-lila.md §1.3` | 🟢 |
| ЗКН-Сд003 | ИСККОН — беспрецедентная волна Гауранга Лилы, предсказанная Самим Гаурангой; все ачарьи внесли вклад, явил Шрила Прабхупада  **Проверено:** все **128** гуру GBC классифицированы ВНУТРИ Гауранга Лилы — не отложены в отдельный список | **У5** | `data-audit.py` | 🟢 |
| ЗКН-Сд006 | **ОДНА ВЕЧНАЯ ФОРМА — ОДИН ЧЕЛОВЕК.** `gauranga-lila-identity` говорит: «в Кришна Лиле этот спутник Гауранги — вот кто». Форма **лична**; двое не могут быть одним. Гейт нашёл живые противоречия: **Санатана Госвами и Шивананда Чакраварти** — оба Лаванга-манджари (канон отдаёт её Санатане; у Шивананды досье **молчит**); **Пундарика Видьянидхи и Мадхава Мишра** — оба Вришабхану (у Пундарики целая вкладка «отец Радхи», у Мадхавы — **ничего**); и **трое** имели формой **«Чайтанья»** — что невозможно: **Чайтанья и есть Кришна в Гауранга Лиле, Он не чья-то форма**. Накула Брахмачари — **сосуд** (шактьявеша), а не тождество. Собирательные (`collective`) — законное исключение | **У5** | `data-audit.py` · признанный долг **4** (решается только загрузкой ГГД) | 🟢 |
| ЗКН-Сд004 | Гауранга Лила — часть Шримад-Бхагаватам, происходящая на планете сейчас; это Кришна Лила в настроении аударьи (милосердие, сострадание) | У1 | `doctrine/gauranga-lila.md`, `ARCHITECTURE.md §1` | 🟡 |
| ЗКН-Сд005 | Персонализм как архитектура: всё висит на графе Личностей (самбандха→абхидхея→прайоджана); никакого имперсонализма (Бог ≠ контент)  **Личность вне графа = брешь в персонализме.** Было **66 сирот**. Среди них — **Шачи Деви, мать Гауранги Махапрабху, не связанная с Ним** (оказалась ДУБЛЕМ: `saci-devi` с досье в 7417 знаков и `shachidevi` — узлом графа; слиты). И четыре экспансии — Маха-Вишну, Санкаршана, Васудева, Гарбходакашайи Вишну — висели без связи с Кришной. Долг **62**: связь надо УСТАНОВИТЬ по источнику, а не выдумать | **У5** | `data-audit.py`, храповик 62 | 🟡 |

### Домен И — Имена и формы · ✅ УТВЕРЖДЁН 10.07.2026
| ID | Закон | Ур. | Проверка | Статус |
|----|-------|-----|----------|--------|
| ЗКН-И001 | Голая «Шри Чайтанья Махапрабху» запрещена → «Гауранга Махапрабху» (общий/до-санньясы) ИЛИ «Шри Кришна Чайтанья Махапрабху» (Пури/санньяса/Госвами) | У4→У5 | `grep -rn 'Шри Чайтанья' apps/web/src` (мимо `cleanCardText`) | 🟡 |
| ЗКН-И002 | «Шримати Радхарани» (кроме реального лица `radharani-de`); «Радха-» в компаундах — оставить | У1→У4 | `grep` одиночной «Радхарани» вне компаундов | 🔴 |
| ЗКН-И003 | «Гауранга Лила» / «Кришна Лила» — с заглавной, без дефиса (кроме slug/id) | У1→У5 | `grep -rn 'Гауранга-лила\|Кришна-лила' apps/web/src` (исключая slug) | 🟡 |
| ЗКН-И004 | **Полный титул Шрилы Прабхупады:** «Его Божественная Милость **Абхай Чаранаравинда** Бхактиведанта Свами Шрила Прабхупада, Ачарья-основатель Международного общества сознания Кришны, ИСККОН». Имя даётся **прописью**, не инициалами. Краткие формы допустимы («Шрила Прабхупада», «Прабхупада»). ЗАПРЕЩЕНА полусокращённая: «Его Божественная Милость **А.Ч.** …» — это ни полный титул, ни краткая форма. Цитаты не редактируются (ЗКН-БТ004) | У1→У4 | `grep` одиночного «Прабхупада» в UI-строках | 🔴 |
| ЗКН-Ц009 | **ХРАПОВИК ДОЛГА В ДАННЫХ.** Иногда нарушение **нельзя** исправить сейчас: источник не загружен, и любое «исправление» будет **догадкой**. Тогда долг **признаётся числом и запирается**: уменьшать можно, увеличивать — нет. Это честнее **двух плохих выходов**: молча закрыть глаза (гейт зелёный, **ложь живёт**) или удалить данные наугад (гейт зелёный, **правда потеряна**) | **У5** | `data-audit.py` · поле `baseline` | 🟢 |
| ЗКН-И005 | Канон-манджари — проектный канон приоритетнее источников (Рагхунатха Бхатта = **Раса**-манджари, не Рага; и т.д.) | У1→У3 | сверка `entity_profiles` по SQL | 🟡 |
| ЗКН-И006 | У каждой личности — авторитетная ПОЛНАЯ форма имени (титулы + имя, напр. полная форма Прабхупады из И004); в `entity_names`, полная форма — где уместна | У1→У3 | `entity_names` | 🟡 |

### Домен Т — Текст карточек · ✅ УТВЕРЖДЁН 10.07.2026
| ID | Закон | Ур. | Проверка | Статус |
|----|-------|-----|----------|--------|
| ЗКН-Т001 | Никаких «;» в тексте карточек. «A; b» → «A. B» | У4→У5 | `cleanCardText` + grep «;» в карточных полях | 🟡 |
| ЗКН-Т002 | **ЛЮБОЙ текст личности проходит через `cleanCardText`.** Очистка — **не косметика**: она правит канонические имена («Шри Чайтанья Махапрабху» → «Гауранга Махапрабху») и разбивает предложения, нанизанные через точку с запятой. Пропустили её ровно там, где ошибку уже **не исправить**: в **PDF**. Экран можно перевыкатить — **бумагу нельзя**. Человек унесёт её с собой, и ошибка переживёт все правки | У2→У5 | все `*Card.tsx`, показывающие прозу, импортируют `cleanCardText` (сейчас 2/13) | 🔴 |

### Домен Д — **ISKCON Design** · ✅ УТВЕРЖДЁН 11.07.2026
> Дизайн-система приложения. Источник истины: **`docs/STANDARD_design.md`**

| ID | Закон | Ур. | Проверка | Статус |
|----|-------|-----|----------|--------|
| ЗКН-Д001 | Шрифт/цвет/отступ/радиус — только через токены **ISKCON Design** (`tk.*` / `var(--…)`). Фирменное золото `#D2AA1B` = `--color-gold` | **У5** | `laws-lint` **храповик**: база `tools/laws-baseline.json`, рост долга роняет сборку. Сейчас: 1468 кеглей + 448 hex (было 510 — золото уведено в токен) | 🟡 |
| ЗКН-Д002 | **ПОВТОРЯЮЩИЙСЯ БЛОК БЕРЁТСЯ ИЗ ПРИМИТИВОВ, А НЕ КОПИРУЕТСЯ.** Одна и та же `plural` (русское число) лежала слово-в-слово в **семи** файлах — `BooksHub`, `KirtansScreen`, `DhamaScreen`, `DhamaDetailPage`, плюс под псевдонимами `pluralRu` (`SadhanaScreen`) и `pluralDays` (`VowScreen`, `AccountScreen`). Семь копий — семь мест, куда правка не доедет. Так же жили строка исполнителя и строка альбома в Киртанах: одна разметка в разделе, вторая — в результатах | **У5** | `laws-lint.py::check_d002` — `function plural`/`pluralRu`/`pluralDays` вне `ui/primitives.tsx` = fail | 🟢 |
| ЗКН-Д003 | Ноль шаблонного мусора в ядре (leftover имя `Apartsales`) | У5 | `grep -rniE 'apartsales' apps/web/src` = **0** ✅ Токены переименованы: `--color-gold` (#D2AA1B, логотип) + `--color-gold-deep` (#9c7c15, акцент); мёртвая синяя шкала заменена золотой рампой | 🟢 |
| ЗКН-Д004 | Общая планка интерфейса — уровень Apple/iOS 2026: Dynamic Type роли, 4pt-сетка, стеклянные поверхности, предсказуемое поведение; `tokens.ts` = TS-зеркало этого стандарта | У2 | дизайн-ревью | 🟡 |
| ЗКН-Д007 | **Суррогатные обложки ЗАПРЕЩЕНЫ.** Нет честного изображения → фирменная заглушка (Д005), а НЕ подделка. Суррогат = любое из: спектрограмма `archive.org/services/img` · буква-монограмма (инициал в круге/рамке) · самодельная типографская обложка с названием · авто-спарсенное случайное фото (640×640 «-ia») · `audio-cover*.png`. Честная заглушка лучше фальшивой обложки | **У5** | `laws-lint` — паттерны суррогатов роняют сборку. Снято: 4 типографских + 14 авто-парсинга + спектрограммы киртанов + монограммы артистов | 🟢 |
| ЗКН-Д007c | **БЕЛАЯ ЗАГЛУШКА В МАССИВЕ ОБЛОЖЕК ГЕРОЯ — ОБХОД ЗАКОНА.** ВБК сам рисует **ТЁМНУЮ** заглушку, когда обложек НЕТ (`n === 0`). Положить в массив белую = массив непуст, тёмная не срабатывает, и **белый текст ложится на светлый логотип** — нечитаемо. Массив обложек должен быть **ПУСТ**. Так я сломал обложки четырёх новых книг | **У5** | `laws-lint`: `covers: [COVER_FALLBACK]` = fail | 🟢 |
| ЗКН-Д009 | **ОТСТУП МЕЖДУ СЕКЦИЯМИ ВИТРИНЫ — ЕДИНЫЙ** (`SECTION_GAP = 28`). Было вразнобой: Книги 18 и 20, Киртаны 26 и 30, Прасад 14. Секции — это дыхание страницы; разный отступ читается как **разная важность там, где её нет** | **У5** | `ui/HubHeader.tsx` | 🟢 |
| ЗКН-Д008 | **Заглушка применяется ВЕЗДЕ, где нет фото — включая ДАННЫЕ.** Суррогат может лежать не только в коде, но и в БД: 339 бхаджанов хранили `hero_image='/audio-cover.png'` — после удаления файла показывались битые картинки. Проверять и код, и данные (SQL-аудит `prayers/tirthas/places/entity_profiles`) | У3→У5 | SQL-аудит + `laws-lint` | 🟡 |
| ЗКН-Д006 | **Пол типографики 11px.** Текст мельче 11px нечитаем (Apple HIG; правило записано в `globals.css`). Исключения: печатная вёрстка (mm/pt), декоративные глифы (◆), водяные знаки | **У5** | `laws-lint` — кегль < 11 роняет сборку. Исправлено 29 нарушений | 🟢 |
| ЗКН-Д005 | **Единая заглушка обложки.** Нет изображения → логотип ИСККОН золотом. **ДВА варианта по назначению:** ТЁМНАЯ (`cover-fallback-dark.svg`, #1E1E1E, 4/5, radius 20) — там, где ПОВЕРХ обложки идёт текст (ВБК, ВСК): скримы и белый текст работают как с фото. БЕЛАЯ (`cover-fallback.svg`) — где текста нет: миниатюры, аватары, круглая иконка ВМК, аудио. Белая подложка под белым текстом нечитаема, а скрим поверх белого гасит золото **Самодельные типографские обложки запрещены** (тёмный градиент + название золотом): название дублировалось на обложке и в тексте карточки. Сняты у 4 книг (Брахма-самхита, Говинда-лиламрита, Вишну-пурана, Кришна-сандарбха) → фирменная заглушка | У2 | буквы-инициалы и `audio-cover*.png` отменены | 🟢 |
| ЗКН-Д013 | **ЧУЖОЙ ГОЛОС — КУРСИВНЫМ GEORGIA. НО ПЕРЕВОД — НЕ СТИХ.** **ЧТО ЗВУЧИТ:** транслитерация стиха и куплета · деванагари (своим шрифтом `--font-deva`) · пословный санскрит · **ЦИТАТА** — стих в карточке личности и **стих внутри чужой прозы** (в комментарии, в житии, в статье) · святое имя. **ЧТО НЕ ЗВУЧИТ: ПЕРЕВОД.** Перевод стиха, перевод куплета, сниппет в поиске — это НАШ ЯЗЫК, тело книги, то, что человек ЧИТАЕТ. Так печатает BBT: транслитерация курсивом, **перевод прямым**, комментарий прямым. **Я применил голос к переводам — и вся книга уехала в курсивную Georgia. Читать стало невозможно.** Откачено: читалка · оглавление · бхаджан · поиск · избранное · загрузчик · Сегодня. **СТИХ ВНУТРИ ПРОЗЫ — ЭТО ФРАЗА, А НЕ НАБОР ПОМЕЧЕННЫХ СЛОВ.** Диакритика в комментариях BBT стоит не на каждом слове («Ведаиш́ ча сарваир ахам эва ведйах̣»), пословный матчер давал ШАХМАТНУЮ ДОСКУ. Полоса растёт от помеченного слова до нашей речи. В кавычках слабый слог якорит («кришнас ту бхагаван свайам» — стих без диакритики). Имя («Дурьйодхана») и термин («Вишну-таттвы») остаются ПРЯМЫМИ (ЗКН-С001). **Ловушка, стоившая всей детекции:** NFD разбирает «й» на «и» + краткую — снимая надстрочные знаки, мы убивали букву: `fold("анитйам") → "анитиам"` | **У5** | `ui/Skt.tsx::verseRuns` · `ui/voice.ts` · `.skt-voice` · `tools/voice-lint.py` · `tools/voice-data-lint.py` (по каждой строке базы, падает на пустой выборке) | 🟢 |
| ЗКН-Д014 | **ПОВЕРХНОСТЬ КОНТЕНТА — ЦВЕТ СТРАНИЦЫ, А НЕ СЕРАЯ ПЛАШКА.** `--color-glass-*` — стекло **КОНТРОЛОВ** (кнопка, чип, дорожка прогресса), а не заливка **КОНТЕЙНЕРОВ**. Карточка = поверхность `--color-bg-2` + волосяная линия `0.5px --color-hairline`. Залитая стеклом карточка на белом холсте читается как **дырка**: контент проваливается, интерфейс выглядит недоделанным. Это тот же диагноз, что у навигации (**ЗКН-Н011**), — он вернулся в плеер ленты, потому что закон был **без механизма**. Сняты три плашки: ВКЗ (аудио-карточка), голосовое, аудио-ссылка. **Следствия:** обложка не затемняется скримом (Д005: скрим гасит золото заглушки → серый квадрат); длительность видна **ДО** нажатия (метаданные тянутся по `IntersectionObserver`, а не весь киртан); пустых слотов и немого `0:00` нет; на чистой поверхности ♥/⋯ — **глифы без серых кружков** (`plain`), стеклянный кружок нужен только поверх медиа | **У5** | `tools/surface-lint.py` — гейт в `deploy-web.yml` (ПРАВИЛО 1 плашка-контейнер · 2 скрим · 3 второй плеер · 4 голый ♥/⋯), проверен на живых нарушениях (вернул плашку и скрим, снял `plain` у ленты → покраснел) | 🟢 |
| ЗКН-Д016 | **ТАБ-БАР — LIQUID GLASS iOS 26, ЭТАЛОН — APP STORE.** Основатель сверил бар со скринами App Store (15.07.2026) — расходились все три слоя. Материал Apple **линзирует**: гнёт и **увеличивает** контент под собой (в капле — масштабные копии ряда: mag ~1.2x по центру + edge-кольцо ~1.5x с аберрацией по кромке), а НЕ мылит blur-плёнкой; дисперсия — **спектральной каймой по кромке** (сферохроматизм), цветные градиенты-плёнки на выделении запрещены; капля **выше плашки** (D76 на 62) и течёт за пальцем пружиной, отрываясь от линзы покоя; кромка капсулы преломляет (кольцо-маска с backdrop). Долгое нажатие вскрывало меню как обычный текст — iOS показывала системную лупу и маркеры выделения → `-webkit-user-select: none` в `.gtab` обязателен. Стекло ПРОЗРАЧНО (белизна ~0.38, blur ~14) — сквозь капсулу читается структура контента, молочная непрозрачность запрещена; голограмма — спектральные кольца капсулы/линзы + блик, едущий за прокруткой (`--gx`, .gtab-glint) | **У5** | `tools/laws-lint.py::check_d016_liquidglass` — слои `.gtab-lens-mag`/`.gtab-lens-edge` обязаны существовать в CSS и в TSX, `user-select: none` в баре, маркеры старых цветных плёнок (`rgba(230, 120, 220` · `rgba(90, 150, 255`) в `globals.css` запрещены; проверен на живом нарушении (вернул плёнку → покраснел) | 🟢 |
| ЗКН-Д017 | **ИКОНКИ КАЛЕНДАРЯ — ТЕМАТИЧЕСКИЙ НАБОР + НАСТОЯЩАЯ ФАЗА ЛУНЫ.** Основатель: «Экадаши должны отмечаться иконкой — где полная луна, где убывающая; иконки событий должны соответствовать теме». Был ОДИН дежурный полумесяц на все Экадаши и смесь fill/stroke-глифов — разнобой, не Apple. Инвариант: **фаза Луны считается из ДАТЫ** (`moonPhase.ts`: шукла-экадаши → растущая почти полная, кришна-экадаши → убывающий серп, пурнима → полная, амавасья → новая); все глифы **одного тонкого веса** (восход — явление, пламя лампады — уход, искра — праздник, лист — Чатурмасья/зелень); цвет один — серый в покое, золото только на «сегодня/выбрано». Экран Экадаши и список календаря говорят **одним тихим языком карточек** (`--color-bg-2` + волосяная, ЗКН-Д014) — золотых плашек-градиентов нет. Один дежурный полумесяц на все экадаши запрещён | **У5** | `tools/nav-audit.py::check_d017` — `moonPhase.ts` отдаёт `moonPhase`+`moonLitPath`; `EventCard.tsx` строит глиф через `glyphFor`/`moonGlyph` (экадаши рисует `moonLitPath`), есть ветки пурнима/амавасья/Чатурмасья, `TypeIcon` принимает `date`+`title`; проверен на живом нарушении (убрал date из TypeIcon и ветку пурнима → покраснел) | 🟢 |
### Домен К — Карточки (юнит-стандарт) · ✅ УТВЕРЖДЁН 11.07.2026
> Полный источник истины: **`docs/STANDARD_cards.md`** (слоты, геометрия, матрица, алгоритмы).

| ID | Закон | Ур. | Проверка | Статус |
|----|-------|-----|----------|--------|
| ЗКН-К001 | Обложка-слайдер — единый модуль `CardCover` (`useCoverSlider`), своя реализация запрещена | **У5** | `cards-audit.py` | 🟡 |
| ЗКН-К002 | Действия (♥ избранное · ⋯ меню) обязательны во ВСЕХ трёх витринных форматах и идут только через `cardActions`  **Оговорка:** у своей заметки нет «избранного» — вместо ♥ стоит 📌 «закрепить». | **У5** | `cards-audit.py` | 🟡 |
| ЗКН-К003 | Ноль дрейфа док↔код ВНУТРИ файла. Было живое: `CardCover` док говорил «3 секунды», а константа = 7000 мс | **У5** | `cards-audit.py` сверяет комментарий с константой | 🟢 |
| ЗКН-К004 | **ВБК** (витринная большая / hero): обложка 4/5, radius 20, текст НА обложке. Слоты строго: лила·волна (золото) → имя → IAST → титул/категория → предложение «за что знает история» → тождество (кросс-лила) → 3 чипа → действия | У1→У2 | `STANDARD_cards.md §2` | 🟡 |
| ЗКН-К005 | **ВСК** (витринная средняя): те же 4/5, половина ширины = ровно ¼ площади ВБК (сетка 2×2). Слоты: лила → имя → одна строка-квалификатор → действия. Без IAST, предложения, чипов | У1→У2 | `STANDARD_cards.md §3` | 🔴 |
| ЗКН-К006 | **ВМК** (витринная минимальная): полоска, круглая иконка 52px (плей — если аудио) → название · автор · контекст → скраббер+время (только аудио) → действия | У1→У2 | `STANDARD_cards.md §4` | 🔴 |
| ЗКН-К007 | **ПК** (подробная) = **ВБК целиком, без изменений** + табы/суб-табы. Своей шапки у ПК НЕТ — расхождение с ВБК есть нарушение | У1→У2 | `STANDARD_cards.md §1, §9` | 🔴 |
| ЗКН-К008 | Правило выбора формата: ведёт картинка → ВСК · ведёт текст/время/звук → ВМК · одна главная вещь → ВБК · полный юнит → ПК | У1 | `STANDARD_cards.md §7` | 🔴 |
| ЗКН-К009 | Полнота форматов: у каждого элемента библиотеки проработаны ВСЕ применимые ему форматы; показ в формате, которого нет по матрице, — нарушение | У1→У3 | матрица `STANDARD_cards.md §8` | 🔴 |
| ЗКН-К010 | Классификатор чипов (ВБК): ровно 3 · только ФАКТ (дата/число/имя/место), не оценка · не повторять сказанное выше · лестница приоритетов (даты → число трудов → место → вклад → раса → парампара → праздник) · нет короткой формы → факт не берётся | У1→У2 | `STANDARD_cards.md §5` | 🔴 |
| ЗКН-К013 | **Ни один слот не дублируется.** Что уже есть в карточке — не повторяется подписью рядом с ней. Было: раса печаталась и в надписи ВБК («КРИШНА ЛИЛА · МАДХУРЬЯ»), и отдельной строкой под карточкой | У2→У5 | `cards-audit.py` | 🟢 |
| ЗКН-К011 | **Нет переносов** в коротких слотах: 3 чипа ВБК — в один ряд; строка-квалификатор ВСК — одна строка (без префикса «В Кришна Лиле —»); строки ВМК — nowrap. Исключение: имя (до 2 строк) | У1→У2 | `STANDARD_cards.md §6` | 🔴 |
| ЗКН-К012 | Полный «адрес» личности = классификация (лила·волна) + титул + тождество, выведенные из графа | У1→У3 | `EntityPage`/граф | 🟡 |

### Домен Н — Навигация · ✅ УТВЕРЖДЁН 11.07.2026
| ID | Закон | Ур. | Проверка | Статус |
|----|-------|-----|----------|--------|
| ЗКН-Н001 | Никакого `pushState`/`replaceState` мимо `src/nav.ts` (иначе теряется `appIdx` и ломается «назад») | **У5** | `laws-lint` — прямая запись истории роняет сборку. Было 2 нарушения: `LichnostiHub` и `player/store` (последний писал `replaceState(null)` и СТИРАЛ `appIdx`) | 🟢 |
| ЗКН-Н002 | Только ОДИН глобальный `popstate` (в `App`); решений на `history.length` — нет | У1→У5 | `grep 'history.length'` = 0 | 🟡 |
| ЗКН-Н003 | «Назад» никогда не выкидывает за пределы сайта; на корне → логический родитель | У2 | ручная сверка `goBack` | 🟢 |
| ЗКН-Н005 | **Каждый уровень навигации — в адресной строке.** Переключение ЛЮБОГО таба/суб-таба ОБЯЗАНО менять URL (deep-link, «назад», шаринг). Состояние навигации в переменной модуля — нарушение (было: `bogSub`, `sadSub`). Садхана: `/` · `/practice` · `/calendar` · `/account`. Богатства: `/dhana` · `/dhana/books` · `/dhana/bhajans` · `/dhana/kirtans` · `/dhana/recipes` · `/dhana/dhama` | У2→У5 | grep: суб-таб без `pushUrl`/`replaceUrl` | 🟡 |
| ЗКН-Н006 | **ЧЕТЫРЕ УРОВНЯ = ЧЕТЫРЕ МЕХАНИЗМА** (утв. основателем 11.07.2026). Уровень опознаётся МЕХАНИЗМОМ, а не оттенком серого: **Tier-1** витрины — **ЛИНИЯ** (`HallTabs`, золотая рейка, sticky) · **Tier-2** область — **РАЗМЕР** (`ScopeTitle`, крупный кегль 21px + вес, без декора) · **Tier-3** фильтр — **КОНТУР** (`FilterChips`, обводка капсулы, активная золотом, БЕЗ заливки) · **Tier-4** группы — **РАСКРЫТИЕ** (`Disclosure` в списке: структура, а НЕ ряд меню). **ЗАЛИВОК НЕТ НИГДЕ** — вес несёт типографика (чёрная капсула = самое тяжёлое пятно в интерфейсе). Счётчики — верхним индексом («Гауранга Лила⁴⁵⁰»), они справка, а не участник иерархии. Золото — только маркер «где я». Единый источник: `apps/web/src/ui/nav4.tsx` — на ВСЕ витрины, не только Личности | У2 | `ui/nav4.tsx` импортируется хабами | 🟢 |
| ЗКН-Н010 | **Липкие слои не накладываются.** Каждый sticky-уровень липнет ПОД вышестоящим: `top: var(--h-hall-tabs)`, а не `top: 0`. Два слоя с `top: 0` = нижний уезжает под верхний и исчезает (был баг: Tier-2 «Гауранга Лила» пропадал при выборе волны). Высота Tier-1 задана токеном, не на глаз | У2→У5 | grep: `position:sticky;top:0` более одного раза в стеке | 🟡 |
| ЗКН-Н007 | **БОГАТСТВА = 7 ВИТРИН, И ПОРЯДОК ИХ — ЗАКОН.** Порядок утверждён основателем 13.07.2026, «Катха» добавлена после «Книг» 18.07.2026: **Книги · Катха · Личности · Бхаджаны · Киртаны · Дхама · Прасад**. Порядок живёт в ОДНОМ месте (`BOG_SUBS`), рейка `HallTabs` идёт тем же списком: два порядка = два разных приложения. Вход в Личности = «Герои» (витрина трёх входов), глубже — четырёхуровневое меню | **У5** | `nav-audit.py::check_n007` | 🟢 |
| ЗКН-Н008 | **Адрес — человекочитаемый и правдивый.** Сегмент URL = смысл страницы, а не внутренний id. ЗАПРЕЩЕНЫ: непрозрачные коды (`g64`, `vrnd2`, `pobeg1`), англо-технические огрызки (`unique-4`, `prema-ladder`, `classic-episodes`, `gauranga-link`), и ВВОДЯЩИЕ В ЗАБЛУЖДЕНИЕ (`obzor` на странице о пяти расах). Слаг отражает label: «Пять рас» → `pyat-ras`, «64 качества» → `64-kachestva` | У3→У5 | SQL-аудит `subtabs[].id` по всем 706 ПКЛ | 🟡 |
| ЗКН-Н009 | **Смена уровня сбрасывает нижние в «Все».** Выбрал лилу → волна и группа = «Все» (показать ВСЁ содержимое), а не первый попавшийся под-фильтр. Пользователь просил раздел целиком, а не его случайный кусок | У2 | `pickLila`/`pickSub` | 🟢 |
| ЗКН-Н011 | **Навигация — стекло ЦВЕТА СТРАНИЦЫ, а не серая плашка.** Sticky-меню не имеет собственного серого фона: оно полупрозрачно в цвете холста (`--color-glass-nav` = белый/чёрный по теме, не `rgba(250,250,252)`). Серый бар поверх белого холста = нарушение | У2→У5 | grep серых фонов у `position:sticky` | 🟢 |
| ЗКН-Н012 | **Меню не режет отступ до контента.** Sticky-навигация обязана оставлять воздух снизу (`margin-bottom: 14px`); нулевой отступ прижимает текст к меню | У2 | `ui/nav4.tsx` | 🟢 |
| ЗКН-Н013 | **Счётчик — настоящая степень.** Пишется как показатель степени (xⁿ): доля от кегля родителя (`0.56em`), `vertical-align: super`, `line-height: 0` (не растягивает строку). Фиксированный кегль (11px) рядом с заголовком 21px степенью не выглядит | У2 | `ui/nav4.tsx::Sup` | 🟢 |
| ЗКН-Н014 | **Досье и витрины СИНХРОНИЗИРОВАНЫ.** Одна сущность (волна, раса, группа) не может жить в двух местах без перехода между ними. Из суб-таба досье («Парикары» Гауранги/Кришны) — прямой мост в Богатства → Личности с тем же фильтром. Слаги отличались (`volna-2` в досье vs `2-volna` в витрине) — мост переводит | У2 | `EntityPage::bridgeHref` | 🟢 |
| ЗКН-Н015 | **Алиасы сущностей.** Одна Личность = один `id`, но у неё есть общеупотребимые адреса-синонимы: `gauranga` ≡ `chaitanya` (одна Личность — ЗКН-Сд002). Без алиаса `/gauranga` отдавал 404 и белую страницу | У3 | `worker.ts::ENTITY_ALIAS` | 🟢 |
| ЗКН-Н016 | **Система навигации — во ВСЕХ экранах, а не в витринах.** Закон, применённый к части экранов, не применён. `ui/nav4.tsx` обязателен везде: Личности · Книги · Прасад · ИСККОН · **Центры · Рестораны · Документы · Календарь · Дхама** · Киртаны · Бхаджаны. Запрещены и свои копии чипов, и старый `SectionSubTabs variant="chips"` (чёрные капсулы) — именно им остались Центры/Рестораны/Документы, и первый линтер их не видел | **У5** | `laws-lint`: `function FilterChip\|Pills\|SegRow` ИЛИ `variant="chips"` = fail | 🟢 |
| ЗКН-Н017 | **Витрина внутри зала не имеет своей шапки и «назад».** Навигация зала — `HallTabs`; своя кнопка «назад» и свой заголовок внутри витрины = дубль навигации (было в Прасаде: «‹ Прасадам»). Витрина не создаёт свой скролл (`100dvh`) — она внутри скролла зала. Разделы витрины = Tier-2 (`ScopeTitle`) | У2→У5 | grep `onBack` в витринах зала | 🟢 |
| ЗКН-Н018 | **Главная страница витрины — её суть, а не список.** Витрина «Прасад» (`/dhana/prasad`): главная = книга «Кухня прасада» (оглавление), рецепты/подбор/подношение — разделы Tier-2. Глава книги открывается **переходом**, а не раздутием страницы | **У5** | `nav-audit.py` (проверен на живом нарушении) | 🟢 |
| ЗКН-Н019 | **Якорная навигация длинной страницы не уезжает при скролле.** Если Tier-2 служит оглавлением (ИСККОН: О движении · Сегодня · Мантра…), он ОБЯЗАН липнуть под Tier-1 — иначе им нельзя пользоваться, пролистав страницу. Фильтр может уезжать, оглавление — нет  **Tier-2 (область) — тоже навигация: липнет под Tier-1 и НЕ уезжает.** Клик по Tier-3 прокручивал страницу и прятал Tier-2 под шапку | У2 | `FilterChips sticky` | 🟢 |
| ЗКН-Н021 | **Обёртка `<div>` вокруг липкого меню ЛОМАЕТ sticky.** Высота обёртки равна высоте меню — у `sticky` нет хода, и он уезжает вместе с ней. Ссылку на узел брать через `navRef`, а не оборачивать. Так «не фиксировалось» меню ИСККОН | У2→У5 | `ui/nav4.tsx::navRef` | 🟢 |
| ЗКН-Н023 | **СТАНДАРТ АДРЕСОВ** — см. `docs/STANDARD_urls.md`. Раздел = ОДИН сегмент в корне (`/books`, не `/dhana/books`). Углубление — сегментами вниз. Личность — в корне (`/abhimanyu`). Корни разделов зарезервированы. Никаких чужих путей в слаге: у всех 339 бхаджанов слаг был `/ru/gaura-arati` — **путь с сайта-источника**, утёкший в базу. Старый адрес → 301 | У1 | документ (внедрение) | 🔴 |
| ЗКН-Н023b | **АДРЕС КНИГИ — ПОЛНОЕ ИМЯ, В КОРНЕ.** `gaurangers.com/book/bs` — двойное нарушение: **лишняя папка** `/book/` и **ШИФР «bs»** вместо имени. Человек «bs» не прочтёт и не запомнит. Стало `/brahma-samhita`. Внутренний ключ остаётся коротким (на нём висят стихи и разделы), наружу идёт слаг. Проверено: ни один слаг книги не сталкивается ни с личностью, ни с корнем раздела | **У5** | `data-audit.py` | 🟢 |
| ЗКН-Н034 | **КНОПКА ВЕДЁТ ПО АДРЕСУ, А НЕ ШЛЁТ СОБЫТИЕ.** Кнопки Практики слали `CustomEvent`. Имена **не совпадали** с тем, что слушал App (`iol:open-daily-verse` против `iol:open-verse`), а ветка-перехватчик в `applyPath` ловила `/japa` **первой** и делала `return` — настоящие обработчики **не достигались**. **Ни одна кнопка Практики не работала.** События выброшены: кнопка → адрес → экран. Один путь | **У5** | `nav-audit.py` | 🟢 |
| ЗКН-Н035 | **ЭКРАН СМЕНИЛСЯ → АДРЕС СМЕНИЛСЯ. ВСЕГДА.** `openEntityTarget` открывал карточку и **адрес не трогал**. Из Календаря открыл Бхактивиноду Тхакура — в строке остался **`/calendar`**; отправил ссылку другу — тот получил **КАЛЕНДАРЬ**. Нажал «назад» — история не знает про карточку, **выбросило на `/verse`**. Так же: бхаджан, книга, киртания, контент — прямые сеттеры без адреса. **Адрес — обещание: «вот что ты видишь». Экран без адреса ЛЖЁТ.** Всё открытие идёт через `navigate` | **У5** | `nav-audit.py` (проверен на живых нарушениях) | 🟢 |
| ЗКН-Н036 | **ОДНО СОДЕРЖИМОЕ — ОДНО МЕСТО.** «Тексты бхаджанов» жили И в Бхаджанах, И в Киртанах. Человек не понимает, где искать и полный ли перед ним список. **Киртаны — это ЗВУК** (альбомы, исполнители, жанры). **Бхаджаны — это ТЕКСТ** (молитвенник) · **НАДПИСИ РАЗДЕЛОВ В КИРТАНАХ УБРАНЫ** (решение основателя 13.07.2026): «ЗВУЧИТ · Слушать сейчас», «ГОЛОСА СВЯТОГО ИМЕНИ · Исполнители», «КЛАССИФИКАЦИИ · Жанры и настроения». Каждая — надпись золотом над заголовком, то есть **вторая шапка витрины** поверх единственной законной (ЗКН-Н024). Содержимое говорит само за себя: альбомы — альбомы, список имён — исполнители, чипы — фильтр. Подпись, которая называет очевидное, — шум | **У5** | `nav-audit.py` | 🟢 |
| ЗКН-Н033 | **ЗАПАСНОЙ НЕ ПЕРЕБИВАЕТ ОСНОВУ. ПОДТАБ ВЫВОДИТСЯ ИЗ АДРЕСА.** КОРЕНЬ, КОТОРЫЙ Я ИСКАЛ ПЯТЬ ЗАХОДОВ. **(1)** В конце `applyPath` стояло голое `setTab("iskcon")` — оно **молча перебивало основу**, поставленную `BASE_OF`, для КАЖДОГО адреса без своей ветки. **Ветки для `/gauranga` не было вовсе** — так терялось **13 адресов**. **(2)** `tab`/`sub` в `EntityPage` были **состоянием**, снятым с адреса ОДИН РАЗ при монтировании. Адрес менялся — состояние оставалось: смена царства уносила подтаб с собой (Кришна/Качества → Гауранга/Качества). **`key` (Н028) это НЕ лечил** — беда была не в монтировании, а в том, что компонент **помнил** подтаб вместо того, чтобы **читать** его. **Адрес — единственный источник истины** | **У5** | `nav-audit.py` (проверен на трёх живых нарушениях) | 🟢 |
| ЗКН-Н031 | **ВИТРИНА НЕ СТАВИТ ОВЕРЛЕЙ. ВИТРИНА ВЫВОДИТСЯ ИЗ АДРЕСА.** Два корня «белых экранов через раз». **(1)** В `Screen` — **тернарная цепочка из 36 ветвей**, и каждая рисуется **ВМЕСТО всего приложения** (без нижнего меню, без зала). Голый `/prasad` ставил `setPrasadamSection` → ветвь перехватывала → экран без меню. Витрину рисует **зал**; оверлей ставится только глубже (`/prasad/<рецепт>`). **(2)** `sub` читался **только при монтировании** зала: из ИСККОН на `/kirtans` — зал монтируется, всё верно; из `/books` на `/kirtans` — **зал не монтируется, `sub` остаётся старым**. Отсюда «через раз» | **У5** | `nav-audit.py` (проверен на живых нарушениях) | 🟢 |
| ЗКН-Н032 | **КОМПОНЕНТ ПИШЕТ АДРЕС ИЗ СЕБЯ, А НЕ ИЗ ЧУЖОГО СЕГМЕНТА.** `EntityPage` брал основу из `segs[0]` — первого сегмента **текущего** адреса. При смене царства старый компонент **ещё не размонтирован**, его эффект срабатывает уже с **новым** адресом и дописывает **свой** подтаб: `/gauranga/guna/milost`. Нажал Гаурангу — попал в «Качества». `key` (Н028) это **не лечит**: эффект успевает сработать до размонтирования | **У5** | `nav-audit.py` · `REALM_BASE` | 🟢 |
| ЗКН-Н029 | **КАЖДЫЙ АДРЕС ОБЪЯВЛЯЕТ ОСНОВУ.** Было: **25 из 42** веток `applyPath` открывали оверлей, **не сказав, что под ним**. Человек стоял на «Богатства → Книги», жал «Джапа» — счётчик открывался, а **закрыв его, он попадал в Книги**, хотя джапа это Садхана. Свежий заход по ссылке `/japa` (закладка, QR) давал **оверлей над пустотой**. **Оверлей без основы — оверлей над неизвестностью.** Два вида: **оверлей РАЗДЕЛА** приносит свою основу (джапа→Садхана, центры→ИСККОН, читалка→Богатства); **оверлей ПРИЛОЖЕНИЯ** (поиск · избранное · корзина · заметки · донат) основу **не трогает**. Механизм: таблица `BASE_OF`, основа ставится ПЕРВОЙ | **У5** | `nav-audit.py` (проверен на двух живых нарушениях) | 🟢 |
| ЗКН-Н030 | **«НАЗАД» И «ЗАКРЫТЬ» — РАЗНОЕ.** «Назад» — шаг по истории. «Закрыть» — **снять оверлей и показать основу**. Видно, когда пришёл **по прямой ссылке**: истории нет, и `goBack` выкидывал на «/» — открыл рецепт по ссылке, закрыл, **оказался на главной, а не в Прасаде**. Теперь `closeOverlay()` ведёт **к основе этого адреса** | **У5** | `nav-audit.py` · `closeOverlay()` | 🟢 |
| ЗКН-Н027 | **ОДИН ЭКРАН — ОДИН ПУТЬ РЕНДЕРА. ВКЛАДКА — ТОЛЬКО ЗАКОННАЯ.** ЭТО КОРЕНЬ ЦЕЛОГО КЛАССА ПОЛОМОК. В приложении жили **ДВА пути** к одному экрану: `tab === "bogatstva"` → зал → **HallTabs + витрина** (с меню) и `tab === "books"` → BooksHub **НАПРЯМУЮ** (без меню). Так же: kirtans · dhama · acharya · account · home. Отсюда **«один и тот же адрес то открывается, то нет»**: попадёшь через адрес — увидишь меню, через `setTab("books")` — не увидишь. А `tab === "prasad"` **не существовало вовсе** → **БЕЛЫЙ ЭКРАН**. Правила: (1) `setTab` принимает только одну из **пяти** вкладок нижнего меню; (2) витрина рисуется **только в зале** | **У5** | `nav-audit.py` (проверен на живом нарушении) | 🟢 |
| ЗКН-Н028 | **СМЕНА ЦАРСТВА = ПЕРЕМОНТИРОВАНИЕ (`key`).** Без `key` React переиспользует `EntityPage`, и **подтаб переносится**: был на Кришна → «Качества», нажал Гауранга — попал сразу в «Качества», а не на главный экран. Царство сменилось, состояние — нет | **У5** | `key="realm-*"` · `nav-audit.py` | 🟢 |
| ЗКН-Н026 | **ЭКРАН БЕЗ АДРЕСА — КАК БУДТО ЕГО НЕТ.** Вкладки ИСККОН (Новости · Центры · Рестораны · Образование · Структура · Документы · Ссылки) держались в `sessionStorage` и **адрес не писали вовсе**. Человек открывал «Центры», а в строке оставалось `/books`: разделом **нельзя было поделиться**, положить в закладку, вернуться назад. Каждый экран, на который можно перейти, ОБЯЗАН иметь адрес | **У5** | `HomeTabs::pathOfTab` · `url-audit.py` | 🟢 |
| ЗКН-Н025 | **АДРЕС ПЕРЕЕХАЛ → ПЕРЕЕХАЛИ ВСЕ, КТО ЕГО ЧИТАЛ.** Я сломал витрину Личностей **дважды подряд, одинаково**: (1) перевёл адреса на `/lichnosti/…`, а `LichnostiHub` продолжал ЧИТАТЬ `/dhana/…` — все три кнопки вели в Гауранга Лилу; (2) перевёл на `/gauranga-lila`, а в зале остался `pp.startsWith("/lichnosti/")` — кнопки перестали вести вглубь. **Оба раза: писатель адреса переехал, читатель — нет.** Это СВОЙСТВО задачи: адрес живёт в двух местах (где СТРОЯТ и где РАЗБИРАЮТ), в разных файлах, ничем не связанных. **Молчаливость — худшее:** ничего не падает, сборка зелёная, кнопка просто ведёт не туда. Реестр переездов `MOVED` пополняется при КАЖДОМ переименовании  **Гейт ловит уже ЧЕТВЁРТЫЙ раз одну и ту же ошибку:** (1) `LichnostiHub` читал `/dhana/`; (2) зал ждал `/lichnosti/`; (3) `bogSubFromPath` читал ВТОРОЙ сегмент (`/dhana/books`) — после переезда витрины в корень **верхнее меню пропадало на `/books` и `/dhama`**; (4) Прасад ждал `/prasad/recipe/<x>` — рецепты не открывались, пустые экраны. **Всегда одно: адрес переехал, читатель остался.** Молчаливо: сборка зелёная, экран пустой  **И ГЛУБИНА ТОЖЕ ПЕРЕЕЗЖАЕТ.** Разбор адреса книги считал от старой схемы `/book/<шифр>/<глава>/<стих>` — глава в `parts[3]`, стих в `parts[4]`. После переезда книга живёт в **КОРНЕ**: `/bhagavad-gita/2/13` → глава в `parts[2]`, стих в `parts[3]`. **Папки `/book/` больше нет — а индексы остались от неё.** Книга читала ПУСТОТУ, и **главы со стихами не открывались**. Переезжают не только имена, но и **ГЛУБИНА** | **У5** | `tools/url-audit.py` | 🟢 |
| ЗКН-Н045 | **УМОЛЧАНИЕ ВКЛАДКИ = ЕЁ ПЕРВЫЙ ПОДТАБ. НЕ ОТДЕЛЬНО ВПИСАННЫЙ АДРЕС.** Основатель переставил витрины (Книги первыми) — а нижняя вкладка «Богатства» продолжала вести **в Личности**. И вкладка «Садхана» вела **в Практику**, хотя первый раздел рейки — Даршан. Причина ОДНА: умолчание было вписано руками в `HOME_OF` (`bogatstva: "/hero"`, `sadhana: "/sadhana"`) — **отдельно от порядка подтабов**. Переставили порядок — умолчание осталось на месте и **промолчало**. Это не забывчивость, это ВТОРОЙ СПИСОК; внимательностью такое не лечится. Теперь порядок → рейка → умолчание **выводятся из ОДНОГО массива** (`BOG_SUBS`/`SAD_SUBS` → `BOG_TABS`/`SAD_TABS` → `HOME_OF`): переставил список — переставилось всё. Рейку запрещено писать литералом в JSX | **У5** | `nav-audit.py::check_n045` | 🟢 |
| ЗКН-Н044 | **У ВИТРИНЫ ЕСТЬ ПОИСК, И ОН ОДИН.** Шапку общим компонентом мы свели (ЗКН-Н024) — а поиск оставили каждому, и он разъехался хуже: разъехалось само **НАЛИЧИЕ**. Книги — лупа, очистка, Enter→первый, рамка-волосок (эталон). Бхаджаны — **голый `input`**: ни лупы, ни очистки, радиус 12, свой кегль. Дхама — капсула `fill-1` высотой 40 без рамки. Личности (первый экран) — **поиска НЕТ ВООБЩЕ при 706 героях**, хотя шапка файла обещала «живой поиск по всем героям», а `ResultRow` лежал готовым и **ни разу не вызывался**. Киртаны — **нет**. Прасад — **нет** при сотнях рецептов. Внутри одной витрины «Личности» поле на первом экране отсутствовало, а на втором (`LichnostiHub`) было **четвёртой копией** со своими `.lh-search`. Человек переключает вкладку — поле то есть, то нет, то другое. **Витрина без поиска — витрина, в которой нельзя ничего найти:** всё содержимое лежит за перелистыванием. Поиск, счётчик результатов и «ничего не найдено» — `HubSearch` · `HubCount` · `HubEmpty`, один файл на всё приложение | **У5** | `nav-audit.py::check_n044` · `ui/HubHeader.tsx` | 🟢 |
| ЗКН-Н024 | **ШАПКА ВИТРИНЫ — ОДИН КОМПОНЕНТ.** Каждая витрина рисовала шапку сама: отступы 4/6/8/нет, у половины **не было описания**. Человек переключал вкладку — и текст прыгал. Лечится ТОЛЬКО общим `HubHeader`: пока каждая рисует сама, договорённость разъедется снова. **Надпись золотом не повторяет вкладку** (вкладка «Личности» → надпись «Вечные спутники»)  **РАССТОЯНИЕ ОТ МЕНЮ ДО НАДПИСИ — ЕДИНОЕ, ОДИН РАЗ, В ЗАЛЕ** (`HUB_TOP = 28`, эталон Бхаджанов). Было: Бхаджаны 28, **остальные — НИКАК**, шапка липла к меню. Витрина об отступе не знает: пока каждая ставит свой, договорённость разъедется снова  **ШАПКА ОДНА НА ВСЁ ПРИЛОЖЕНИЕ, А НЕ «В БОГАТСТВАХ».** Первая версия гейта стерегла только шесть витрин — и пропустила **ВТОРУЮ шапку** (`SectionHead` в ИСККОН): отступ 16 против 14, тег `h2` против `h1`, вес надписи 700 против 600, отступ описания 8 против 4. Человек переходил из Богатств в ИСККОН — **и текст прыгал**. Две шапки = две типографики. Отступ от меню — единый (`HUB_TOP = 28`), живёт в зале, витрина о нём не знает · **ОТСТУП ВИТРИНЫ — НИ СВЕРХУ, НИ СБОКУ.** Гейт стерёг только `<div style={{marginTop}}>` — и **пропустил `<section style={{marginTop: 28}}>` в Бхаджанах**: 28 поверх зальных 28, шапка Бхаджанов сидела на 28px ниже всех остальных. И пропустил **боковой**: Прасад ставил свой `padding: 18px 16px` поверх зальных 16px — его содержимое было вдвинуто на 16px глубже всех и не совпадало даже со своей же рейкой разделов. **И ШАПКА ПРИНАДЛЕЖИТ ВИТРИНЕ, А НЕ ЕЁ РАЗДЕЛУ:** в Прасаде `HubHeader` жил ВНУТРИ раздела «Книга» — на «Рецептах», «Подборе», «Кому дорого» и «Подношении» надзаголовка с заголовком **не существовало вовсе** | **У5** | `nav-audit.py` · `ui/HubHeader.tsx` | 🟢 |
| ЗКН-Н022 | **Один Tier-1 на приложение.** `HomeTabs` (46px) и `HallTabs` (40px) давали разную высоту → липкий Tier-2 вставал со щелью (было видно в Центрах). Высота Tier-1 — всегда `var(--h-hall-tabs)` | У2 | сверка высот | 🟢 |
| ЗКН-Н004 | Верхнее меню (везде): поиск — слева, избранное (сердце) — справа, вордмарк ISKCON ONE LOVE по центру → на Главную; нативный поиск по всему приложению | У2 | `App.tsx` TopHeader, `STANDARD_navigation.md §4` | 🟢 |
| ЗКН-Н046 | **ОДИН СУБ-ТАБ — НЕ СУБ-ТАБ.** Кнопка выбора, у которой один вариант, ничего не выбирает: она крадёт строку экрана и заставляет человека кликать туда, где выбора нет. Суб-табы рисуются, только если их больше одного | У1 | `EntityPage.tsx` (`subItems.length > 1`) | 🟡 |
| ЗКН-Н047 | **ОРГАН ВЫБОРА СЛЕДУЕТ ЗА ЧИСЛОМ РАЗДЕЛОВ.** Лента пилюль верна, пока разделов мало: три лилы ЧЧ, двенадцать песней ШБ — глаз охватывает целиком. На 82 исполнителях и на 90 главах песни она перестаёт быть навигацией: чтобы дойти до последнего, нужно восемьдесят свайпов вслепую. Механизм разделов при этом ПРАВИЛЬНЫЙ — меняется орган выбора. **≤ 14 разделов → пилюли. > 14 → кнопка текущего раздела + лист: поиск, азбука, счёт записей.** Порог не выдуман: 14 пилюль ≈ два экрана прокрутки, дальше человек уже не помнит, что было слева. | `DivisionPicker` в `NowPlaying.tsx`; `PILL_LIMIT = 14` |
| ЗКН-Н048 | **У ДЛИННОГО СПИСКА ЕСТЬ ЯКОРЯ.** «Все» — это 1062 строки подряд. Без разделителей человек не знает, ГДЕ он, и не может выйти к исполнителю той записи, что видит перед собой. Липкий заголовок отвечает на оба вопроса разом: он говорит «ты здесь» и по тапу открывает папку этого исполнителя. Список длиннее ~40 строк без якорей — это стена, а не навигация. | липкие заголовки разделов в очереди `NowPlaying.tsx` |
| ЗКН-Н049 | **ИГРАЮЩУЮ ЗАПИСЬ ВСЕГДА МОЖНО НАЙТИ.** В папке на 140 записей достаточно пролистать — и то, что звучит, потеряно: человек не знает даже, в какую сторону крутить. Кнопка «К текущей» появляется РОВНО тогда, когда играющая строка ушла из поля зрения, и исчезает, когда она видна. Кнопка, висящая всегда, — шум; кнопка, которой нет в нужный момент, — провал. | `IntersectionObserver` на активной строке, `NowPlaying.tsx` |
| ЗКН-Н050 | **ОДИН ЭКРАН — ОДНА ЗАДАЧА. ТРАНСПОРТ — ВСЕГДА.** Обложка, список на 1062 строки и транспорт были сложены в ОДИН прокручиваемый столбец — и мешали друг другу: обложка сдувалась при прокрутке, транспорт уезжал, внизу оставалась пустая плешь. Но это ТРИ РАЗНЫХ МОМЕНТА: «слушаю» (что звучит), «выбираю» (что ещё есть) и «управляю» (пауза, перемотка — нужно ВСЕГДА, в обоих случаях). Отсюда: **две панели («Сейчас» / «Очередь») + транспорт, прибитый ко дну**. Панель занимает весь остаток высоты — пустая плешь становится невозможной ПО УСТРОЙСТВУ, а не по настройке отступов. | сегмент + `flex:1` панель + `flexShrink:0` транспорт в `NowPlaying.tsx` |
| ЗКН-Н051 | **НАЗЫВАЕМ ТЕМ, ЧЕМ ЧЕЛОВЕК УПРАВЛЯЕТ, А НЕ ТЕМ, КАК УСТРОЕНО.** «Очередь» — слово ДВИЖКА: так называется структура воспроизведения внутри. Человек, пришедший слушать святое имя, не думает «очередь»; он думает: что играет · что ещё есть · чей это голос. Правильное слово уже стояло в шапке витрины: «Святое имя **в голосах** ачарьев и киртания». **Очередь → Записи. Папки → Голоса. Дорожки → Записи.** Слово в интерфейсе — часть проектирования, а не подпись под ним. | вкладки плеера, `NowPlaying.tsx` |
| ЗКН-Н052 | **СЕРДЦЕ СТАВИТСЯ НА ЗАПИСЬ, А НЕ НА АЛЬБОМ.** Сердце висело на всей коллекции: нажал — «добавлено в избранное», а что именно добавлено, непонятно. Человек хочет отметить КОНКРЕТНЫЙ киртан — тот, что сейчас звучит и лёг на сердце. Без этого вкладка «Моё» — пустое обещание. Ключ — путь дорожки в архиве: он уникален и переживает переименования. | `trackKey()` + `addFavorite/removeFavorite`, `NowPlaying.tsx` |
| ЗКН-Н053 | **ЧЕЛОВЕК ЗНАЕТ, ЧТО ЗАЗВУЧИТ ДАЛЬШЕ.** Плееру, который молчит о следующем, не доверяют: его держат за руку и не отпускают — а киртан слушают часами, фоном. Одна строка «Дальше: …» снимает тревогу и отдаёт человеку время. | строка «Дальше» на панели «Играет», `NowPlaying.tsx` |
| ЗКН-Н054 | **ТАЙМЕР СНА — НЕ УДОБСТВО, А ПРИРОДА ПРЕДМЕТА.** Киртан слушают ЗАСЫПАЯ: святое имя ставят на ночь. Плеер без таймера заставляет ПРОСЫПАТЬСЯ, чтобы его выключить — и человек либо не ставит киртан на ночь вовсе, либо утром находит разряженный телефон. Два вида: «через N минут» и **«после этой записи»** — второй важнее: киртан не обрывают на середине. | `setSleep()` в `store.tsx`, лист таймера в `NowPlaying.tsx` |
| ЗКН-Н055 | **КРУГ ПОВТОРА СЛЕДУЕТ ЗА ПРЕДМЕТОМ.** У книги уровни повтора — глава → книга → библиотека. У аудиотеки книги нет, есть **ГОЛОС**: запись → голос → вся коллекция. Один круг на оба предмета был бы ложью: «повтор книги» в аудиотеке ничего не значит. Граница голоса при повторе — это граница очереди: наружу не выходим ни автопереходом, ни кнопкой «вперёд», потому что человек ЯВНО попросил остаться. | `RepeatMode="group"`, `relIndex()` в `store.tsx` |
| ЗКН-Н056 | **ЭЛЕМЕНТ, КОТОРЫЙ НИЧЕГО НЕ СООБЩАЕТ, НЕ МОЖЕТ БЫТЬ САМЫМ КРУПНЫМ НА ЭКРАНЕ.** Обложка у всех 1062 записей ОДНА И ТА ЖЕ — она не несёт ни бита информации, а занимала 40% высоты плеера. У Apple большая обложка оправдана тем, что она у каждого альбома СВОЯ: по ней узнают пластинку. У нас узнавать нечего. Смысл несёт **само имя** — это святое имя, а не обложка альбома, и слово здесь главное. Поэтому: **герой — имя**, знак — **печать** (56px, подпись, а не витрина). И слот знака получает РАБОТУ: у играющей записи печать оживает эквалайзером и отвечает «вот что звучит». | `Seal` + типографский герой в `NowPlaying.tsx`; `@keyframes iol-eq` |
| ЗКН-Н075 | **«МОЁ» — ЭТО ДНЕВНИК, А НЕ КАТАЛОГ: НОВЫЕ СВЕРХУ.** Избранное шло в порядке очереди (по голосам) — и запись, отмеченную ТОЛЬКО ЧТО, человек не находил: она тонула в середине, и он решал, что кнопка не сработала. Последнее действие человека обязано быть у него перед глазами, иначе интерфейс не подтверждает то, что человек сделал. Сортировка по голосам верна для КАТАЛОГА; «Моё» — не каталог. | сортировка по `addedAt`, `NowPlaying.tsx` |
| ЗКН-Н057 | **ЧИСЛО СТОИТ ТАМ, ГДЕ ОНО ЗНАЧИТ ДЕЛО.** Рядом с именем голоса стояло ЧИСЛО ГОЛОСОВ: «Шрила Прабхупада · 83» читалось как «у него 83 записи», хотя 83 — это сколько всего голосов в коллекции. Число, поставленное не туда, ХУЖЕ отсутствующего: оно не молчит — оно врёт. И одно и то же число не пишется дважды рядом (вкладка + кнопка): два одинаковых числа сбивают сильнее, чем ни одного. | `DivisionPicker` в `NowPlaying.tsx` |
| ЗКН-Н059 | **АДРЕС ЕСТЬ ИСТИНА ЭКРАНА.** Экран, который открывается тапом, но **не восстанавливается из своего же адреса**, — не экран, а состояние; ссылка на него ложь. Страница стиха жила в локальном `readerRef`, а восстановление цели из адреса стояло под флагом **«один раз за жизнь компонента»**: книга уже открыта → заходишь из избранного на другой стих → эффект видит флаг и **молчит** → остаёшься на ГЛАВЕ. Цель открывается на **каждое изменение адреса** (подпись цели гасит повторные рендеры) | **У5** | `BookDetailPage` · `nav-audit.py::check_once_only_restore` | 🟢 |
| ЗКН-Н060 | **ПУТЬ РОЖДАЕТСЯ В ОДНОМ МЕСТЕ.** Избранное строило адрес книги СВОЕЙ строковой хирургией (`/books/{шифр}/{глава}/{стих}`), а книга живёт в корне (`/{слаг}/{лила}/{глава}/{стих}`, ЗКН-Н023). **Второй построитель пути обязан разойтись с первым — и разошёлся.** Все пути — только через `bookSlug`/`ROUTES`. `/books/<шифр>` — это **эндпоинт сервера**, а не маршрут человека; путать их и есть корень бага. Исправлено в 7 файлах | **У5** | `nav-audit.py::check_second_path_builder` · `deploy-web.yml` | 🟢 |
| ЗКН-Н076 | **ССЫЛКА НА СТИХ — ТА ЖЕ КАНОНИЧЕСКАЯ ФОРМА, ЧТО И НА ГЛАВУ.** Избранное главы кладёт слаг-адрес (`/${bookSlug}/{глава}`) и открывается; избранное стиха строило адрес через `ROUTES.book(work, …)`, а `ROUTES.book` берёт **шифр** книги (`bg`), не слаг → `/bg/2/13`. Весь контур читалки (URL-sync, safety-net подписки `startsWith("/${bookSlug}")`, гейт route) завязан на слаг; work-code-форму он не узнаёт → при открытой книге стих не доставляется, открытие схлопывается на главу. **Ссылка на один и тот же контент из разных мест обязана быть побайтово одной канонической формой; лист (стих) обязан открываться листом, не родителем (главой).** Продолжение ЗКН-Н060: там убрали `/books/`-префикс, здесь — `work`-шифр. Исправлено в `BookDetailPage.tsx` (verseUrl → `bookSlug`) | **У5** | `nav-audit.py::check_n076` (ломан на живом нарушении → красный, slug → зелёный) | 🟢 |
| ЗКН-Н077 | **ИЗБРАННОЕ ТРЕКА ВЕДЁТ К САМОМУ ТРЕКУ, НЕ К БИБЛИОТЕКЕ.** Избранное киртана добавлялось как `addFavorite(k, {t,s})` — БЕЗ `h` (адреса); тогда `hrefFor` уходил в fallback `if (kirtan) return "/kirtans"` → тап открывал библиотеку (что сейчас играет), а не сам трек. При этом ключ избранного `kirtan:<хвост audio-URL>` уже содержит идентификатор трека — но он никуда не вёл. **Каждый сохраняемый объект обязан нести адрес, ведущий к самому объекту (листу), а не к его контейнеру; и этот адрес обязан открываться воспроизведением самого объекта.** Та же болезнь, что Н076 (лист схлопывается на родителя), только в аудио. Исправлено: (1) `hrefFor` выводит `/kirtans?t=<хвост из ключа>` — чинит и старые записи без `h`; (2) `NowPlaying.toggleTrackFav` пишет `h`; (3) `KirtansScreen` по `?t` находит трек в манифесте и `jumpTo` (автоплей), при нужде догружая альбом `all`; (4) ключ трека — единый источник `store.kirtanTrackKey` для избранного, вкладки «Моё» и deep-link | **У5** | `nav-audit.py::check_n077` (ломан на двух живых нарушениях — NowPlaying без `h` и голый `/kirtans` → красный; восстановление → зелёный) | 🟢 |
| ЗКН-Н078 | **«В ИЗБРАННОЕ» ОБЯЗАНО РЕАЛЬНО СОХРАНЯТЬ И ВЕСТИ ВНУТРЕННИМ АДРЕСОМ К САМОМУ ОБЪЕКТУ.** Обобщение Н076/Н077 после сплошного аудита всех точек «в избранное». Два инварианта каждой кнопки-сердца: **(1) она реально сохраняет** — не держит состояние в локальном `useState` (фальшивое сердце: щёлкает, но ничего не пишет); **(2) сохранённый `h` — ВНУТРЕННИЙ путь к самому объекту** (`/…`), не внешний URL и не контейнер. Найдены и починены 4 нарушения: контент (`ContentDetailPage` — `setFavorited` вместо `useFavorite`), книга в плеере (`NowPlaying` — книжная ветка сердца в локальном стейте), книга в читалке (мёртвый `favorited`), даршан (`h` = внешний `channelUrl`/`srcUrl` → ломал SPA-навигацию; заменён на `/darshan?d=<id>` + вьюер открывает сторис, если она ещё в сегодняшней ленте — даршаны эфемерны). Плюс: `hrefFor` покрывает все типы (center/dhama/darshan/content) для legacy-записей без `h`; `catOf` группирует `center`. Рабочие категории проверены: книга, глава, стих, киртан, бхаджан, личность, дхама, тиртха, центр | **У5** | `nav-audit.py::check_n078` (ломан на двух живых нарушениях — `setFavorited` и внешний `channelUrl` в `h` → красный; восстановление → зелёный) | 🟢 |
| ЗКН-Н079 | **ИЗБРАННЫЙ/ГЛУБОКО-ССЫЛОЧНЫЙ СТИХ ОТКРЫВАЕТСЯ СТИХОМ, А НЕ ГЛАВОЙ. АДРЕС (ref) СТИХА У КАЖДОЙ КНИГИ СВОЙ И БЕЗ ИМЕНИ КНИГИ.** Надстройка над Н076/Н078. Корень: `ref` стиха в БД у книг РАЗНЫЙ и часто человекочитаемый — ШБ «9.8.11» (песнь.глава.стих, БЕЗ префикса «sb.»), БГ «БГ 2.13» и ЧБ «Ади 1.94» (с кириллицей). При этом ключ РАЗДЕЛА-главы `division_id` префикс несёт («sb.9.8»). Достроить ref стиха из адреса НЕЛЬЗЯ. Прошлые `openTarget` достраивали `fallbackRef = work.<песнь/лила>.<глава>.<стих>` («sb.9.8.11», «bg.2.13») — он НИКОГДА не совпадал с реальным ref → `/verses/<неверный>` = 404, стих не открывался. На ШБ падение было детерминированным из-за двух вещей: (1) `dv?.chapters.find` в разборе `/toc` — когда `/toc` возвращал книгу без дивизионов, `dv=null`, `.find` на undefined кидал TypeError, управление уходило в `.catch`, минуя `/division/read`+resolveVerseRef, прямо в неверный `fallbackRef`; (2) URL-sync (состояние→URL) строил адрес из `readerRef.split('.')` в расчёте на 4-частный ref с префиксом — для «9.8.11» (3 части) это давало адрес ГЛАВЫ, `pushUrl` перезапускал навигацию, и стих схлопывался. Инвариант: истинный ref берётся ТОЛЬКО из загруженного списка стихов главы через `resolveVerseRef`, из адреса — никогда. Фикс: обе ветки `openTarget` тянут стихи главы напрямую по достроенному id РАЗДЕЛА (`/division/${chapId}/read`, где `chapId = work.<div>.<chapter>` совпадает с `division_id`) и матчат `resolveVerseRef` — независимо от `/toc`; `/toc` идёт фоном только для заголовка, с защитой `dv?.chapters?.find`. URL-sync строит путь из `openChapter.id` (id раздела с префиксом) + последний сегмент ref. Плоская ветка не ждёт загрузки `chapters` (реконструирует главу) — БГ открывается на холодном входе. Гейт запрещает достройку ref с префиксом работы и требует `/division/${chapId}/read`+`resolveVerseRef` как ОСНОВНОЙ путь; поведение проверяется живым self-тестом. | **У5** | `nav-audit.py::check_n079` (статика: обе ветки зовут `resolveVerseRef`, хрупкий матч отсутствует, достройка на месте) + `tools/verse-ref-selftest.mjs` (живой self-тест реального резолвера на всех форматах ref; проверен на двух живых нарушениях — возвращённый хрупкий матч и сломанная достройка → красный; восстановление → зелёный) | 🟢 |
| ЗКН-Н080 | **ТАП ПО СОБЫТИЮ КАЛЕНДАРЯ/ЛЕНТЫ ВЕДЁТ СРАЗУ К ЦЕЛИ, БЕЗ ВСПЛЫВАЮЩЕГО ЛИСТА.** Раньше тап по дню/событию открывал нижний лист `CalendarEventCard` — лишний слой (объект → лист → снова переход к тому же объекту), особенно мешавший на экадаши и на карточках личностей. Инвариант: ОДНА точка перехода `goEvent` в `EventCard.tsx` (личность → `onOpenEntity`/адрес, экадаши → экран практики). И строка календаря (`variant="list"`), и закреплённый пост события в Ленте (`PinnedEvents`, `variant="feed"`) рендерятся ОДНОЙ карточкой `EventCard` и тапают через `goEvent` — общий контракт перехода. Лист-попап события снят и запрещён к возврату: ни импорта, ни JSX, ни объявления `CalendarEventCard` (историческое упоминание в комментарии допустимо, живой компонент — нет). Следствие: у события нет промежуточного экрана — нажатие ведёт прямо в карточку личности или в практику экадаши. Обобщение линии Н077/Н078 (лист ведёт к самому объекту) на календарь и ленту. | **У5** | `nav-audit.py::check_n080` (проверен на живых нарушениях — возвращён импорт `CalendarEventCard` и снят `goEvent` → красный; восстановление → зелёный) | 🟢 |
| ЗКН-Н087 | **ДОМЕН ЖИВЁТ В РЕЕСТРЕ; ПЕРЕЕЗД САЙТА = ОДНА СТРОКА. ПРЕЖНИЙ ДОМЕН НЕ ВЫБРАСЫВАЕТСЯ, А 301-ИТ НА КАНОНИЧЕСКИЙ.** Домен приложения меняется: gaurangers.com → brajs.com → iskcone.com. Пока хост был записан СТРОКОЙ в воркере (канонический 301, адрес отправителя писем, прокси на api-поддомен), в `wrangler.toml` и в ШЕСТИ шагах `deploy-web.yml` (сброс кэша зоны, проба apex, проба свежести бандла, проба локатора, origin генератора PDF), переезд был ОХОТОЙ ЗА ХАРДКОДАМИ: что-то обязательно оставалось на старом хосте и ломалось МОЛЧА — хуже всего пробы CI, которые продолжали зеленеть, подтверждая мёртвый сайт. Инвариант: канонический хост записан ТОЛЬКО в `src/routes.ts` (`SITE_HOST`), всё остальное выводится — `ORIGIN`, `REDIRECT_HOSTS`, redirect_uri провайдеров входа, шаги деплоя (читают хост грепом из реестра в `$GITHUB_ENV`). Почтовый хост (`MAIL_HOST`) и хост второго бэкенда (`API_HOST`) — ОТДЕЛЬНЫЕ константы: почта считается рабочей только после верификации в Resend, а api-поддомен переезжает своим деплоем, и оба события не совпадают с переездом сайта. Прежние домены живут в `ALIAS_HOSTS`: остаются привязанными к воркеру и 301-ят на канонический, потому что ссылки, QR-коды и уже опубликованные метаданные Internet Archive ведут на них НАВСЕГДА (ЗКН-Н023, правило 6). Служебный `workers.dev` из редиректа исключён — иначе сорвалась бы мгновенная проверка свежести деплоя. | **У5** | `infra-audit.py::check_n087` — статья A: в `apps/web/src/**` и `worker.ts` нет голого домена (*.com/.ru/.org/.net) строкой мимо реестра, комментарии не считаются; статья B: маршруты `wrangler.toml` покрывают `SITE_HOST`, его www и каждый `ALIAS_HOST` (иначе прежний домен отвязан и 301 отдавать некому). Проверен на живых нарушениях по обеим статьям: вернул домен строкой в `vows.ts` → красный; снял маршрут www → красный; восстановил → зелёный. Статья Г (авария 18.07.2026: gaurangers.com сняли, `API_HOST` остался на api.gaurangers.com — /api-прокси бил в мёртвую зону, витрины «Личности»/«Бхаджаны» опустели при живой D1; статьи A–B молчали, т.к. сканируют только apps/web): (1) `API_HOST` — поддомен `SITE_HOST` или alias, поддомен снятого домена — авария; канон — производная форма `api.${SITE_HOST}`, переезжающая сама; (2) `apps/api/wrangler.toml` привязывает custom_domain ровно на `API_HOST`; (3) smoke-проверка `deploy-api.yml` бьёт ровно в `https://API_HOST`. Проверена на живом нарушении: вернул api.gaurangers.com → три красных (реестр, wrangler, smoke); восстановил → зелёный. | 🟢 |
| ЗКН-Н086 | **ЛАНДШАФТ ТЕЛЕФОНА — СПРОЕКТИРОВАННЫЙ РЕЖИМ: ХРОМА И ШИРИНА ЛИСТА ЖИВУТ В ТОКЕНАХ, КОНТЕНТ УВАЖАЕТ БОКОВЫЕ ВЫРЕЗЫ, ТЕКСТ ДЕРЖИТ МЕРУ.** Н085 сделал так, что поворот перестал ЛОМАТЬ вёрстку; этот закон делает поворот ПОЛНОЦЕННЫМ режимом чтения и просмотра. Корень прежнего бессилия: высоты шапки/меню и ширина листа были зашиты числами по двум десяткам файлов (`height: 56`, `maxWidth: 480`, `bottom: 16`, `116px`), поэтому ландшафт нельзя было спроектировать — только перевёрстывать руками. В ландшафте телефона ВЫСОТА дефицитна (~390px), ШИРИНА избыточна (~844px) — значит хрома обязана ужиматься, а лист расширяться. Инвариант: (1) вся геометрия оболочки живёт в токенах `--h-top-header` · `--gtab-h` · `--gtab-bottom` · `--content-bottom` · `--sheet-max` · `--read-max` · `--safe-l`/`--safe-r`; (2) ландшафтный `@media (orientation: landscape) and (max-height: 600px)` переопределяет ТОКЕНЫ (шапка 56→44, пилюля ближе к низу 16→10, лист 480→680) — вся оболочка перестраивается разом; (3) БОКОВЫЕ ВЫРЕЗЫ: `env(safe-area-inset-left/right)` не использовался НИГДЕ, и в ландшафте вырез/Dynamic Island съедал край — теперь поле держит отступ, лист центрируется в безопасной области; (4) МЕРА ЧТЕНИЯ отстаёт от листа (`--read-max` 600 против 680): строка длиннее ~75 знаков нечитаема, поэтому 9 полноэкранных читалок (книга · бхаджан · статья · заметка) и 15 контентных колонок берут ширину из токена, а не из числа 480; модальные листы и плеер сознательно НЕ растягиваются — Apple их тоже капает. (5) Высота пилюли НЕ ужимается: её центр считает JS (`BAR_H`) для физики линзы Liquid Glass (ЗКН-Д016) — 12px высоты не стоят риска увести сигнатурную каплю. Портрет не меняется ни на пиксель: 62+16+26 = прежние 104. | **У5** | `nav-audit.py::check_n086` (токены на месте · ландшафтный @media переопределяет их · .app-viewport держит боковые вырезы · .app-shell/.gtab-wrap на токенах · TopHeader и нижний воздух на токенах · ни одной читалки с зашитым `maxWidth: 480, zIndex: 70/80` · СВЕРКА `--gtab-h` в CSS с `BAR_H` в App.tsx. Проверен на трёх живых нарушениях: рассинхрон BAR_H 62→50 → красный · снятие боковых вырезов → красный · возврат читалке 480 → красный; восстановление → зелёный) | 🟢 |
| ЗКН-Н085 | **СПРОЕКТИРОВАННЫЙ ЛАНДШАФТ ТЕЛЕФОНА: ОБОЛОЧКА НЕ ЛОМАЕТСЯ, А ПРЕДЪЯВЛЯЕТСЯ ЦЕНТРИРОВАННЫМ ЛИСТОМ НА СПОКОЙНОМ ПОЛЕ.** Прямая жалоба основателя: на перевёрнутом (ландшафтном) экране вёрстка разваливалась — нижнее меню (`.gtab-wrap` absolute bottom:16) всплывало в СЕРЕДИНУ, контент резался; приложение не спроектировано под поворот. Корень тот же, что у Н084: оболочка не была прибита ровно к вьюпорту, и в широком-низком ландшафте `bottom:16` считался от завышенной высоты. Прибив оболочку к `100dvh` (Н084), поворот перестаёт ЛОМАТЬ вёрстку — меню внизу, контент цел. Дальше — намерение: портретный лист (ширина до 480 тонко подогнана) на всю ширину распался бы, поэтому его НЕ тянут, а предъявляют спокойным центрированным листом на приглушённом нейтральном поле, с волосяными кромками и мягкой тенью — как «карточку» приложения; шапка и меню приколоты, контент скроллит внутри. Вёрстка оболочки вынесена из inline-стилей в CSS-классы `.app-viewport`/`.app-shell` (иначе поворотом не порулить). Порог `max-height: 600px` отделяет телефон в ландшафте (высота ~375…430) от планшета (≥768) — планшету рамка-лист не нужна. | **У5** | `nav-audit.py::check_n085` (globals.css: классы .app-viewport/.app-shell на 100dvh · App на этих классах · @media (orientation: landscape) оформляет .app-shell; проверен на живом нарушении — удалён ландшафтный @media → красный, восстановление → зелёный) | 🟢 |
| ЗКН-Н084 | **ОБОЛОЧКА ПРИКОЛОТА К ДИНАМИЧЕСКОМУ ВЬЮПОРТУ; BODY/#root НЕ СКРОЛЛЯТ — СКРОЛЛИТ ТОЛЬКО ВНУТРЕННИЙ `<main>`. ВЕРХНЯЯ ШАПКА НЕ МОЖЕТ УЕХАТЬ ЗА ЭКРАН.** Прямая жалоба основателя: «переходя из таба в таб или ещё какое-то действие, пропадает верхняя шапка и не появляется». Верхняя шапка (`TopHeader`) лежит в потоке НАД скролл-контейнером `<main>`. Пока `body` мог прокручиваться (`min-height: 100vh` без блокировки, `#root` вовсе без правил), на iOS Safari показ панели URL давал разницу «`100vh` — большой вьюпорт ↔ видимая высота меньше»; body уезжал на эту разницу, и шапка прокручивалась вверх за край экрана. Смена таба сбрасывала лишь ВНУТРЕННИЙ скролл (`mainRef.scrollTop = 0`) — а `body` оставался прокручен, и шапка «не возвращалась». Ровно тот симптом. Инвариант: `body` и `#root` прибиты к `height: 100dvh` + `overflow: hidden` (единственный скроллер приложения — внутренний `<main>` с `overflow-y:auto`), внешняя оболочка App — `height: 100dvh`, НЕ `min-height: 100vh`. Тогда шапке и нижнему меню (оба вне скролла) уехать некуда. Компромисс осознан: фиксированная оболочка теряет авто-скрытие панели URL Safari, но получает стабильные шапку и меню — ровно то, чего требует жалоба. Блокировка снимается под `body.printing` и в `@media print` (`height:auto; overflow:visible`), иначе многостраничный PDF схлопнулся бы в один экран. | **У5** | `nav-audit.py::check_n084` (globals.css: body — `overflow:hidden` + `100dvh`, НЕ `min-height:100vh`; `#root` — `100dvh`+`overflow:hidden`; снятие под `body.printing`; App.tsx — оболочка не `minHeight:"100vh"`; комментарии вырезаются, гейт стережёт код. Проверен на живом нарушении: возврат `min-height:100vh` и удаление `#root` → красный; восстановление → зелёный) | 🟢 |
| ЗКН-Н083 | **ЗАКЛАДКА СТИХА/ГЛАВЫ ПОКАЗЫВАЕТ КАНОНИЧЕСКУЮ ССЫЛКУ — ПИСАНИЕ · ПЕСНЬ/ЛИЛА · ГЛАВА · СТИХ, А НЕ ГОЛОЕ «ТЕКСТ 17».** В «Избранном» сохранённый стих показывал лишь «Текст 17» с подписью «Шримад-Бхагаватам» — без песни и главы: по такой закладке невозможно понять, ЧТО это (текст 17 есть в тысячах глав). Это была прямая жалоба основателя. Корень: снимок избранного писал `t = data.label` («Текст 17») и `s = bookTitle`, а локация (`division_id` = «sb.1.17» = песнь.глава) терялась. Инвариант: у стиха/главы писания подпись строит ЕДИНЫЙ модуль `bookRef.scriptureRef` из сохранённого slug-пути `h` (иначе division): писание — жирная строка («Шримад-Бхагаватам»); путь «Песнь 1 · Глава 17 · Текст 17» приглушён, сам стих подсвечен весом 600; плитка несёт монограмму книги (ШБ · БГ · ЧЧ) вместо безликого значка-кавычки. Диапазон склоняется («Тексты 13-14»); ЧЧ даёт лилу («Мадхья-лила»); БГ — плоскую «Глава 2 · Текст 13» без эпитета «как она есть». ТОТ ЖЕ строитель ставит снимок избранного стиха в читалке (`scriptureRef("verse", …)`) — формат «как сохранили» и «как показали» не расходится (в духе Н060: ссылка рождается в одном месте). Поиск по избранному тоже идёт по полному пути закладки. | **У5** | `nav-audit.py::check_n083` (статика: FavoritesScreen импортирует и зовёт `scriptureRef`, рисует путь+якорь `sref.lead`/`sref.anchor` и монограмму `sref.abbr`; читалка ставит снимок через `scriptureRef("verse")`; модуль на месте) + `tools/book-ref-selftest.mjs` (живой self-тест на ШБ/ЧЧ/БГ: стих + глава + диапазон + неизвестная книга; проверен на живом нарушении — сломан словарь лил ЧЧ и убран вызов scriptureRef → красный, восстановление → зелёный) | 🟢 |
| ЗКН-Н082 | **ПОВТОРНЫЙ ТАП АКТИВНОГО TIER-1 ТАБА (И ЛОГОТИП ISKCON ONE LOVE) ВОЗВРАЩАЮТ ЗАЛ НА ЕГО ПЕРВЫЙ ПОДТАБ.** Стоя на Календаре (подтаб зала Садханы), тап по нижнему табу «Даршан» (тот же Tier-1) или по логотипу (он ведёт на `HOME_TAB`, а ЗКН-Н074 сделал им «sadhana») НИЧЕГО не делал: тап зовёт `onChange`=`setTab` к ТОЙ ЖЕ вкладке — пустышка (React-стейт не меняется, адрес не трогается), а событие `tab-reset`, которое шлёт `onClickTab`/логотип, слушал ТОЛЬКО зал ИСККОН — да и тот сверял МЁРТВОЕ `detail === "home"` (диспетчер шлёт id таба, не «home»). Инвариант: каждый Hall с подтабами слушает `tab-reset` по СВОЕМУ id и на своё событие уходит на ДОМАШНИЙ адрес таба (`SAD_PATH[SAD_SUBS[0]]` → лента Даршана · `BOG_PATH[BOG_SUBS[0]]` → первая витрина · `/iskcon` → корень) — адрес меняется, подтаб пересчитывается из него (ЗКН-Н031). Кросс-таб переход (на ДРУГУЮ вкладку) отрабатывает монтированием зала, тут менять нечего. Событие несёт id таба (`sadhana`/`bogatstva`/`iskcon`), НЕ «home». | **У5** | `nav-audit.py::check_n082` (проверен на живом нарушении — снят слушатель Садханы / возвращён мёртвый `detail === "home"` → красный; восстановление → зелёный) | 🟢 |
| ЗКН-Н081 | **В НАВИГАТОРЕ КАЛЕНДАРЯ НЕТ КНОПКИ-ДВУСМЫСЛЕННОСТИ «ВПЕРЁД»; ВОЗВРАТ К НАСТОЯЩЕМУ — «СЕГОДНЯ», ВИДИМ ТОЛЬКО ВНЕ ТЕКУЩЕГО МЕСЯЦА.** Кнопка «Вперёд» стояла рядом со стрелкой `›` (обе читаются как «дальше»), вела на `/calendar` (живой вид «Ближайшее»), но появлялась ПРИ ЛЮБОМ открытом месяце — в т.ч. на ТЕКУЩЕМ, где возвращать некуда: назначение кнопки было непонятным. Инвариант (паттерн Apple «Today»): возврат к настоящему — кнопка **«Сегодня»** (ярлык говорит сам за себя), видима СТРОГО когда открыт не текущий месяц (`route.ym && route.ym !== todayYm`, `todayYm = ymOfToday()`); в текущем месяце и в live-режиме СКРЫТА — ты уже в «сейчас», а живой вид достигается самим табом «Календарь». Шаг ‹ › — от опорного месяца `baseYm = route.ym || todayYm`, поэтому и из live-вида стрелки листают соседние месяцы предсказуемо (`ymAdd(baseYm, ±1)`). Ярлык «Вперёд» в навигаторе запрещён. | **У5** | `nav-audit.py::check_n081` (проверен на живом нарушении — возвращён ярлык «Вперёд» и безусловный показ → красный; «Сегодня» + условие → зелёный) | 🟢 |
| ЗКН-Н072 | **НИЖНЕЕ МЕНЮ = 5 ВКЛАДОК, И ПОРЯДОК ИХ — ЗАКОН.** Порядок кнопок нижнего меню утверждён основателем 15.07.2026 (**ИСККОН · Богатства · Кришна · Гауранга · Садхана**) — решение основателя, не разработчика; правится ТОЛЬКО им. Но список живёт ДВАЖДЫ: `TABS` (что рисуется) и `TAB_IDS` (канонический источник вкладок, ЗКН-Н040). Два списка = два порядка; разъедутся молча — ровно болезнь Н007/Н045. Гейт сверяет ОБА против утверждённого порядка. Приземление («/» → Садхана) от порядка кнопок независимо и здесь не меняется | **У5** | `nav-audit.py::check_n072` | 🟢 |
| ЗКН-Н073 | **БЛОК ДЛИННОЙ СТРАНИЦЫ ЖИВЁТ В АДРЕСЕ.** Лендинг ИСККОН — длинный скролл с меню по блокам. Меню и подсветка активного блока были, а **адрес не менялся**: стоя на блоке, сослаться на него было нельзя — только на верх страницы. Теперь скролл и клик тихо пишут активный блок в адрес (`/iskcon/dhama`) через `replaceAnchor` — **replaceState, не push** (история не засоряется десятком секций; «назад» уводит СО страницы, а не листает блоки) и **без notifyNav** (вкладка и субтаб не меняются — незачем будить роутер на каждый кадр скролла). Кадр скролла НЕ перехватывает чужой адрес (ушёл кликом на витрину — адрес уже не наш) и НЕ считает «низ», пока страница не набрала высоту (иначе прыжок на /iskcon/next при заходе). Заход по ссылке доводит до блока (секции резервируют высоту `aspectRatio`). Гейт сверяет: у каждого якоря есть секция `hsec-*`, скролл пишет адрес, заход читает | **У5** | `nav-audit.py::check_n073` | 🟢 |
| ЗКН-Н074 | **ГЛАВНЫЙ ТАБ = ПЕРВЫЙ В МЕНЮ, И ОН ВЫВОДИТСЯ, А НЕ ВПИСАН.** Стартовая вкладка приложения и то, что открывает корень `/` (домен), выводятся из `TAB_IDS[0]` через `HOME_TAB` — не прибиваются к конкретному табу руками. Поменялся порядок кнопок (ЗКН-Н072) → сменился и главный таб, одним движением. Раньше дефолт был прибит к `sadhana` ОТДЕЛЬНО от порядка — классическая болезнь «второго списка» (как Н007/Н045): порядок сменили, а корень по-старому тянул `/darshan`. Затрагивает три точки — начальное состояние `useState(HOME_TAB)`, `BASE_OF[""]`, обработчик `«/»` — и кнопку-логотип. Специфические маршруты Садханы (`/sadhana /id /calendar /darshan`) остаются за собой. Гейт сверяет: `HOME_TAB` выведен из `TAB_IDS[0]`; три точки им пользуются; строкой дефолт не вписан | **У5** | `nav-audit.py::check_n074` | 🟢 |
### Домен С — Санскрит / терминология · 🟠 УТВЕРЖДЁН С ОГОВОРКОЙ 10.07.2026 (С001 в доработке)
| ID | Закон | Ур. | Проверка | Статус |
|----|-------|-----|----------|--------|
| ЗКН-С001 | **Georgia + курсив — ТОЛЬКО чужой/священный голос:** стих (деванагари, транслитерация) · цитата (ЗКН-БТ004) · пословный перевод · IAST-форма имени. **Термины в прозе** («манджари», «сакхи», «бхакти») — ОБЫЧНЫЙ шрифт. Georgia без курсива у терминов была полумерой: шрифт другой, смысла нет, текст рябит. Правило: Georgia = «говорит не автор карточки» | **У5** | `laws-lint`: `font-scripture` у терминов = fail | 🟢 |
| ЗКН-С002 | Цитаты и блочные транслитерации (стихи, пословный перевод, вербатим) — Georgia+курсив (`var(--font-scripture)`), как и термины; чужой голос выделен шрифтом | **У5** | `laws-lint.py` (проверен на живом нарушении) | 🟢 |
| ЗКН-С004 | **`\b` В РЕГУЛЯРКЕ РАБОТАЕТ ТОЛЬКО ДЛЯ ЛАТИНИЦЫ — НА КИРИЛЛИЦЕ ОНА МОЛЧИТ.** `\b` определена через `\w` = `[A-Za-z0-9_]`: между «Харе» и пробелом движок границы **не видит**. Регулярка святого имени с `\b` не распознала **ни одной** маха-мантры (0/3 на живых примерах) — и молчала бы вечно: она ничего не ломает, она просто **не срабатывает**. Это родня ЗКН-Ф012 (`set()` диакритики): кириллица требует `\p{L}` и флаг `u`, иначе проверка есть на бумаге и отсутствует на деле | **У5** | `laws-lint.py::check_cyrillic_b` — `\b` рядом с кириллицей в регулярке = fail (проверен на живом нарушении) | 🟢 |

### Домен Б — Книги · ✅ УТВЕРЖДЁН 11.07.2026
| ID | Закон | Ур. | Проверка | Статус |
|----|-------|-----|----------|--------|
| ЗКН-Б001 | **ИМЯ КНИГИ ЦЕЛОЕ. ПОЛОВИНА — НЕ ИМЯ.** У книги имя составное: «Бхагавад-гита» + «как она есть». Показать только `titleLine1` — значит показать **половину**: в одном месте книга зовётся полно, в другом обрублена. **Читатель решит, что это разные книги.** Законно: обе строки рядом ИЛИ `bookFullTitle()` | У2→У5 | `grep` использования `titleLine1` без `bookFullTitle` | 🟡 |
| ЗКН-Б002 | Ровно 3 факт-чипа в фикс. порядке (глав/стихов/лет; проза → глав/лет/автор). Книжная ветка классификатора чипов ЗКН-К010 — тот же принцип: чип несёт ФАКТ, не оценку | У1→У3 | сверка `BOOKS.chips` | 🟢 |
| ЗКН-Б007 | **МОЛЧАЛИВАЯ ПОДМЕНА КНИГИ — ХУЖЕ ОШИБКИ.** `App.tsx`: `const bk = BOOKS[work] ? work : "bg"` — книги нет в бандле → читалка **молча открывает «Бхагавад-гиту»**. Человек жмёт «Чайтанья-бхагавата» и попадает в другую книгу, не понимая почему. Книга, читаемая в D1, ОБЯЗАНА быть в `books.ts` | **У5** | `product-audit.py::check_b007` | 🟢 |
| ЗКН-Б004 | **КАНОН ИМЁН ДЕЙСТВУЕТ И В КАТАЛОГЕ КНИГ.** Гейты стерегли код и досье, а каталог — НЕТ. Там жили: автор «Шри Шикшаштаки» = «Шри Чайтанья Махапрабху» (голая форма), полусокращённый титул Прабхупады, точка с запятой в описании | **У5** | `data-audit.py` | 🟢 |
| ЗКН-Б005 | **КНИГА СВЯЗАНА С ЛИЧНОСТЬЮ АВТОРА.** От «Бхакти-расамрита-синдху» нельзя было пройти к Рупе Госвами — связи не было у **18** книг. Двух авторов пришлось **создать**: их не было в реестре вовсе — Парашара Муни (отец Вьясадевы, поведал «Вишну-пурану») и Махараджа Кулашекхара (один из двенадцати Альваров). Исключения: Веды/Упанишады (нет единого автора), современные биографы | **У5** | `data-audit.py`, храповик 2 | 🟢 |
| ЗКН-Б006 | **НЕТ КНИГ-ПРИЗРАКОВ.** `author_entity_id` обязан указывать на существующую личность, иначе ссылка ведёт в никуда | **У5** | `data-audit.py` | 🟢 |
| ЗКН-Б010 | **ПОДПИСЬ ОДНА НА ВСЕ ПОВЕРХНОСТИ.** Читалка подписывает стих «Текст 5» / «Тексты 21-22» (мн. ч. у слитых), а плеер собирал строку сам и писал «Текст 21–22» — та же книга, разные подписи. И названия главы в плеере не было ВОВСЕ: у ШБ дорожка — это СТИХ, и без «Песнь · Глава · Название» человек не понимает, где он в книге. Подпись считается из `ref` ОДНОЙ функцией (`sbVerseLabel`), подзаголовок — одним `trackSubtitle()` на мини-плеер, Now Playing, экран блокировки и «продолжить слушать» | **У5** | `worker.ts::sbVerseLabel` · `store.tsx::trackSubtitle` | 🟢 |
| ЗКН-Б011 | **АУДИО И ТЕКСТ — ОДНА КНИГА. ПЕРЕХОД В ОБЕ СТОРОНЫ, В ТО ЖЕ МЕСТО.** Связь ломается тихо и ОДНОБОКО: «Слушать» из текста делают сразу, обратный путь забывают. Так и было — плеер у иерархических книг (ШБ/ЧЧ) **ОТБРАСЫВАЛ ГЛАВУ**: человек слушал ШБ 1.1.5, жал «Читать» и падал на ОБЛОЖКУ. Четыре звена: (1) «Слушать» с главы/стиха → звук ЭТОГО места; (2) «Читать» из плеера → текст ТОГО ЖЕ места (у ШБ дорожка = СТИХ, значит и вести на стих); (3) дорожка несёт `ref` — ключ стиха, без него «то же место» недостижимо; (4) открытый текст СЛЕДУЕТ за звуком до стиха — подкручивается и подсвечивает | **У5** | `product-audit.py::check_b009` (проверен на живом нарушении) | 🟢 |
| ЗКН-Ц010 | **НОМЕР ЗАКОНА УНИКАЛЕН.** Два закона под одним номером — не свод, а каша: ссылка «см. ЗКН-Б008» ведёт в ДВА разных места, и гейт нельзя привязать к закону. Поймано на живом: я сам налетел на занятый номер, а гейт нашёл ещё **три** давних дубля (Сд005, Ц004, Ц006 — их гейты переплетены, разводить наспех опасно). Храповик: новый дубль — падение, эти три — зафиксированный долг | **У5** | `laws-lint.py::check_unique_ids` | 🟢 |
| ЗКН-Б003 | В ВБК книги слот «титул/категория» заполняется **категорией книги** (место в каноне: напр. «Основная канва Гауранга Лилы», «Комментированный перевод БТТ») — уточняется при реализации книг | У1 | `STANDARD_book_titles.md` | 🟠 |

### Домен Р — Реестр / данные · ✅ УТВЕРЖДЁН 11.07.2026
> Источник истины уровней: **`docs/entity-registry/GOLD_STANDARD_PROFILE.md`** · кузница: `tools/goldforge/`

| ID | Закон | Ур. | Проверка | Статус |
|----|-------|-----|----------|--------|
| ЗКН-Р001 | Принцип «id ≠ имя»: имена/связи — атрибуты `id`, правка в одном месте видна везде | У3 | схема `entities`/`entity_names` | 🟢 |
| ЗКН-Р002 | **МАССОВОЕ УДАЛЕНИЕ ЗАПРЕЩЕНО. ЗАЛИВКА НЕДЕСТРУКТИВНА.** `registry-load` уже убил базу: DROP+CREATE и молчаливое падение ПОСЛЕ дропа. Тот же почерк остался ещё в **ПЯТИ** воркфлоу: `DELETE FROM prayers` (все **339 бхаджанов**), `DELETE FROM content_items` (170 записей), `content_blocks`, `prayer_verses`, `tilda_pages`. «Снести и залить заново» кажется чистым — но если разбор дал МЕНЬШЕ, разница **пропала навсегда**. А у молитв теперь **вычищенные слаги** (были чужие пути `/ru/gaura-arati`) — прогон вернул бы мусор. `DELETE FROM <таблица>` без `WHERE` — запрещён; точечное по ключу — законно | **У5** | `infra-audit.py` (проверен на живом нарушении) | 🟢 |
| ЗКН-Р003 | **ЗОЛОТО = авторитетная мини-книга.** Критерий — не объём, а **исчерпание источников**: в доступных источниках больше нечего взять. Полная биография (явление → детство → встреча → служение → труды → уход → наследие), учение, труды, наследие; каждое утверждение со ссылкой до стиха. Всё, что до этого — silver и ниже | У1→У5 | гейт `goldforge audit` | 🔴 |
| ЗКН-Р004 | **Уровень ВЫЧИСЛЯЕТСЯ гейтом, а не ставится вручную.** Метка `gold` присваивается только при покрытии досье = 100%. Ручная метка запрещена. Ложные метки подлежат понижению (следствие ЗКН-БТ001: ноль фабрикации касается и метаданных)  **ЗОЛОТО ТРЕБУЕТ УЛИКИ:** метка в D1 без паспорта имён и досье = самозванство. Так 649 профилей объявляли себя золотом, имея <2000 знаков — все понижены | **У5** | `pkl-audit.py` — мост D1 ↔ улики в репозитории | 🟢 |
| ЗКН-Р007 | **ОДНО МЕСТО — ОДНА ЗАПИСЬ.** Каталог дхамы грузился из разных источников без сверки транслитерации: «Вишрама-гхат» и «Вишрам-гхат», «Кусум-саровара» и «Кусума-саровара», «Чир-гхат» — **трижды**. Человек видит одно место несколько раз, счёт врёт. **Оговорка:** одинаковое имя в РАЗНЫХ кластерах — не дубль (во Врадже реально три Говинда-гхата и три Дауджи-мандира) | **У5** | `data-audit.py`. **ДОЛГ ЗАКРЫТ:** все 12 дублей слиты — курируемая запись выжила, скрейп отдал ей свой текст. Мест 672 → **660** | 🟢 |
| ЗКН-Р010 | **ОДНА СТРАНА — ОДНО НАПИСАНИЕ.** В каталоге центров США жили под **ТРЕМЯ** написаниями: «United States **Of** America» (56) · «United States **of** America» (10) · «United States» (2). Фильтр строил **три вкладки «США»** — человек не мог найти свой центр. Ср. Р009 (одно понятие — одна категория) | **У5** | `data-audit.py` | 🟢 |
| ЗКН-Р009 | **ОДНО ПОНЯТИЕ — ОДНА КАТЕГОРИЯ.** В базе живут ДВЕ на одно: `lila-gauranga` (448 — по ней **фильтрует приложение**) и `gauranga-lila` (394 — метка чипа). **Мой собственный гейт на этом соврал:** проверял `gauranga-lila`, а у Шрилы Прабхупады её нет — только `lila-gauranga`. Прежде чем проверять категорию — узнай, ПО КАКОЙ приложение реально показывает | У2 | комментарий в `data-audit.py` | 🟡 |
| ЗКН-Р008 | **МУСОРНЫЙ id ПОПАДАЕТ В АДРЕС.** Из CMS-импорта утекли `trashed` (артефакт корзины!), `gokula-7264`, `akrura-bhavan-6002`. Человек видел `/dhama/vrindavan/trashed`. Слаг отражает СМЫСЛ | **У5** | `data-audit.py` | 🟢 |
| ЗКН-Р005 | **Паспорт имён обязателен.** Жатва идёт по ВСЕМ формам: RU + склонения + EN + IAST + алиасы + эпитеты (+ уточнители и омонимы). Поиск по одной языковой форме — нарушение (терял до 99% материала). **Паспорт строится САМ:** `goldforge passport` берёт имена из `entity_names`, разворачивает русские склонения и подставляет титулы-уточнители | **У5** | `goldforge passport` · `pkl-audit.py` (золото без паспорта = самозванство) | 🟢 |
| ЗКН-Р006 | **Досье прежде сборки.** Мини-книга собирается только из досье — файла-улики, где каждый пассаж имеет провенанс (источник + смещение + форма). Ничего мимо досье | У2 | `docs/dossiers/` | 🔴 |

### Домен П — ПКЛ (карточки личностей) · ✅ УТВЕРЖДЁН 11.07.2026
> Конвейер: **`tools/goldforge/`** · стандарт золота: `GOLD_STANDARD_PROFILE.md`

| ID | Закон | Ур. | Проверка | Статус |
|----|-------|-----|----------|--------|
| ЗКН-П001 | **СЕМЬ КАНАЛОВ ЖАТВЫ** (было объявлено шесть, реализованы были три): **k1** книги приложения (D1: ВСЕ 28 книг, **68 816 стихов** — перевод И комментарий + quotes) · **k2** архив библиотеки (`docs/sources`, 69 МБ, RU+EN) · **k3** книги онлайн (vanisource · vedabase · granthamandira) · **k4** бхаджаны приложения (339 молитв, 1 989 куплетов) · **k5** бхаджаны онлайн (kksongs …) · **k6** Википедия (ru+en) · **k7** сайты ИСККОН и связанные (vaniquotes · vanipedia · iskcon.org · Desire Tree · gaudiya-history · harekrishna.ru). Реестр внешних источников — `tools/goldforge/sources.json`: добавил строку → канал сам её опрашивает **Гейт смотрит в реестр `CHANNELS`, а не на слово в комментарии:** первая версия проверяла `grep k7` и оставалась зелёной при удалённом канале (проверено на живом нарушении) | **У5** | `pkl-audit.py` · `goldforge harvest` печатает, какой канал не собран | 🟢 |
| ЗКН-П002 | Цитаты — генерируются напрямую из БД (`json_group_array/json_object`), не ручным набором | У1→У5 | ревью источника цитат | 🟡 |
| ЗКН-П003 | AUDIT-TO-ZERO: `json_valid=1` · `bad_links=0` · `total_refs=distinct` · `char(39)=0` · `total_h=distinct` · канон-гейт (0 неканоничных форм в `by/see/h/kicker/p`)  **Оговорка:** прямой апостроф ЗАКОНЕН в транслитерации стиха («према крама бади\' хайа» — знак элизии). Править его = испортить стих (ЗКН-БТ004). Гейт ловит апостроф только в ПРОЗЕ | **У5** | `data-audit.py` | 🟢 |
| ЗКН-П004 | Вербатим-цитата — только если источник в приложении (есть `to:`-ссылка); иначе факты в прозу  **«Источник в приложении» =** книга `readable=1` · бхаджан из `prayers` · личность-рассказчик. Долг **21** — главный: «Гаура-ганоддеша-дипика» (27 дословных цитат, текста книги нет) | **У5** | `data-audit.py`, храповик 21 | 🟡 |
| ЗКН-П005 | Число табов/суб-табов НЕ фиксировано — вытекает из объёма материала | У1 | `pkl-hero-playbook.md` | 🟢 |
| ЗКН-П006 | NARRATOR источника = дефолтный `by` повествования (ЧЧ→krishnadasa-kaviraja, ШБ→shukadeva, БР/Нароттама-виласа→Нарахари Чакраварти, Чайтанья-бхагавата→Вриндавана дас, Чайтанья-мангала→Лочана дас, Кави Карнапура); прямая речь→говорящий; чужой труд→его автор | У1→У3 | ревью атрибуции | 🟡 |
| ЗКН-П007 | Канонический ростер манджари — проектный КАНОН, приоритетнее ГГД/Роузена (Рагхунатха Бхатта=Раса не Рага; Локанатха=Манджулали не Лила; Гопала Бхатта=Гуна не Ананга и т.д.)  **Ростер замкнут гейтом:** 10 тождеств в графе. Недоставало **Нароттамы даса Тхакура** — создана Чампака-манджари (не путать с Чампакалатой — та из ашта-сакхи) | **У5** | `data-audit.py` | 🟢 |
| ЗКН-П008 | Исчерпывающий проход по ВСЕМ каналам; собрать всё, писать пачками, аудит и отчёт ОДИН раз в конце (не пилюлями) | **У5** | `pkl-audit.py` · `docs/pkl-hero-playbook.md` | 🟡 |
| ЗКН-П009 | **Уверенность, а не отсев. НИЧЕГО НЕ ВЫБРАСЫВАЕТСЯ.** Каждый пассаж маркируется: **уверенно** (точное имя с титулом — «Джива Госвами») · **кандидат** (голое имя — «Джива»: решает куратор, уходит в бриф) · **омоним** (нарицательное: «джива» = душа — в сборку не идёт). **Точное имя проверяется ПЕРВЫМ, и это спасает жатву:** стем «джива» стоит в тысячах комментариев Шримад-Бхагаватам как «душа» — узкий фильтр убил бы материал, широкий утопил бы карточку в чужом тексте | **У5** | `goldforge harvest` (тиры) + `<id>.brief.md` — бриф куратора | 🟢 |
| ЗКН-П010 | **КОНВЕЙЕР, А НЕ РУКИ.** Карточка личности собирается командой `goldforge forge <id>`: паспорт имён → жатва по 7 каналам → сборка → гейт → запись в D1. Ручная сборка «на глазок» — источник всех прежних бед: выборочные источники, однобокая карточка, десять отчётов вместо одного. Стадии обязаны существовать ВСЕ — пропала любая, и «сборка» снова становится непроверяемым ручным трудом | **У5** | `pkl-audit.py::check_forge_pipeline` | 🟢 |
| ЗКН-П011 | **ГЕЙТ НУЛЕВОЙ ФАБРИКАЦИИ.** ЗКН-БТ001 годами держался на добросовестности: документ говорил «не выдумывай». Теперь это ПРОВЕРЯЕТСЯ. Каждое имя собственное (заглавная в середине фразы) и КАЖДОЕ число из авторской прозы обязано лежать в досье — то есть в реально собранном материале. Не лежит → сборка падает. **Проверено на живом нарушении:** «основал в 1789 году школу в Ливерпуле, учитель Джон Смит» → гейт назвал выдумкой «Ливерпуле», «Джон», «1789»; а «Рамакели», «1513», «Мадхусудана» пропустил — они в корпусе ЕСТЬ. Гейт точный, а не параноидальный | **У5** | `goldforge/gate.py::containment` | 🟢 |
| ЗКН-П012 | **ДОСЛОВНОСТЬ ПРОВЕРЯЕТСЯ, А НЕ ОБЕЩАЕТСЯ.** Цитата рождается в БД, а не в голове: текст стиха/комментария/куплета тянется из D1 и кладётся в карточку без правки. Гейт сверяет КАЖДУЮ цитату с источником подстрочно — не совпало, значит это уже не шастра. Снимает невидимое расхождение (прекомпозиция «ё», nbsp), из-за которого `instr(longform, verse_text)` молча давал 0 | **У5** | `goldforge/gate.py::verbatim` | 🟢 |
| ЗКН-П013 | **ПОДПИСЬ ЗАРАБАТЫВАЕТСЯ, А НЕ НАЗНАЧАЕТСЯ.** «— Вриндаван дас Тхакур» под цитатой — это УТВЕРЖДЕНИЕ о том, кто это сказал, и оно требует улики. Конвейер ставил подпись по правилу («повествователь книги») на КАЖДУЮ строку — и подписал Вриндаваном дасом Тхакуром (ум. 1589) текст о Шриле Прабхупаде. **Предисловие, введение, «От издателей», приложение — НЕ шастра и не речь автора** (198 таких строк лежат в таблице стихов как стихи: издатель кланяется Шриле Прабхупаде, а конвейер подписал этот поклон самим Шрилой Прабхупадой). Они не цитируются вовсе | **У5** | `data-audit.py::ЗКН-П013` — SQL по D1: подпись на предисловии/приложении (проверен на живом нарушении) | 🟢 |
| ЗКН-П014 | **ГЕЙТ АНАХРОНИЗМА · ЛОЖНОЕ СВИДЕТЕЛЬСТВО.** Прежний гейт проверял имена и числа и ни разу не спросил главного: **МОГ ЛИ ПОДПИСАННЫЙ ЭТО СКАЗАТЬ.** Теперь спрашивает: если подписанный умер раньше того, о чём в тексте речь, цитата отвергается ЦЕЛИКОМ — ссылка на источник остаётся правдой, ложное свидетельство не остаётся. Годы жизни — `people.json`. Для библиотеки, обещающей BBT-точность, ложная подпись хуже пустоты | **У5** | `data-audit.py::ЗКН-П014` — SQL по D1: древний автор о XX веке (проверен на живом нарушении) | 🟢 |
| ЗКН-П015 | **СТРОКА, КОТОРАЯ НЕ СТИХ, — НЕ ЦИТАТА.** `cb.adi.17.164` держит в поле «перевод» **26 312 знаков**: при импорте туда склеили всё приложение книги — жизнеописание автора и современную биографию. Конвейер вырезал оттуда кусок и выдал за перевод стиха XVI века. Перевод длиной больше 2 500 знаков в стиховой книге — мусор импорта, а не стих. **Долг данных: 102 такие строки — они бьют и по читалке, не только по карточкам** | **У5** | `data-audit.py::ЗКН-П015` — SQL по D1: стих с транслитом и переводом >2500 (долг 94) (проверен на живом нарушении) | 🟢 |
| ЗКН-П016 | **ЗАКОН НЕОБХОДИМОСТИ ФАКТА. Раздел определяет РОЛЬ факта, а не книга, в которой он лежит.** Сборщик был СВАЛЬЩИКОМ: брал каждый стих, где мелькнуло имя, и клал в список по книгам и главам — сто цитат, из которых девяносто не говорят о герое ничего. Факт входит в карточку, только если известно **что он даёт** и **куда он идёт**. Роли: **актор** (герой действует → Житие) · **труд** (речь о его книге → Труды) · **авторитет** (на него ссылаются как на прамана → Учение) · **качество** (его славят → Прославление) · **перечисление** (имя в ряду имён → связь, не цитата) · **упоминание** (имя мелькнуло, и ничего → БРИФ, в карточку НЕ входит). Инвариант стоит в самой точке цитирования: даже если сборка ошибётся разделом, свалка не пройдёт | **У5** | `goldforge/role.py::classify` · `compose.quote_of` · `pkl-audit.py::check_role_law` | 🟢 |
| ЗКН-П017 | **ПРОВЕРЯЕТСЯ ОПУБЛИКОВАННАЯ КАРТОЧКА, А НЕ НАМЕРЕНИЯ СБОРКИ.** Гейт судил сборку — но люди читают БАЗУ. Карточка берётся из D1 такой, какой её видит человек, и каждая цитата допрашивается по пяти статьям: **логика** (мог ли подписанный это сказать) · **источник** (стих или предисловие/склейка) · **категория** (раздел соответствует виду источника) · **необходимость** (что факт даёт) · **дословность**. Роль проверяется НА ВЫРЕЗКЕ — на том, что увидит человек, а не на пассаже, из которого её взяли: факт нередко остаётся за краем окна | **У5** | `data-audit.py` — 52 SQL-проверки по D1: судится ОПУБЛИКОВАННАЯ карточка (ЗКН-Ц012) (проверен на живом нарушении) | 🟢 |
| ЗКН-П018 | **ПОРЯДОК КАРТОЧКИ: СМЫСЛ ВПЕРЁД, АППАРАТ НАЗАД.** У Шрилы Прабхупады вышло 14 вкладок: восемь машинных, потом шесть курированных. Человек открывал карточку и получал КАТАЛОГ («Житие · Лиламрита · Том 1»), а СМЫСЛ («Джаладута», «Пророчества», «Парампара», «Кульминация Гауранга Лилы») ждал его на девятой вкладке. **Курированные главы — это КНИГА. Машинные табы — АППАРАТ к ней:** источники, цитаты, провенанс. Аппарат не имеет права стоять впереди книги. Порядок: **[Вклад в Гауранга Лилу] → [главы куратора] → [аппарат машины]** | **У5** | `data-audit.py::ЗКН-П018` — SQL по D1: аппарат не первым табом (проверен на живом нарушении) | 🟢 |
| ЗКН-П019 | **КАРТОЧКА — НЕ ПРОВЕНАНС. Раздела «Источники» в карточке НЕТ.** Человек читает биографию, а не путь к txt-файлу: «Пассажей с именем героя: 25 · docs/sources/hagiography/Rosen_Six-Goswamis.RU.txt» — это бухгалтерия конвейера, а не житие. Провенанс живёт в **досье и брифе куратора**, где ему и место. У каждой цитаты и так стоит ссылка на стих | **У5** | `data-audit.py::ЗКН-П019` — смотрит в БАЗУ, а не в намерение `compose` (проверен на живом нарушении) | 🟢 |
| ЗКН-П020 | **НИКАКОЙ СЛУЖЕБНОЙ ПРОЗЫ В БИОГРАФИИ.** «Что даёт этот раздел: …», «Эпизод: здесь герой действует», «Стихов в этой главе: 4» — так **не пишут в биографии**. Это разметка конвейера, выведенная на глаза читателю. Заголовок главы и сама цитата говорят за себя. Проверка карточки теперь **ЛОВИТ** такую прозу, а не требует её | **У5** | `data-audit.py::ЗКН-П020` — смотрит в БАЗУ (проверен на живом нарушении) | 🟢 |
| ЗКН-П021 | **ЦИТАТА — ЭТО ВЫСКАЗЫВАНИЕ, А НЕ СТЕНА.** Раздел «Его миссия — своими словами» у Шрилы Прабхупады открывался стеной пурпорта в 1 500 знаков, оборванной посреди санскритского слова, — и к миссии не имевшей отношения. Потому что «лучшие» цитаты выбирались **сортировкой по длине**: самое длинное = самое лучшее. Цитата имеет право стоять в книге, только если она: **высказывание** (есть сказуемое, а не обрывок списка имён — «Шримад Бхактисиддханта Сарасвати, А.Ч. Бхактиведанта Свами.») · **целая** (начинается с заглавной, кончается точкой; никаких «....» и обрывов на полуслове) · **читаемая** (не слипшийся импорт — «пран̣аш́йати«Для того…») · **не канцелярия** («ПШП президентам храмов, подписано…» — это шапка письма) · **короткая** (комментарий ≤ 600 знаков, целыми предложениями). Выбор — по цельности, а не по длине | **У5** | `data-audit.py::ЗКН-П021/П021b` — SQL по D1: стена (24) · обрыв (59) (проверен на живом нарушении) | 🟢 |
| ЗКН-П022 | **«ВКЛАД В ГАУРАНГА ЛИЛУ» — ЭТО ОТВЕТ, А НЕ МЕТАДАННЫЕ.** Раздел отвечает на ОДИН вопрос: **что человек дал лиле.** У Шрилы Прабхупады там стоял список названий его книг и заглушка «Личности, с которыми он связан в источниках» — с пустотой под ней. Это метаданные, выставленные на место смысла. **Его вклад — что он вынес движение Махапрабху всему миру, исполнив пророчество «в каждом городе и селении».** И это было НАПИСАНО КУРАТОРОМ — в главах «Ачарья», «Наследие», «Пророчества», «Парампара». Машина поставила свою пустышку ПЕРЕД его текстом: первое, что видел человек. **Есть главы куратора — машина свой «Вклад» НЕ строит** и не соревнуется с человеком. Нет глав — даёт то, что может предъявить честно: кто он в лиле, его труды (кликабельно), слово шастры о нём. Крит. 10 читается ПО СУТИ: карточка обязана **открываться смыслом**, а не аппаратом (Житие · Труды · Учение · Прославление) | **У5** | `data-audit.py::ЗКН-П022` — SQL по D1: заглушка вместо ответа (долг 34) (проверен на живом нарушении) | 🟢 |

### Домен Ф — Инфраструктура / деплой
| ID | Закон | Ур. | Проверка | Статус |
|----|-------|-----|----------|--------|
| ЗКН-Ф001 | `registry-load` — **DISABLED** навсегда без понимания (DROP+CREATE на entity-таблицах) | У3 | workflow disabled (id 286038419) | 🟢 |
| ЗКН-Ф002 | CSV-реестр ≠ полный бэкап D1; сущности/связи через MCP в CSV не отражены | У1 | — | 🟢 |
| ЗКН-Ф003 | Критерий свежести деплоя = `deploy-web.yml` шаги 11/12 зелёные | У3 | GitHub Actions API | 🟢 |
| ЗКН-Ф004 | D1 Time Travel rollback — проверенная процедура (`d1-time-travel.yml`) | У3 | — | 🟢 |
| ЗКН-Ф005 | Финал каждого деплой-ответа: «Обнови жёстко: Cmd+Shift+R» | У1 | — | 🟢 |
| ЗКН-Ф006 | **`vite build` НЕ ловит отсутствующий импорт** — esbuild не проверяет типы. Такой код собирается и падает в браузере ReferenceError = **БЕЛАЯ СТРАНИЦА В ПРОДЕ** (было: 4 файла без импорта `COVER_FALLBACK`). Гейт обязан включать `tsc --noEmit`: любая ошибка TS2304/TS2552 (Cannot find name) роняет сборку  **TS2448 (использование до объявления) — та же бомба, что TS2304.** `/dhana/books` лежал: `LINEAGE_NAV` использовал `LIBRARY` до объявления → ReferenceError → белая страница. Гейт ловил только TS2304 | **У5** | `laws-lint` → `check_missing_imports()` | 🟢 |
| ЗКН-Ф007 | Любое изменение схемы D1 (ALTER/CREATE через MCP) зеркалится файлом `apps/api/migrations/NNNN_*.sql` | У1→У3 | сверка миграций против схемы | 🟡 |
| ЗКН-Ф008 | Роуты с под-путями `/api/me/*` (pushApi, vowsApi) монтируются ДО `accountApi` (иначе он ловит весь `/api/me`) | У1→У5 | grep порядка в `worker.ts` | 🟡 |
| ЗКН-Ф009 | Максимум 3 крон-триггера на воркер — консолидировать в `scheduled()` с тайм-гейтингом | У1→У5 | grep triggers в `wrangler.toml` | 🟢 |
| ЗКН-Ф012 | **`set()` НАБОРА ДИАКРИТИКИ — ЛОВУШКА.** `set("а̄ӣӯр̣н̣ш́…")` разбивает строку на символы, и комбинирующие знаки идут ОТДЕЛЬНО от букв. В наборе оказываются обычные «а», «р», «н», «ш», «т», «д», «м» — они есть в КАЖДОМ русском слове. Определитель транслитерации считал таковой любую русскую строку. Брать ТОЛЬКО комбинирующие знаки: `U+0304 U+0323 U+0301 U+0307` | У2 | `ingest-chaitanya.py::DIACRITICS` | 🟢 |
| ЗКН-Ф020 | **`/sw.js` НИКОГДА НЕ КЕШИРУЕТСЯ. ЭТО БЫЛ ЗАМКНУТЫЙ КРУГ — И ОН СТОИЛ НЕДЕЛЬ.** Service Worker обновляется, **только когда браузер сходит за новым файлом `/sw.js`**. А браузер, по спецификации, уважает `Cache-Control` **на самом этом файле**. Он уходил через ASSETS как обычная статика — **с кешем**. Круг: **старый SW отдаёт старый бандл → правки не доезжают; новый `sw.js` не запрашивается → старый SW не сменится никогда.** Я чинил, деплоил — а основатель видел **прежние баги в новом деплое**. Это и есть «ничего не изменилось» после КАЖДОЙ починки. `index.html` от кеша был защищён, `sw.js` — **нет**. Плюс `updateViaCache: "none"` при регистрации и одноразовая чистка старого воркера | **У5** | `infra-audit.py` (проверен на живом нарушении) | 🟢 |
| ЗКН-Ф019 | **КОД ПРИЛОЖЕНИЯ НИКОГДА НЕ БЕРЁТСЯ ИЗ КЕША ПЕРВЫМ.** SW кэшировал ВСЁ «сначала кеш» — включая оболочку и бандл. Человек **жил на старом коде**: я чиню, деплою, а у него из кеша поднимается прежняя сборка. **Правки не доезжали.** Оболочка и JS/CSS — **всегда из сети**; кешируем только то, что не меняет поведение (картинки, шрифты) | **У5** | `infra-audit.py` | 🟢 |
| ЗКН-Ф022 | **ПУШ ИЗ ВОРКФЛОУ НЕ ЗАПУСКАЕТ ДЕПЛОЙ. ДАННЫЕ БЕЗ ДЕПЛОЯ = ДАННЫХ НЕТ.** GitHub **намеренно** не поднимает воркфлоу на коммиты, сделанные `GITHUB_TOKEN` (защита от рекурсии). Значит любой пайплайн, кладущий ассеты в `apps/web/`, кладёт их **в репозиторий** — и **никогда** не доносит до людей. И всё при этом **зелёное**: файл есть, коммит есть, воркфлоу «success» — а в проде пусто. Так чуть не пропал архив календаря: **4403 файла легли в main, `deploy-web` не проснулся**, и работа считалась бы сделанной. Тот же корень, что у Ф021: **починка, которая не доехала, — не починка**. Пайплайн обязан САМ дёрнуть `deploy-web` (`actions: write` + dispatch), причём **только если коммит реально был**. Гейт нашёл **16 пайплайнов** с этим капканом (сборка мира GCal, уплотнение СНГ, обложки, каталог, центры, здоровье мест, стихи ВД) — **храповик**: долг зафиксирован, новый нарушитель роняет сборку | **У5** | `infra-audit.py::check_f022` (проверен на живом нарушении) | 🟡 |
| ЗКН-Ф021 | **ПОЧИНКА, КОТОРАЯ НЕ ДОЕХАЛА, — НЕ ПОЧИНКА. И ГЕЙТ ОБЯЗАН ПРОВЕРЯТЬ ТО, ЧТО ВИДИТ ЧЕЛОВЕК.** Код Н039–Н043 был исправлен, собран, задеплоен, все гейты зелёные — а основатель видел **прежние баги**. Круг: старый SW отдаёт старую оболочку → человек грузит старый бандл → самолечение **живёт в бандле** и до него не доезжает → бандл не обновится **никогда**. Разорвать круг изнутри приложения нельзя: всё внутри бандла приходит из кеша. Мимо кеша идёт **ровно один файл** — `/sw.js` (`no-cache`, Ф020). Значит рычаг один: **сменить его байты** (версия `CACHE`) при каждой починке, которая обязана доехать. Второй корень — **гейт врал**: Step 13 проверял свежесть только на `/__deploycheck-$RANDOM`, пути, которого **нет ни в одном кеше** — запрос всегда доходил до воркера, и гейт всегда был зелёным, пока люди сидели на старом коде. Гейт проверяет **реальные адреса** (`/`, `/books`, `/kirtans`, `/dhama`) · **§2. ДЕПЛОЙ ОБЯЗАН ДОКАЗАТЬ СЕБЯ, А НЕ ОТРАПОРТОВАТЬ.** 13.07.2026 две правки подряд «выкатились» зелёными — и обе не доехали: и домен, и `workers.dev` (то есть МИМО всех кэшей) отдавали вчерашний код. Основатель смотрел на старое приложение и был прав, что злился. **КОРЕНЬ:** воркер работает в режиме версий (gradual deployments) — `wrangler deploy` ЗАЛИВАЕТ версию, но НЕ переводит на неё трафик. Перевод делал шаг промоушена, и Cloudflare ОТКЛОНЯЛ его каждый раз: `10210 Invalid annotations: "workers/triggered_by" is not supported`. Ответ никто не читал (`set +e`, результат только в `echo`) — **версия заливалась, трафик оставался на старой, деплой рапортовал успех**. **ВТОРАЯ ДЫРА — САМ ГЕЙТ:** он сверял ХЭШ файла `/assets/index-<hash>.js` из свежей `dist` против живого. Хэш ВХОДНОГО чанка не меняется, если правка легла в лениво-подгружаемый чанк — «ожидаемое» равнялось «живому», и гейт был зелёным, хотя не уехало НИЧЕГО. **ЛЕЧЕНИЕ:** (1) в бандл вшивается **ШТАМП КОММИТА** (`buildStamp.ts` → `window.__IOL_BUILD__`, `<html data-build>`), и гейт ищет ЭТОТ SHA **в теле живого файла** — совпасть случайно нечему; (2) гейт проверяет **ОБА входа** — apex (через край) и `workers.dev` (мимо края): расхождение между ними и есть «кэш держит старое»; (3) промоушен **перечитывается**: боевая версия обязана равняться залитой, иначе ОШИБКА. **Правило: ни один шаг деплоя не смеет глотать ответ API.** `set +e` + `echo` = молчаливый сбой | **У5** | `deploy-web.yml` Step 13 · `sw.js::CACHE` | 🟢 |
| ЗКН-Н040 | **РОУТЕР НЕ СТАВИТ ВКЛАДКУ, КОТОРОЙ НЕТ. ЭТО БЫЛ КОРЕНЬ БЕЛЫХ ЭКРАНОВ ВСЕГО ПРИЛОЖЕНИЯ.** `Screen` рисует **пять** вкладок (`sadhana · krishna · gauranga · iskcon · bogatstva`). А `applyPath` ставил ещё **шесть несуществующих**: `books · kirtans · dhama · acharya · account · feed` — и делала `return`, из-за чего **настоящие обработчики этих адресов становились недостижимы**. Незаконная вкладка = ни одна ветка не совпала = **пустой экран, молча, без ошибки**. Витрина открывалась только мышью из зала; тот же адрес «по ссылке» или «назад» давал белый лист: ИСККОН→Практика→«Мантра» (`/kirtans`), Дхама→место→назад (`/dhama`), Бхагавад-гита→назад (`/books`). Второй такой же экран — **Календарь**: `/calendar` уводил в ИСККОН и просил подтаб `calendar`, которого в `HOME_TABS` **нет**. Источник законных вкладок ОДИН — `TAB_IDS`; `Screen` имеет **запасную ветку** (экран никогда не пуст) | **У5** | `nav-audit.py::check_n040` | 🟢 |
| ЗКН-Н039 | **АДРЕС МЕНЯЕТСЯ → РОУТЕР ЗНАЕТ. ВСЕГДА. ОДНИМ ПУТЁМ.** `notifyNav` звался **только из popstate** — значит роутер и подписчики (подтабы залов, ПКЛ) узнавали о смене адреса **только когда человек жал «назад»**. А `pushUrl` из вкладки/зала/книги менял адрес **молча**: экран и адрес расходились, один и тот же адрес вёл себя **по-разному в зависимости от того, как в него попали** («работает через раз»). Теперь запись истории **любым** способом идёт одной цепочкой: `история → роутер (состояние App) → подписчики`. Прямой `history.pushState` в обход `nav.ts` запрещён | **У5** | `nav-audit.py::check_n039` · `nav.ts::setRouter` | 🟢 |
| ЗКН-Н041 | **ПИСАТЕЛЬ И ЧИТАТЕЛЬ АДРЕСА — ОДИН СЛОВАРЬ.** Садхана: писатель слал «Практику» на `/sadhana`, а читатель искал сегмент `practice`; «Кабинет» на `/id` — читатель искал `account`. Разъехались молча: нажал — работает; вернулся «назад» или обновил — **снова «Лента»**. И зал Садханы **не был подписан на адрес вовсе** (Богатства были). Раздел, в который нельзя вернуться, — раздел **без адреса** | **У5** | `nav-audit.py` · `sadSubFromPath` | 🟢 |
| ЗКН-Н042 | **У ВКЛАДКИ МНОГО ЗАКОННЫХ АДРЕСОВ — НЕЛЬЗЯ СВОДИТЬ ЕЁ К ОДНОМУ.** `pathFromState` отдавал для «Богатств» `/hero` с **любого** адреса, кроме `/hero*`. Стоя на `/kirtans`, любое изменение состояния роняло эффект «состояние → URL», тот видел `/hero ≠ /kirtans` и **переписывал адрес сам, без клика**. Правило: `BASE_OF` знает, какой вкладке принадлежит адрес; принадлежит текущей — **он и есть истина**. Каждая in-app ссылка обязана иметь основу в `BASE_OF` | **У5** | `nav-audit.py::check_n042` | 🟢 |
| ЗКН-Н043 | **ФЛАГ «ПРИШЛО ИЗ РОУТЕРА» НЕ ДОЛЖЕН ЗАЛИПАТЬ.** `fromPop` ставился в `applyPath`, а сбрасывался эффектом-синхронизатором — но тот бежит, **только если что-то изменилось**. Применили тот же адрес, ничего не изменилось — флаг остался поднятым **навсегда** и съедал **следующий честный переход**: экран менялся, адрес — нет. `routeGen` растёт на каждом применении пути → эффект просыпается **всегда** → флаг сбрасывается **всегда** | **У5** | `nav-audit.py` · `routeGen` | 🟢 |
| ЗКН-Н038 | **КОМПОНЕНТ НЕ ПИШЕТ ЧУЖОЙ АДРЕС.** ЭТО И БЫЛА ПРИЧИНА, ЧТО ПОДТАБ ПЕРЕЕЗЖАЛ МЕЖДУ ЦАРСТВАМИ. При клике «Гауранга» адрес мгновенно становится `/gauranga`, но старый `EntityPage(krishna)` **ещё жив** (React размонтирует его в следующем проходе) — и его эффект-писатель вписывает **свой** таб на **уже чужой** адрес: `/gauranga/rupa/zolotaya`. Ни `key`, ни подписка этого не лечили: **беда была не в чтении, а в ЗАПИСИ**. Проверка владения: адрес не мой — не трогаю | **У5** | `nav-audit.py` | 🟢 |
| ЗКН-Р013 | **СВЯЗЬ ОБЪЯВЛЕНА В ДОСЬЕ → ОНА ЕСТЬ В ГРАФЕ.** Досье содержит поле `see` — «смотри также». Это **объявленная** связь: автор карточки уже сказал, с кем личность связана. Но в графе её **не было**: **1807 связей** лежали в досье и не были рёбрами, **62 личности висели сиротами** — с богатыми досье (до 7000 знаков), но **без родства**. Открыв такую карточку, человек не видел, чей это ученик и чей спутник. Материализация закрыла **59 из 62**. Тип связи — `see-also`: досье говорит «смотри также», но **не говорит, КЕМ приходится**; достроить до `disciple-of` без источника — **соврать** | **У5** | `data-audit.py` | 🟢 |
| ЗКН-Р012 | **ПЕРЕИМЕНОВАЛ КЛЮЧ — ПЕРЕИМЕНУЙ ВЕЗДЕ, ГДЕ ОН ССЫЛКА.** Я вычистил слаги в `prayers` (были чужие пути `/ru/gaura-arati`) — и **не тронул `prayer_verses`**, где тот же слаг лежит **ссылкой**. Связь порвалась **полностью**: **1961 строка стихов, 158 бхаджанов** — все отвязаны. **Текст в базе ЕСТЬ, приложение его НЕ НАХОДИТ.** И это **молчит**: ни ошибки, ни падения — бхаджан просто открывается пустым, будто текста никогда и не было. Тот же почерк, что Н025 («адрес переехал — читатель остался»), только **в данных**. **Ключ — не строка. Ключ — это СВЯЗЬ** | **У5** | `data-audit.py` | 🟢 |
| ЗКН-Б012 | **СТИХ БЕЗ ПЕРЕВОДА — НЕМОЙ СТИХ.** Читатель видит бенгальский стих и толкование к нему — но **не понимает, что стих говорит**. Толкование объясняет то, чего он **не прочёл**. Так было с **43 стихами «Чайтанья-бхагаваты»**. Переводы всё это время **лежали в источнике**: между стихом и переводом вклиниваются **колонтитулы страницы** («Стих 2», «Глава 5»), попавшие в текст при распознавании — разбор упирался в них и **бросал**. **Возвращено 30.** Осталось 13: 2 слитых диапазона + **11, где отпечаток неоднозначен** (пять первых слов совпали, а стихи разные). Подставить любой — дать стиху **чужой перевод**. Это **хуже пустого места**: пустое видно сразу, а подменённый перевод **читается как настоящий** и живёт в книге, пока кто-нибудь не сверит с оригиналом — а сверять **никто не будет: он же выглядит правильно**. **Лучше немой стих, чем лживый** | **У5** | `data-audit.py` · признанный долг **13** | 🟢 |
| ЗКН-Б013 | **ОЧЕРЕДЬ ПЛЕЕРА ОПОЗНАЁТСЯ ПАРОЙ (ИСТОЧНИК, ИМЯ) — ОДНИМ ИМЕНЕМ НЕЛЬЗЯ.** Общая очередь раздела зовётся `all` у киртанов, у катхи и у любой следующей аудиотеки: имя уникально ВНУТРИ раздела, а не между разделами. Пойман на Катхе до выпуска — `loadKirtan` сверял только `bookRef.current`, и переход Катха → Киртаны давал ранний выход «уже загружено»: витрина киртанов показывала катху. Экран говорит одно, звучит другое. Каждый ранний выход `load*` обязан сверять и `sourceRef.current` | **У5** | `product-audit.py::check_b013` | 🟢 |
| ЗКН-Б009 | **ТЕКСТ БЕЗ СКЕЛЕТА — МОЛЧАЛИВАЯ ПОЛОВИНЧАТОСТЬ.** Книга живёт в ДВУХ таблицах: `verses` — скелет (id, раздел, порядок), `verse_texts` — сам текст. **Тексты льются своим ключом и проходят ВСЕГДА. Скелеты могут затереть друг друга.** `ref` был «Живопись · 67-12» — но **два письма одной темы в один месяц дают ОДИН И ТОТ ЖЕ `ref`**, и `INSERT OR REPLACE` затирал предыдущее (на `verses` стоит UNIQUE по `work_id, ref`). Потерялось **1547 наставлений**. И это **молчит**: текст в базе ЕСТЬ, а в книге его НЕТ — книга читается через скелет. Гейт нашёл ещё **167 сирот в «Чайтанья-бхагавате»**, о которых никто не знал: **142 настоящих стиха вернулись в книгу** | **У5** | `data-audit.py` | 🟢 |
| ЗКН-Б008 | **У КАЖДОГО БХАДЖАНА ЕСТЬ СТИХИ. РАЗДЕЛЫ КАТАЛОГА — НЕ БХАДЖАНЫ.** «178 бхаджанов без перевода» оказалось **двумя**: 179 из них — навигационные строки (`is_catalog`/`is_section`). **Считать их за молитвы — врать себе о размере долга.** Настоящих было две — обе **поэмы самого Прабхупады**, написанные на борту «Джаладуты»: «Маркине бхагавата-дхарма» (16 стихов) и «Молитва лотосным стопам Кришны» (12). Текст был — лежал единым куском | **У5** | `data-audit.py` | 🟢 |
| ЗКН-Р011 | **ЗНАЧЕНИЕ ФИЛЬТРА В КОДЕ ОБЯЗАНО СУЩЕСТВОВАТЬ В БАЗЕ.** Фильтр книг искал `lineage === "iskcon"`, а в базе — **`"guru-iskcon"`**. Счётчик показывал **0**, хотя книги есть («Уроки любви» Бхакти Вигьяны Госвами Махараджа). **Ноль выглядит как «книг нет», а не как «фильтр сломан»** — молчаливая ложь | **У5** | `product-audit.py` · `data-audit.py` | 🟢 |
| ЗКН-Р014 | **СКРИПТОВАЯ ПРАВКА НЕ УДАЛЯЕТ ТО, ЧЕГО Я НЕ ВИДЕЛ ГЛАЗАМИ.** Сводя четыре копии `plural` в примитивы, я вырезал их регуляркой `function X\(...\)\s*\{.*?\n\}\n` с флагом DOTALL. В `VowScreen` функция была **однострочной** — закрывающей `}` в начале строки у неё нет, и `.*?` пробежал ДАЛЬШЕ, до конца **следующей** функции: вместе с `pluralDays` молча исчезли `roundsWord`, `japaCommitment` и **целый компонент `Ring`**. `vite build` этого НЕ ПОЙМАЛ (esbuild не проверяет типы) — поймал только `tsc`. Сборка «зелёная» при вырезанном компоненте — это ЗКН-Ф016 в чистом виде. **Правило:** после любой скриптовой правки кода — (1) `tsc` со сверкой с ФОНОМ (ноль НОВЫХ ошибок, а не «примерно столько же»), (2) глазами прочитать КАЖДУЮ удалённую строку `git diff`, а не только те файлы, где что-то закричало | **У5** | `tsc-gate.py` — ноль НОВЫХ ошибок к зафиксированному фону (в CI, рядом с линтером); + разбор каждой удалённой строки `git diff -U0` | 🟢 |
| ЗКН-Р015 | **«ГОТОВО» = УВИДЕЛ НА ЖИВОМ. НЕ «ЗАДЕПЛОИЛ», НЕ «ГЕЙТ ЗЕЛЁНЫЙ».** 13.07.2026 я дважды доложил «выкачено» — и дважды на проде лежал старый код. Основатель нашёл это ЗА МЕНЯ, второй раз подряд. Его слова: «ты должен каждый раз это проверять, а не отчитываться; я не должен за тебя всё просматривать». Он прав. Зелёный CI — это не доказательство: он проверяет СВОЮ работу, а не результат. **Правило:** перед словом «готово» — прогнать `prod-probe` по ЖИВОМУ сайту со списком приёмки: что обязано ИСЧЕЗНУТЬ и что обязано ПОЯВИТЬСЯ. Зонд качает вход и ВСЕ ленивые чанки (~43 файла, ~2 МБ) и грепает по ним. Не сошлось — доклада нет. Результат приёмки идёт в ответ основателю ВМЕСТЕ с докладом, чтобы ему не пришлось перепроверять | **У5** | `prod-probe.yml` (inputs: `absent` / `expect`) + штамп сборки (ЗКН-Ф021 §2) | 🟢 |
| ЗКН-Ф017 | **SERVICE WORKER ОБЯЗАН ВЕРНУТЬ RESPONSE. ВСЕГДА. КОРЕНЬ ВСЕХ БЕЛЫХ ЭКРАНОВ.** В `sw.js` стояло `const net = fetch(req).then(...).catch(() => hit); return hit \|\| net;`. Если в кеше **пусто** И сеть упала — `.catch(() => hit)` отдаёт **`undefined`**. `respondWith(Promise<undefined>)` → браузер бросает **«TypeError: Failed to convert value to Response»** → ответа нет → **БЕЛЫЙ ЭКРАН**. Било по «назад» **везде** — из дхамы, из киртана, из Бхагавад-гиты: **«назад» это навигация**, а навигация шла через тот же сломанный SW. В ветке навигации стоял `Response.error()` — явный сетевой сбой, и браузер честно показывал пустоту. Из `respondWith` **всегда** выходит настоящий Response; пустоту — **никогда** | **У5** | `infra-audit.py` (проверен на живом нарушении) | 🟢 |
| ЗКН-Ф018 | **`onClick={fn}` ПЕРЕДАЁТ СОБЫТИЕ МЫШИ КАК АРГУМЕНТ.** `onClick={onOpenBook}` слал в функцию событие; оно улетало в адрес и превращалось в **`/prasad/book/[object Object]`** → «Глава не найдена». Если функция ждёт другой аргумент — звать через стрелку: `onClick={() => onOpenBook()}` | У2 | комментарий в коде | 🟢 |
| ЗКН-Н037 | **ПУТЬ ЧИТАЕТСЯ СВЕЖИМ, А НЕ ИЗ СНИМКА ПРИ МОНТИРОВАНИИ.** `useRef(window.location.pathname)` снимает путь **один раз** и держит вечно: адрес сменился — снимок нет. Так смена царства **уносила подтаб** (Кришна/Гуна → Гауранга/Гуна). И две подписки на адрес **спорили между собой**: одна ставила таб из адреса, вторая — из снимка | **У5** | `nav-audit.py` · `pathNow` | 🟢 |
| ЗКН-Ф016 | **ОШИБКА ТИПА = КРАШ В БРАУЗЕРЕ. ОДИН КРАШ УБИВАЛ ВСЁ ПРИЛОЖЕНИЕ.** В Рецептах стояло `CATEGORIES.map((c) => ({ id: c, label: c }))`, где `CATEGORIES` — **уже** `{id,label}[]`. React получал ОБЪЕКТ вместо текста и падал: **«Minified React error #31»**. И вот что важнее: React, **не найдя границы ошибок, размонтирует ВСЁ ДЕРЕВО** — человек видел белый лист **везде**, на «назад» и на любой вкладке, пока не перезагрузит. **Один битый экран делал мёртвым всё приложение** — отсюда и загадочное «через раз». Сборка не ловила: **esbuild не проверяет типы**, а типы React **вообще не были установлены** (223 ошибки, 89 из них — шум «нет namespace React»), и в этот шум никто не смотрел. Поставлены типы (223 → 20), **граница ошибок** и **храповик** | **У5** | `laws-lint` храповик `type_errors: 20` · `ui/ErrorBoundary.tsx` | 🟢 |
| ЗКН-Ф015 | **D1: «too many terms in compound SELECT».** Цепочка `UNION ALL` из 10 звеньев не проходит. Брать `WITH t(a,b) AS (VALUES …)` | **У5** | `laws-lint` храповик `long_union: 0` | 🟢 |
| ЗКН-Ф013 | **D1: «too many SQL variables». ПРЕДЕЛ ~100 ПЕРЕМЕННЫХ НА ЗАПРОС.** Считать надо **произведение**: (полей на строку) × (размер батча). Сейчас в `ingest-books.py` **5 × 20 = 100 — ровно на пределе**: одно новое поле в схеме, и заливка книг **молча упадёт** (6 × 20 = 120). Ошибка выглядит как «база не отвечает», а не как «запрос слишком широкий». Глазами не поймать — **гейт считает** | **У5** | `infra-audit.py` (считает полей × батч; проверен) | 🟢 |
| ЗКН-Ф014 | **СКРИПТ ГОВОРИТ, ЧТО ИМЕННО СЛОМАЛОСЬ.** `urlopen` кидает `HTTPError` на 400, и без перехвата CI показывает лишь «exit code 1». Три прогона ушли впустую, пока `d1()` не начал печатать тело ответа. Ошибка HTTP всегда разворачивается в `::error::` с текстом и куском SQL  **Найдено 16 голых `urlopen`** в инструментах — все падали бы молча. Свои гейты защищены; остальные — **храповик**: рост запрещён. **Статья Б — ОДНА ФЛАКА НЕ ГЛУШИТ ВЕСЬ АУДИТ (18.07.2026).** `d1()` поднимал `SystemExit` прямо из себя, а цикл `main()` ловит `Exception` — и ОДИН случайный «internal error» D1 (код 7500, отказ на стороне Cloudflare) убивал все 53 проверки разом. CI краснел так, будто нарушен закон, и держал `laws-lint` красным, МАСКИРУЯ настоящие нарушения (в ту же ночь под этой маской пряталось 4 мёртвых читателя `/account` — ЗКН-Н025). Отличаем ОТКАЗ ИНФРАСТРУКТУРЫ от ДЕФЕКТА ЗАПРОСА: 5xx и таймаут — три попытки с паузой, затем проверка помечается «D1 недоступна» и аудит идёт ДАЛЬШЕ, а прогон возвращает **RC=2** («не выполнилось» ≠ «прошло» ≠ «нарушено»); 4xx (битый SQL, >100 переменных) падает СРАЗУ и громко, без ретраев. Проверено на живых отказах: постоянный 500 → 3 попытки на проверку, все 53 отчитаны, RC=2; 400 → одна попытка, `::error::` с телом и куском SQL | **У5** | `laws-lint`, храповик `bare_urlopen: 16`; `data-audit.py::d1` (ретраи 5xx, `D1Down`, RC=2) | 🟢 |
| ЗКН-Ф010 | Массовая загрузка в D1 — только через HTTP API/воркфлоу с CF-токеном, НЕ инлайном через MCP; D1 энфорсит FK, `INSERT OR IGNORE` НЕ подавляет FK (один сирота валит батч) | У1→У3 | — | 🟢 |

### Домен Пф — Печать и ссылки (PDF · QR · адреса)
| ID | Закон | Ур. | Проверка | Статус |
|----|-------|-----|----------|--------|
| ЗКН-Пф001 | **Версия кэша PDF выводится из СБОРКИ, а не из руки.** PDF отдаются `immutable` на год — это верно только если у нового содержимого новый ключ кэша. Ручной `PDF_CACHE_REV = "c3"` никто не бампал → PDF во всём приложении оставались старыми навсегда. Теперь ключ = SHA коммита, подставляется шагом деплоя | **У5** | шаг «Bump PDF cache revision» в `deploy-web.yml` | 🟢 |
| ЗКН-Пф002 | **PDF собирается из ЖИВЫХ данных**, а не из снимка. Изменился контент → следующий PDF уже новый (гарантируется Пф001) | **У5** | `infra-audit.py` | 🟢 |
| ЗКН-Н020 | **Адрес — только из реестра `routes.ts`.** Хардкод `${ORIGIN}/...` в компонентах запрещён. Мы несколько раз переименовывали маршруты (`/dhana/recipes` → `/dhana/prasad`), и каждый хардкод превращался в битую ссылку; **уже напечатанные QR-коды вели в никуда**. Переименовал в реестре — поменялось везде, включая QR | **У5** | `laws-lint`: `${ORIGIN}/` ИЛИ `https://gaurangers.com/` вне `routes.ts` = fail. Первый линтер ловил только `${ORIGIN}`, пропустив **24** захардкоженных боевых URL — все QR/шаринг | 🟢 |
| ЗКН-Н062 | **ПОЛНЫЙ ЭКРАН СУЩЕСТВУЕТ РАДИ ОБЛОЖКИ.** Раздутая компоновка тесной коробки — это не полноэкранный вид, а та же коробка, только больше: смысла в ней нет. Во встроенном плеере обложка делит место с очередью; во весь экран она ГЛАВНОЕ и берёт всё, что можно. Отдельно: обложка схлопывалась в ноготь, потому что стояла в `grid` + `placeItems:center` с `width:100%` — в центрированной сетке столбец меряется ПО СОДЕРЖИМОМУ, и `100%` разрешается в натуральный размер картинки, то есть в ничто. Ширина обложки задаётся ЯВНО, а не через `100%` в сетке. | `KirtanHero({ big })`, `NowPlaying.tsx` |
| ЗКН-Н063 | **У ПЛЕЕРА ЕСТЬ ПОЛ. ОН НЕ УЖИМАЕТСЯ ДО НЕПРИГОДНОСТИ.** Высота считалась как «остаток экрана» — и на телефоне, где сверху шапка, вкладки, заголовок и поиск, остатка не хватало: плеер схлопывался, обложка становилась ногтем, список — двумя строками, внутри нечего было делать. **Не влезло — пусть прокручивается СТРАНИЦА.** Лучше дотянуться пальцем, чем не суметь пользоваться вовсе. Пол — 600 точек. | `Math.max(600, …)` в `KirtansScreen.tsx` |
| ЗКН-Н064 | **ЛИСТ ВЫБОРА ПРИНАДЛЕЖИТ ОКНУ, А НЕ КОРОБКЕ.** Лист на 83 позиции открывался ВНУТРИ плеера (`position:absolute`) — на телефоне в коробку влезали азбука и ОДНА строка. Выбрать было нечего. Список, который сам по себе является отдельной задачей, требует ВСЕГО ЭКРАНА: `position:fixed`. То же для таймера сна. Азбука при этом **переносится по строкам**, а не обрезается: буква, уехавшая за край, — не указатель. | `DivisionPicker` и лист таймера, `NowPlaying.tsx` |
| ЗКН-Н065 | **ДВА ПЛЕЕРА НА ОДНОМ ЭКРАНЕ — ЭТО ОШИБКА.** На витрине киртанов встроенный плеер уже на виду, а мини-плеер вылезал ПОВЕРХ него: то же название, те же кнопки — и он ЗАСЛОНЯЛ то, чем человек в эту секунду пользуется. Два плеера рядом — не «удобно»: это две правды о том, что играет. Встроенный объявляет о себе (`setEmbeddedVisible`), мини-плеер уступает ему место. | `store.tsx: embeddedOn`, `MiniPlayer.tsx` |
| ЗКН-Н066 | **ЦЕЛЬ КАСАНИЯ — НЕ МЕНЬШЕ 44 ТОЧЕК.** Палец не курсор: он мягкий, широкий и не видит, куда бьёт. Apple меряет 44pt не из вкусовщины — ниже начинаются ПРОМАХИ. В плеере было шестнадцать целей меньше нормы: буквы азбуки 26, переключатели 28, сердце 30, транспорт 34, полоса перемотки 16. **Знак может быть мелким — ЗОНА обязана быть крупной.** | `const TAP = 44` в `NowPlaying.tsx`; `bareBtn`/`iconBtn` зажимают через `Math.max` |
| ЗКН-Н067 | **ЖЕСТ НЕ СРАБАТЫВАЕТ ТАМ, ГДЕ ЕГО НЕ ЖДУТ.** Тап по пустому месту раскрывал плеер на весь экран — в том числе в СПИСКЕ, где человек листает и промахивается мимо строки. Его выбрасывало в полный экран посреди выбора. Раскрытие живёт только там, где человек не листает (панель «Играет», обложка). Жест, срабатывающий не там, — не удобство, а ловушка. | `onClick={embedded && pane === "now" …}`, `NowPlaying.tsx` |
| ЗКН-Н068 | **`position: fixed` НЕ РАБОТАЕТ ВНУТРИ СКРОЛЛЕРА — РИСУЙ ПОРТАЛОМ.** Лист выбора объявлен `fixed`, а вёл себя как `absolute`: его запирало в коробке плеера. Причина не в CSS — в Safari на iOS `fixed` внутри контейнера с `-webkit-overflow-scrolling: touch` прижимается к СКРОЛЛЕРУ, а не к окну. Со Safari спорить нечем. **Полноэкранный слой рисуется ПОРТАЛОМ в `body`** — тогда он принадлежит окну по факту, а не по объявлению. | `createPortal(…, document.body)` в `NowPlaying.tsx` |
| ЗКН-Н069 | **СТРАНИЦА ОСТАВЛЯЕТ МЕСТО ПОД ПЛАВАЮЩИМ МЕНЮ.** Нижнее меню висит НАД страницей. Если страница не расступилась, её последний экран НЕДОСТИЖИМ: доскроллить нельзя — меню просто закрывает содержимое. Транспорт плеера (скорость, повтор, таймер) оказался под белой плашкой и был недоступен. Отступ снизу ≥ высоты меню + safe-area. | `paddingBottom: calc(96px + env(safe-area-inset-bottom))` |
| ЗКН-Н070 | **ДЫХАНИЕ БРАУЗЕРА — НЕ ПЕРЕСТРОЙКА. НЕ ОТВЕЧАЙ НА НЕГО.** Safari прячет адресную строку при прокрутке: `innerHeight` меняется на ~100 точек, `resize` срабатывает — и плеер пересчитывал высоту ПРЯМО ВО ВРЕМЯ ЖЕСТА. Экран дёргался под пальцем, палец промахивался. **Настоящая перестройка меняет ШИРИНУ** (поворот, разделение экрана, смена окна) — по ней и судим. Дрожание высоты игнорируем. | `if (window.innerWidth === lastW) return;` в `KirtansScreen.tsx` |
| ЗКН-Н071 | **ЖЕСТ ТАМ, ГДЕ ПАЛЕЦ.** В полноэкранном плеере палец УЖЕ на обложке — там, куда смотрят глаза. Вбок — предыдущая и следующая запись; вниз — свернуть. Плеер, где закрывать надо крестиком в углу, а листать — кнопками внизу, заставляет палец ЕЗДИТЬ по экрану вместо того, чтобы слушать. Направление решается по БОЛЬШЕЙ составляющей: жест редко бывает чистым, и гадать по одной оси — значит срабатывать не на то. | `KirtanHero({ onSwipe, onDismiss })`, `NowPlaying.tsx` |

### Домен Пл — Пайплайны наполнения (автоскрипты) · ✅ УТВЕРЖДЁН 11.07.2026
| ID | Закон | Ур. | Проверка | Статус |
|----|-------|-----|----------|--------|
| ЗКН-Пл001 | Календарь наполняется автоматически (Vaishnava/Gaurabda-движок → `gcal_days`; воркфлоу `vaisnava-calendar-fetch`/`gcal-ingest`); события синхронизированы с личностями (лила/явление/уход) | У3 | воркфлоу + `/api/calendar` | 🟢 |
| ЗКН-Пл011 | **КАЛЕНДАРЬ БЕЗ ПРОШЛОГО — ПОЛОВИНА КАЛЕНДАРЯ.** Экран показывал только будущее (`filter(e => e.date >= today)`), а фиды начинались с 2026-01-01: «когда была Гаура-пурнима в 2020-м» приложение не отвечало **никак**. Движок GCAL астрономический — считает **любой** год; отсутствовало не знание, а данные. Архив `/data/gcal-past` закрывает 2016–2025 по каждому городу. **Пояс — ловушка:** 4403 живых фида собраны ТРЕМЯ пайплайнами, зона лежит только у 272; ошибиться поясом = молча сдвинуть прошлое на день. Поэтому **гейт паритета**: восстановленная локация обязана воспроизвести уже отгруженный живой фид **до минуты**, иначе город не пишется. Проверено негативно — чужой пояс отвергается | **У5** | `generate_past_archive.py` (гейт паритета) · `gcal-past-archive.yml` | 🟢 |
| ЗКН-Пл002 | Лента наполняется автоматически (`feed_video`/даршан; воркфлоу `tg-video`/`darshan-daily`, крон); тяжёлое медиа — на Internet Archive, инлайн-воспроизведение через `/video/` прокси | У3 | воркфлоу | 🟢 |
| ЗКН-Пл003 | **ОДНА СЕССИЯ TELEGRAM — ОДНА ОЧЕРЕДЬ.** Сессия (`TG_SESSION_STRING`) ОДНА на все качалки — и на вход тоже. **Вход создаёт новую сессию и тем самым ГАСИТ старую:** качается видео — оборвётся на полпути, а логи скажут «сессия недействительна», и настоящая причина (вход, запущенный рядом) останется невидимой. Такую поломку **невозможно воспроизвести** — она зависит от того, кто когда нажал. Гейт нашёл `tg-login.yml` **без общей очереди** | **У5** | `infra-audit.py` (проверен на живом нарушении) | 🟢 |
| ЗКН-Пл004 | Общий стандарт пайплайна: идемпотентность, тяжёлое медиа — на Internet Archive (не в репо), крон-тайм-гейтинг (лимит 3 триггера), scrub секретов. Распространяется и на контентные пайплайны (кузница золота `tools/goldforge/`) | У1→У3 | §6 | 🟡 |
| ЗКН-Пл025 | **ВИДЕО ИЗ TELEGRAM — ЧАСТО И АВТОМАТИЧЕСКИ В ARCHIVE.ORG; ЛЕНТА ИГРАЕТ С ЗЕРКАЛА, БЕЗ СЫРОГО TELEGRAM-ВИДЖЕТА.** Усиление Пл002 (была У3 — «на словах»). Конвейер `tg-video` мирроринг видео в archive.org работал по крону, НО каждые **2 часа** — свежее видео до следующего прогона висело в ленте сырым Telegram-виджетом (`?embed=1&mode=tme`), а большое показывало **«Media is too big / VIEW IN TELEGRAM»**. Это выглядит как «скрипт не работает / ручной режим», хотя он автоматический — просто **окно до 2ч** + **уродливый фолбэк**. Диагноз: конвейер зелёный (мирроринг до post_id, `skip-ids` пропускает готовые), воркер `workerHome.ts` джойнит `feed_video` и подставляет `src` с archive.org для больших видео; сырой виджет — только пока не замирроренo. Фиксы: (1) cron `17 */2` → **`*/30`** (миррор каждые 30 мин, не сталкивается с tg-audio :05 / tg-stories :23 — cron-lint зелёный); (2) `VideoBox` **убрал встроенный Telegram-виджет** — незамирроренное видео открывается в Telegram новой вкладкой, а в ленте остаётся только наша опрятная превью-карточка. Инварианты: миррор **частый** (cron `*/N` ≤ 60 мин, не ручной), лента подаёт видео **через джойн `feed_video` с archive.org**, **никакого `embed=1`-виджета** в ленте | **У5** | `infra-audit.py::check_pl025` (проверен на двух живых нарушениях — cron `17 */2` и возвращённый `embed=1&mode=tme` → красный; восстановление → зелёный) · `cron-lint.py` держит отсутствие клэша слотов | 🟢 |
| ЗКН-Пл006 | **ИСТОЧНИК ВАЖНЕЕ МЕТОДА.** Прежде чем чинить данные, спроси: откуда они. Локатор центров был мёртв (792 из 956 без координат) — и это была НЕ проблема геокодинга: 729 записей пришли из архива `iskcondesiretree` 2011–2013, а все 164 координаты — из официального `centres.iskcon.org`. Геокодировать протухшие адреса = поставить точки, ведущие в никуда | У2 | `centres-official.py` · `vraja-coords.py` | 🟡 |
| ЗКН-Пл007 | **ДАННЫЕ НЕ ПРОТУХАЮТ ПО-РАЗНОМУ.** Центры ИСККОН переезжают и закрываются → адрес из архива опасен. Места Враджа (гхаты, кунды, храмы) стоят веками → координата верна навсегда. Один и тот же приём (геокодинг) в одном случае ложь, в другом истина. Решает ПРИРОДА объекта | У1 | документ | 🟡 |
| ЗКН-Пл008 | **СОВПАДЕНИЕ ПРОВЕРЯЕТСЯ, А НЕ ПРИНИМАЕТСЯ.** Геосервис на «Сурья-кунда» вернул координаты **Радха-кунды** — и молча. Ответ пришёл, поле заполнилось, и любой гейт сказал бы «координата есть». **Машина не говорит «не знаю» — она отдаёт БЛИЖАЙШЕЕ и уверенно.** Принять её ответ без проверки — **поставить паломника не туда, куда он шёл**. След ошибки: два **независимых** места в одной точке (храм внутри места — законно) | **У5** | `data-audit.py` (проверен на боевой базе) | 🟢 |
| ЗКН-Ц003-бис | **СКРИПТ ПАДАЕТ БЫСТРО.** Если источник молчит — 15 отказов подряд и стоп. Иначе скрипт часами висит на таймаутах, создавая видимость работы | У2 | `vraja-coords.py` | 🟢 |
| ЗКН-Пл009 | **СУЩЕСТВУЮЩАЯ КООРДИНАТА НЕ ПЕРЕЗАПИСЫВАЕТСЯ МАШИНОЙ.** Заполняем только ПУСТОЕ. У «Шриваса-анганы» и «Самадхи Чанда Кази» координаты в базе расходятся с геосервисом на 1.8 и 4 км — потому что есть **исторический спор**, где эти места (Маяпур или город Навадвипа). Позиция приложения по спорному вопросу — решение человека, а не скрипта  **Гейт сразу поймал нарушителя — мой же `vraja-coords.py`:** он писал `UPDATE … WHERE id = ?` без `AND lat IS NULL` и перезаписал бы координату, поставленную человеком | **У5** | `infra-audit.py` | 🟢 |
| ЗКН-Пл010 | **«СТРУКТУРЫ НЕТ» — ОБЫЧНО ЗНАЧИТ «ИЩУ НЕ ТАМ».** Дважды я объявлял книгу неразбираемой — и дважды ошибался. **Мадхья-кханда ЧБ**: искал заголовки «Глава N», их не было → «главы не размечены». Они размечены **МАРКЕРАМИ КОНЦА**: «Так заканчивается двадцать восьмая глава… — "Господь принимает санньясу"» — 28 глав подряд, с названиями. **Навадвипа-дхама-махатмья**: искал «Глава N» цифрой, нашёл только оглавление → «структура не проверяема». Номер стоял **СЛОВАМИ в колонтитуле** («Глава первая» ×164), граница — смена номера: 18 глав подряд. Прежде чем объявить книгу неразбираемой — перебери ВСЕ формы разметки: заголовок · маркер конца · колонтитул · цифрой · словами | У1 | документ | 🟢 |
| ЗКН-Пл016 | **ИСТОЧНИК СМЕРТЕН. ПРОВЕРЬ ЕГО, ПРЕЖДЕ ЧЕМ ВИНИТЬ СКРИПТ.** Координаты **520 мест Враджа** должны были прийти с `vrajapedia.com`: ссылки проставлены, инструмент написан, воркфлоу настроен, гейт проверяет попадание точки во Врадж. **Сайт умер.** Он отвечает `302` **на самого себя** — бесконечная петля. Поиск его не находит. **Снимка в архиве нет.** Инструмент цел; источника больше **нет**. И мы этого не видели **месяцами**: скрипт падал, воркфлоу отменяли — и все думали, что дело **в скрипте**. Никто не спросил, **жив ли источник** (ср. ЗКН-Пл006). Теперь спрашиваем **первым делом** и говорим прямо, а не «15 отказов подряд» | **У5** | `infra-audit.py` (проверен на живом нарушении) · `vraja-coords.py::source_alive` | 🟢 |
| ЗКН-Пл017 | **КАТАЛОГ НЕ МЕЛЬЧАЕТ ПРИ СМЕНЕ ИСТОЧНИКА.** `/iskcon/centers` показывал **956 центров** из таблицы `places`. Новый пользовательский слой (таблица `centers`, 6 seed-строк) подключили к ТОМУ ЖЕ адресу — а 956 записей перенести **забыли**. Локатор молча ушёл **с 956 на 0**: «Ничего не найдено» при целой базе. Данные не терялись — оборвался **источник страницы** (ср. ЗКН-Пл006), и никто не заметил, потому что ни один гейт не стерёг, что публичный каталог **наполнен**. Публичный каталог не имеет права падать ниже пола живых записей; смена питающей таблицы требует **переноса покрытия ПЕРЕД переключением**. Пол 900 (было 956) | **У5** | `data-audit.py` (проверен на живой базе: пол 2000 → n=1041) | 🟢 |
| ЗКН-Пл014 | **ОЗВУЧКА СВЯЗЫВАЕТСЯ С КНИГОЙ ПО КЛЮЧУ, А НЕ ПО ПОРЯДКУ.** Озвучка ШБ — по-стиховая: 13 256 файлов, 335 глав. Привязка «N-й файл = N-я глава» здесь ложь: 770 стихов слиты («текст 21-22»), есть введения к главам и передняя материя. Файл несёт `ref` («ШБ 10.14.21-22»), и он же ключ в `verses` — совпадение проверяется, а не предполагается: **770 слитых в аудио = 770 слитых в D1**, один в один. Гейт `sb-verify` ловит сироту-ref (аудио есть, стиха нет) и дубль позиции | **У5** | `sb_verify.py` · воркфлоу `sb-verify` | 🟢 |
| ЗКН-Пл012 | **РЕЛЕЙ ЧЕРЕЗ РАННЕР МЕРЯЕТСЯ, А НЕ ПРЕДПОЛАГАЕТСЯ.** Первая версия ингеста качала последовательно: 3.5 файла/мин = **40 часов** на 33.5 GB. Замер показал, что упор в скачивание, и оно масштабируется линейно: 6 потоков → 18.9 файла/мин. Прежде чем запускать многочасовой перегон — сними скорость на первой сотне файлов | У2 | `sb_audio.py` (конвейер DL/IA) | 🟢 |
| ЗКН-Пл013 | **АВТОМАТИЗМ — ЭТО МЕХАНИЗМ, А НЕ ОБЕЩАНИЕ.** Самодокат заливки ШБ держался ровно на одном: `GH_TOKEN` должен уметь запускать воркфлоу. Токен был **МЁРТВ (401)** — цепочка встала бы молча, и половина «Бхагаватам» не доехала бы никогда. Тем же 401 бился крон воркера, диспатчащий истории. Прежде чем сказать «идёт само» — ПРОВЕРЬ дispatch механически | **У5** | шаг «Самодокат жив?» в `sb-verify` | 🟢 |
| ЗКН-Пл005 | **Кузница золота — контентный пайплайн**: 6 каналов жатвы → досье (провенанс) → сборка → гейт. Подчиняется Пл004 | У2→У5 | `tools/goldforge/` | 🔴 |
| ЗКН-Пл018 | **ЕДИНАЯ ЛЕНТА, МНОГО ЛИНЗ.** Лента Даршана показывает РАЗНЫЕ типы контента одним потоком: посты Telegram-канала (живой парсинг), **новости** агентств ИСККОН (`news_posts`, перевод RU) и **медиа** с archive.org (`feed_media`, видео/аудио). Тип различается полем `kind`; каждый тип — своя карточка, но живёт в общем потоке и в общих действиях (♥ · поделиться · PDF · QR — через `cardActions`). Линзы (Лента · Новости · Фото · Видео · Аудио · Файлы · Ссылки) — СРЕЗЫ одной ленты, а не отдельные экраны с копиями логики: `DarshanFeed` фильтрует поток по `kind`, `HomeFeed` вклеивает внешние элементы по дате. Витрины **не плодят** свои копии карточки поста | У2→У3 | `apps/web/src/feed/` (`types.ts` — дискриминатор; `DarshanFeed` — срезы) | 🟡 |
| ЗКН-Пл019 | **ИСТОЧНИК — ЭТО КОНФИГ, А НЕ КОД.** Новости берутся из НЕСКОЛЬКИХ официальных агентств (iskconnews.org · iskcon.org · dandavats.com). Добавить сайт = ОДНА запись в реестре `SOURCES`, а не новый скрипт: у каждого источника свой короткий префикс (guid/slug неймспейснуты, чтобы не сталкиваться), метод забора (WordPress REST → откат на RSS/Atom) и фильтр шума. Конвейер (забор → дедуп по D1 → перевод RU с BBT-точностью → запись) — ОБЩИЙ. Плодить парсер-на-сайт значит гарантировать, что они разъедутся | У2 | `tools/news/iskcon_news.py::SOURCES` · `--selftest` | 🟡 |
| ЗКН-Пл020 | **СТОРОННЕЕ ВИДЕО ПЕРЕЗАЛИВАЕТСЯ НА ARCHIVE.ORG, А НЕ ХОТЛИНКУЕТСЯ.** Ролики @bhakti.school и т. п. не встраиваются с YouTube (реклама, трекинг, уход из приложения, ломкие эмбеды) — они скачиваются (`yt-dlp`), перезаливаются на наш archive.org по стандарту (mediatype movies, коллекция, источник в метаданных) и играют ЗДЕСЬ бесшовным `<video>` из `stream_url`. Заголовок переводится на русский. Тот же закон, что у ленты ТГ (Пл002): тяжёлое медиа — на IA, инлайн-плеер, дедуп по guid | У2→У3 | `tools/video/bhakti_school.py` · `bhakti-school-video.yml` · `feed_media.stream_url` | 🟡 |
| ЗКН-Пл021 | **ФАНТОМНЫЙ ФИЛЬТР: ОТСУТСТВУЮЩИЙ ПАРАМЕТР ≠ 0.** Публичный локатор `/api/centers` молча отдавал **0 центров** при целой базе (959 живых) — «Ничего не найдено». Причина не в данных (ср. Пл017), а в СЛОЕ ОТДАЧИ: хелпер `num()` возвращал `0` для отсутствующего параметра (`Number(null)===0`, `Number("")===0`), из-за чего `hasGeo` становился `true` **без координат**, и запрос фильтровал центры в радиусе 50 км от точки **(0,0)** в Гвинейском заливе → пусто. Опциональный числовой параметр: **нет значения → `null`, а не `0`**; опциональный фильтр (гео, дата, диапазон) **не активируется** без явного ввода. Каталог наполнен (Пл017) ≠ каталог **отдаётся**: страница обязана реально ВОЗВРАЩАТЬ записи | **У5** | пост-деплой гейт в `deploy-web.yml` (plain `/api/centers?limit=1` → items ≥ 1; проверен на живом нарушении: до фикса items=0) | 🟢 |
| ЗКН-Пл022 | **МАШИННЫЙ ПЕРЕВОД ПРОХОДИТ ЧЕРЕЗ КАНОНИЗАТОР ИМЁН.** Бесплатный переводчик (Пр008) не знает наших форм: он отдаёт голое «Чайтанья Махапрабху», одиночное «Радхарани», «Прабхупада» — а это ровно те формы, что запрещены каноном (И001/ПР005). Пока имена правил промпт Claude, канон держался переводчиком; убрали Claude — канон обязан держать **детерминированный** пост-проход. Любой выход MT (заголовок, лид, тело новости, заголовок видео) ОБЯЗАН пройти `canonize_ru`: «Гауранга Махапрабху», «Шримати Радхарани», «Шрила Прабхупада», «Гауранга Лила». Канонизатор — ОДИН на новости и видео (видео импортирует его, ЗКН-Д002). Не хочешь голых форм в проде — прогоняй перевод через канон, а не надейся на модель | У2→У4 | `iskcon_news.py::canonize_ru` · `--selftest` (проверен на живых формах: Чайтанья→Гауранга, Радхарани→Шримати) | 🟢 |
| ЗКН-Пл023 | **НЕКРИТИЧЕСКИЙ БИНАРЬ ЖИВЁТ НА ДВУХ ЗЕРКАЛАХ ПО МАНИФЕСТУ, А НЕ В GIT.** Тяжёлые несекретные бинари (фото/видео/аудио/PDF-мастера) выносятся из репозитория на **ДВА независимых бесплатных зеркала** — и в этом вся страховка от бана: **archive.org** (основное, щедро, вечно) + **GitHub Releases** (зеркало: для публичного репо бесплатно, ассет НЕ раздувает git, ≤2 ГБ/файл). Забанили аккаунт archive.org → раздаёт Releases; сняли релиз → раздаёт archive.org. Индекс истины — `docs/assets/manifest.jsonl` (лежит В git: переживает потерю ЛЮБОГО хоста и D1): на класс — sha256 tar-архива, оба URL, первоисточник для перезаливаемого. Гейт живости еженедельно HEAD-проверяет оба зеркала — бан виден СРАЗУ, а не когда преданный упрётся в мёртвую ссылку. Восстановление — одной командой (`restore` качает с живого зеркала, сверяет sha256, распаковывает; перезаливаемое — из первоисточника). **«Хранить только на archive.org» — это и есть однопунктовый отказ; страховка = НЕ зависеть от одного хоста.** Только бесплатно (Пр008) | У2→У5 | `tools/assets/offload.py` · `verify.py` · `assets-offload.yml` · `assets-verify.yml` · `docs/assets/manifest.jsonl` | 🟢 |
| ЗКН-Пл024 | **СБОЙ ОДНОГО ИСТОЧНИКА/ЭЛЕМЕНТА НЕ РОНЯЕТ ВЕСЬ ПРОГОН — И `SystemExit` НЕ ЛОВИТСЯ `except Exception`.** Новостной прогон дважды падал с exit 1, добавив 12 и 8 статей: первый источник отработал, а на втором фетч (REST/RSS) при таймауте/500/битом XML делал `raise SystemExit` — и всё умирало. Ключ: `SystemExit`/`KeyboardInterrupt` наследуют `BaseException`, а НЕ `Exception`, поэтому обёртка `try/except Exception` их **пропускает** — прогон не спасти на уровне цикла. Многоисточниковый парсер обязан изолировать сбой: (1) фетч-хелперы на исчерпании попыток **возвращают** `None`/`[]` с `::warning`, а не `raise SystemExit`; (2) каждый источник и каждый элемент — в своём `try/except Exception` → пропуск, остальные продолжают; (3) `SystemExit` допустим ТОЛЬКО на старте (нет доступа к D1), ДО цикла. Один мёртвый сайт/битая статья не смеют обнулить работу остальных | У2→У4 | `iskcon_news.py` (фетч возвращает, а не рушит; обёртки на источник и элемент) · `--selftest` | 🟢 |

### Домен Пр — Продуктовая дисциплина · 🟠 УТВЕРЖДЁН С ОГОВОРКАМИ 10.07.2026
> Пр003 / Пр005 — твёрдо. Пр001 / Пр004 / Пр006 — **🟠 ориентир** (не абсолют, прорабатывается по ходу). Пр002 — переформулирован основателем.

| ID | Закон | Ур. | Проверка | Статус |
|----|-------|-----|----------|--------|
| ЗКН-Пр001 | 🟠 Сангха преимущественно через совместные враты; лента/чаты/соц-граф — НЕ по умолчанию (тянут в майю). НЕ абсолютный запрет — прорабатывается по ходу | У1 | ревью соц-фич | 🟡 |
| ЗКН-Пр002 | Центр прославления — Шрила Прабхупада и ИСККОН в Гауранга Лиле. Вклад ДРУГИХ сампрадай в эту волну можно УПОМИНАТЬ (созвучно Сд003); не смешивать доктрину и не переносить центр на чужие гуру-бренды | У1 | ревью контента | 🟡 |
| ЗКН-Пр003 | НЕТ свободному AI-философствованию — только со ссылкой на стих + «уточняйте у наставников» | У1→У3 | гейт на AI-фичах | 🟡 |
| ЗКН-Пр004 | 🟠 Северная метрика — дни полной садханы (не DAU); главный экран — порог (даршан/стих/практика), не витрина/корзина. НЕ абсолют — прорабатывается по ходу | У1 | ревью аналитики/главного экрана | 🟡 |
| ЗКН-Пр005 | Гаятри — только инициированным (гейт по профилю через `effectiveLevel`)  Гейт **УПРЕЖДАЮЩИЙ:** гаятри в приложении пока нет, но файл с гаятри-содержимым обязан звать `effectiveLevel` | **У5** | `product-audit.py` | 🟢 |
| ЗКН-Пр007 | **КНОПКА ОБЕЩАЕТ ТОЛЬКО ТО, ЧТО ЕСТЬ.** Элемент помечен «читаемым»/«слушаемым» ТОЛЬКО если содержимое реально внесено. Пустое обещание ХУЖЕ отсутствия: человек жмёт «Читать» и упирается в белый экран — тратится доверие, а не время. Было **14 книг** с `readable=1` и нулём стихов. Честное «скоро» лучше ложного «есть» | **У5** | `data-audit.py` | 🟢 |
| ЗКН-Пр006 | 🟠 Table-stakes (джапа/стрик/локатор) — база, а не главное отличие. НЕ абсолют — прорабатывается по ходу | У1 | продуктовое ревью | 🟡 |
| ЗКН-Пр008 | **НАПОЛНЕНИЕ — ТОЛЬКО НА БЕСПЛАТНОМ. НИКАКИХ ПЛАТНЫХ API И БАЛАНСОВ.** Стратегическое НЕТ основателя (13.07.2026, дословно: «никаких платных балансов в наших процессах — исключительно бесплатные решения, скрипты и парсеры»). Пайплайны, наполняющие приложение (новости, видео, календарь, кузница), работают на бесплатных инструментах: свои скрипты/парсеры, свободные веб-эндпоинты, офлайн-библиотеки. **Платный ключ или биллинговый баланс в процессе наполнения — нарушение.** Перевод новостей и заголовков видео шёл через платный Claude API — заменён на бесплатный веб-эндпоинт (Google → откат MyMemory) + детерминированный канонизатор имён (Пл022). Ключ `ANTHROPIC_API_KEY` из пайплайнов наполнения убран | У2 | продуктовое ревью · `grep ANTHROPIC tools/news tools/video` = 0 | 🟡 |

### Домен БТ — Достоверность / BBT · ✅ УТВЕРЖДЁН 10.07.2026
| ID | Закон | Ур. | Проверка | Статус |
|----|-------|-----|----------|--------|
| ЗКН-БТ001 | **НОЛЬ ФАБРИКАЦИИ** — жёсткий инвариант. Ничего не выдумывать; нет источника → нет утверждения  | **У5** | `data-audit.py`: цитата без источника = 0. Проверено на 1932 цитатах | 🟢 |
| ЗКН-БТ002 | Точность уровня BBT (Бхактиведанта Бук Траст / Прабхупада) во всём контенте | У1 | ревью | 🟡 |
| ЗКН-БТ003 | Цитаты — до КОНКРЕТНОГО стиха (ШБ/БГ/ЧЧ, книга/песнь/стих), не до главы  **Долг: 68 цитат привязаны к книге, но не к стиху** (на 11.07.2026). Чинить выдумыванием номера ЗАПРЕЩЕНО — это была бы фабрикация (ЗКН-БТ001); стих ищется в источнике кузницей. **ХРАПОВИК:** гейт держит долг от РОСТА | **У5** | `data-audit.py` — новая цитата без стиха роняет CI | 🟡 |
| ЗКН-БТ004 | Чужой голос (цитаты писаний, Прабхупада) НИКОГДА не редактируется; копирайт-дисциплина = часть рва | У1→У4 | освобождение цитат в санитайзере/рендере | 🟡 |
| ЗКН-БТ005 | **ВЕРБАТИМ-ЦИТАТА ВЕДЁТ В ЖИВУЮ КНИГУ.** Дословная цитата — **обещание**: «вот точные слова, вот где их проверить». Ссылка в книгу, которой в приложении **нет**, — обещание пустое: человек нажмёт и упрётся в ничто. Хуже: **проверить нас он не сможет** — и вынужден будет верить на слово. Мы для того и приводим источник, чтобы верить было **не нужно** | У1→У3 | ревью `longform` + `to:` | 🟡 |
| ЗКН-БТ007 | **СЫРОЙ СКРЕЙП НЕ ПОПАДАЕТ В БОЕВОЙ ТЕКСТ.** Каталог дхамы грузился скрейпером, и человек читал как описание святого места: **407** строк-источников («Бхактиведанта Нараяна Госвами Из книги…») вместо рассказа · **403** описания, обрезанных посреди слова («…каменная горка, на ко») · неразобранные HTML-сущности (`&#8212;`) · **заметки редактора** («Надо поехать отфоткать»). Описание — это описание; источник — в поле `source` | **У5** | `data-audit.py` | 🟢 |
| ЗКН-Ф011 | **`\b` НЕ РАБОТАЕТ С КИРИЛЛИЦЕЙ.** В JavaScript граница слова определяется через `\w` = `[A-Za-z0-9_]` — кириллицы там нет. `/^ГГД\b/` НЕ совпадает с «ГГД 193». Первая версия `sourceRef.ts` молча не срабатывала **ни на одной** ссылке. Вместо `\b` — явный просмотр вперёд `(?![А-Яа-яЁёA-Za-z])`  **Гейт нашёл ЖИВОЙ БАГ:** в фильтре даршанов стояли `\bкурс\b`, `\bбилет`, `\bзакат\b` — **три мёртвых условия**, никогда не срабатывавших. Фильтр молча пропускал посты про курсы, билеты и закаты | **У5** | `laws-lint`: `\b` впритык к кириллице = fail | 🟢 |
| ЗКН-Ф023 | **КИРИЛЛИЦА ИЩЕТСЯ НОРМАЛИЗАЦИЕЙ, А НЕ КЛАССАМИ РЕГИСТРА.** SQLite не складывает регистр для не-ASCII. Обходили классом `[аА]` на КАЖДУЮ букву — и упёрлись в стену: **D1 рубит GLOB-шаблон на 50 БАЙТАХ** (измерено на живой базе: 7 классов проходят, 8 — `LIKE or GLOB pattern too complex`). Класс `[аА]` = 6 байт → **потолок 8 кириллических букв**. «прабхупада» — 10, «бхактиведанта» — 13. Поиск личностей отдавал **500 на любое настоящее имя**, а клиент ловил это в `.catch(() => setHits([]))` и показывал «Ничего не найдено»: человек читал, что **Шрилы Прабхупады в приложении НЕТ**. В `/api/search` костыль стоял (обрезать классы шестью) — падения нет, но с 7-й буквы регистр молча переставал складываться; а `/api/entities` жил СВОЕЙ копией той же логики, без обрезки, и падал (ЗКН-Д002: копия разъехалась). **ЛЕЧЕНИЕ:** складываем ОБЕ стороны к одному виду — нижний регистр, без диакритики — и ищем `instr`. Потолка длины нет вовсе. Побочно: «prabhupada» находит «Prabhupāda». **И: сбой не выдаётся за пустоту** (ЗКН-Ф014) | **У5** | `laws-lint.py::check_f022_glob` · `normSql`/`normNeedle` | 🟢 |
| ЗКН-Ф024 | **КЭШИРУЕТСЯ ТОЛЬКО УСПЕХ. ОТКАЗ — НИКОГДА.** Проксирование звука стояло с `cacheTtl: 31536000` — год на ЛЮБОЙ ответ, включая 404. Пока шла заливка, плеер запрашивал файл, которого ещё не было; приходил 404 — и Cloudflare **запоминал его на год**. Файл давно в архиве, архив его отдаёт по ссылке, а наш край продолжает отдавать сохранённый отказ. **Кэш переживает деплой: новой сборкой это не лечится.** Отказ — состояние минуты, а не года; запоминать его надолго значит закрепить временную беду навсегда. Лечится `cacheTtlByStatus` (успех — год, 4xx/5xx — ноль) и СМЕНОЙ КЛЮЧА подзапроса (метка ставится на адрес ИСТОЧНИКА, а не на наш: кэшируется подзапрос). | `serveAudio()` в `worker.ts`, `AUDIO_CACHE_BUST` |
| ЗКН-Ф025 | **ТЯЖЁЛЫЙ БИНАРЬ НЕ КОММИТИТСЯ В GIT.** Фото/видео/аудио/PDF в дереве репозитория раздувают клон, жгут минуты CI на каждом чекауте, а `.git`-историю потом **не почистить** без переписывания (меняет все хэши). Один воркфлоу (`tilda-import`) молча заливал **438 МБ** экспорта Tilda в `archive/tilda-export/` на каждом прогоне — груз, который приложение вообще не читает. Новый отслеживаемый медиа/документ-бинарь тяжелее **512 КБ** (шрифты не в счёт — они нужны при сборке) роняет линтер. Выносить на два зеркала (ЗКН-Пл023), а не коммитить. **Храповик:** текущие 293 зафиксированы как долг, новый нарушитель роняет сборку; после выноса счёт падает — база пересчитывается вниз | **У5** | `laws-lint.py` храповик `tracked_big_binary: 293` (проверка `git ls-files` × размер) | 🟢 |
| ЗКН-Ф026 | **КОНТЕНТ КНИГ КЭШИРУЕТСЯ НА КРАЮ (масштаб + скорость повторных загрузок).** Чтения книг (`/api/books/*`: `/toc`, `/verses/:ref`, `/chapters/:n/read`) — статика: стих не меняется от захода к заходу. Раньше same-origin-прокси на `api.gaurangers.com/v1` ставил `no-store` на ВСЕ ответы, и КАЖДЫЙ заход на стих/главу делал полный хоп `worker→/v1→D1` — лишний сетевой хоп на каждый экран И нагрузка на D1 при масштабе (100k пользователей = 100k запросов в D1). Теперь GET-чтения книг кэшируются на краю Cloudflare (`caches.default`) и в браузере (`public, max-age=60`): первый заход в регионе греет край, дальнейшие — из края, без хопа и без D1; при масштабе D1 видит ~1 запрос на эндпоинт в минуту на регион. TTL 60с — правки книг в D1 доезжают за ≤1 мин (дев-цикл не ломается). Кэшируется ТОЛЬКО успех (200) — согласно Ф024; динамика (`me`/`push`/заметки/обеты/мутации) остаётся `no-store`. На промахе кэша ветка ведёт себя как прежде (проксирует) — правка строго аддитивна, сломать экраны не может. ВАЖНО: это лечит масштаб и скорость ПОВТОРНЫХ загрузок; латентность ПЕРВОГО захода (медленный D1/хоп на /v1) — отдельная задача, требует замера на устройстве. | **У5** | `infra-audit.py::check_f026` (прокси обязан смотреть край перед хопом на /v1, класть успех книги в `caches.default` и помечать чтения книг кэшируемыми — иначе каждый заход бьёт в D1) | 🟢 |
| ЗКН-БТ006c | **СОКРАЩЕНИЕ В ССЫЛКЕ — НЕ ССЫЛКА, А ШИФР.** Основатель поймал на карточке Ангады: «**РКГД, 14**». Читатель не знает, что такое РКГД, и перейти некуда. Было **132**: НП (63) · РКГД (45) · ПЛ (16) · БРС (8). Ссылка пишется ПОЛНОСТЬЮ: «Нектар преданности», глава 41 | **У5** | `data-audit.py` | 🟢 |
| ЗКН-БТ006 | Ссылка на источник пишется ПОЛНОСТЬЮ и по-человечески: полное название · деление (Ади-/Мадхья-/Антья-лила · песнь · канто) · глава · стих. Пример: «Шри Чайтанья-чаритамрита, Ади-лила, глава 15, стих 135». Никаких обрубков («ЧЧ 1.15.135») в отображаемом тексте  | **У5** | `data-audit.py`: ссылка латиницей/кодом = fail | 🟢 |

### Домен Ц — Скрипты / CI · ✅ УТВЕРЖДЁН 11.07.2026 (см. §6)
| ID | Закон | Ур. | Проверка | Статус |
|----|-------|-----|----------|--------|
| ЗКН-Ц001 | Токены всегда scrub-ятся в выводе (`ghp_`/`github_pat_` → `***`) | У2 | grep вывода | 🟢 |
| ЗКН-Ц002 | Коммит-сообщения — короткие однострочные (многострочные ломают сериализацию тула) | У1 | — | 🟢 |
| ЗКН-Ц008 | **ИНСТРУМЕНТ НЕ ЛОВИТ ОБЪЯСНЕНИЕ НАРУШЕНИЯ.** Я написал в комментарии: «очистка правит имена (**запрещённая форма** → канон)» — и линтер поймал запрещённую форму там, где она **приведена как пример**. CI упал. **Комментарий — место, где закон ОБЪЯСНЯЮТ.** Запретить в нём называть нарушение — значит запретить объяснять, **почему так нельзя**; тогда в коде останутся правила без причин, и первый же человек их снесёт, не поняв. **Гейты стерегут КОД. Комментарий — не код.** Ср. Ц005 (инструмент не ловит сам себя) | **У5** | `laws-lint.py` (проверено с обеих сторон) | 🟢 |
| ЗКН-Ц005 | **ИНСТРУМЕНТ НЕ ЛОВИТ САМ СЕБЯ.** Линтер **упоминает** `urllib.request.urlopen` в тексте своего правила — и **посчитал это за нарушение**. Долг вырос на 1, храповик упал в CI, и я пошёл искать несуществующий баг в чужом коде. **Инструмент, который ловит нарушение, не должен ловить СВОЁ ОПИСАНИЕ нарушения** — иначе он врёт, и врёт убедительно. Гейты исключают себя из обхода | **У5** | `laws-lint.py` | 🟢 |
| ЗКН-Ц011 | **УДАЛЕНИЕ ГЕЙТА НЕ ПРОХОДИТ МОЛЧА.** Вырезая один блок из `data-audit.py`, я срезал вместе с ним **три соседних** (Б004/Б005/Б006) — гейт каталога книг молча исчез, а свод продолжал объявлять их У5. Обнаружилось случайно. Теперь: закон объявлен У5, а механизма нет → **аудит падает** | **У5** | `laws-audit.py` | 🟢 |
| ЗКН-Ц007 | **ГЕЙТ, КОТОРЫЙ ТРЕБУЕТ НАРУШИТЬ ДРУГОЙ ЗАКОН, — НЕ ГЕЙТ.** `data-audit` проверял канон имён (ЗКН-И001) по **всему досье**, включая **тексты цитат**. Все 472 вхождения «Шри Чайтанья Махапрабху» лежали в ЦИТАТАХ; в прозе — **ни одного**. А ЗКН-БТ004 прямо запрещает править чужой голос. Закрыть такой гейт можно было ТОЛЬКО переписав слова Прабхупады и Кришнадаса Кавираджи — то есть **совершив то, что свод называет фабрикацией**. Гейт горел вечно, и `laws-lint` стоял красным **восемь сборок подряд** — а в красном CI **тонет настоящий регресс**: на него уже никто не смотрит. **Красный гейт, который нельзя закрыть по закону, вреднее отсутствующего:** тот хотя бы не лжёт. **Тот же класс всплыл в тот же день ТРИЖДЫ:** (1) И001 требовал править цитаты; (2) П019 проверял намерение скрипта, а не базу (ЗКН-Ц012); (3) БТ003 требовал номер стиха у **списка книг** — `{"ref":"Бхагавад-гита как она есть","to":"/bhagavad-gita"}` — и 127 «нарушений» из 146 оказались обычной библиографией. Прежде чем писать проверку — назови ПРЕДМЕТ: канон имён есть закон ПРОЗЫ (её правит `cleanCardText`), цитата — чужой голос, а отсылка к книге — не цитата | **У5** | `data-audit.py` — И001/И004 считают прозу, чужой голос исключён по `key`+`path` | 🟢 |
| ЗКН-Ц012 | **ГЕЙТ СМОТРИТ НА РЕЗУЛЬТАТ, А НЕ НА НАМЕРЕНИЕ ИНСТРУМЕНТА.** У ЗКН-П019 («карточка — не провенанс») в графе «Проверка» стояло: «`compose.build` — таб `istochniki` не строится». Это проверка **НАМЕРЕНИЯ**. А таб **был** — в 15 боевых карточках, с заголовками «Prabhupada Shikshamrita.RU», ссылками `docs/sources/…/Lives-of-the-Saints.EN.txt` и пометками «Пассажей с именем героя: 159, упоминаний: 345». Данные положил другой путь: старая версия скрипта, ручная правка, ингест. **Инструмент можно обойти, заменить, откатить — данные останутся.** Поэтому «инструмент так не делает» — это **У3**, а не У5; объявить это У5 значит записать себе закрытый долг, которого нет. Гейт уровня У5 обязан смотреть туда, куда смотрит человек: в базу и в прод | **У5** | `data-audit.py::ЗКН-П019/П020/БТ006b` — SQL по D1 (проверены на живом нарушении) | 🟢 |
| ЗКН-Ц003 | Скрипт идемпотентен; `sh`-совместимость (bash-массивы/C-циклы — только `bash file.sh`); `jq` нет → python3 | У1 | — | 🟡 |
| ЗКН-Ц004 | Деструктивные воркфлоу — в карантин; на гейтах — тег «закон» | У1→У3 | инвентаризация `.github/workflows` (~110) | 🔴 |
| ЗКН-Ц006 | **ЗАКОН, ПРИМЕНЁННЫЙ К ЧАСТИ ФАЙЛОВ, НЕ ПРИМЕНЁН.** Линтер смотрел ТОЛЬКО в `apps/web/src`. А этажом выше лежат **воркер-файлы** — и они отдают человеку тот же текст, что и экран. За пределами папки закон просто не действовал, и это не теория: **(1)** `workerCalendar.ts` печатал «явление Шри Чайтаньи Махапрабху» (ЗКН-И001) — голая форма, **в проде**, в каждой строке Гаура-пурнимы, прошлой и будущей. **(2)** `worker.ts` держал **ВТОРУЮ КОПИЮ** фильтра даршанов с теми самыми тремя мёртвыми условиями (`\bкурс\b`, `\bбилет`, `\bзакат\b`), ради которых писался ЗКН-Ф011: копию в `src` починили и дополнили — **эту не увидели**, и фильтр воркера продолжал пропускать курсы, билеты и закаты. **(3)** `worker.ts` строил боевой домен строкой (ЗКН-Н020), и `bookPageUrl` вёл на `/books/<слаг>` — **мёртвый адрес**: книга по ЗКН-Н023b живёт в корне. Эта ссылка печатается в метаданные Internet Archive **навсегда**. Область гейта = весь исполняемый код, а не одна папка (ср. ЗКН-Н016) | **У5** | `laws-lint.py::WORKERS` — ЗКН-Ц006 (найдены три живых нарушения) | 🟢 |
| ЗКН-ПР001 | **НИ ОДИН СТИХ НЕ ВХОДИТ В БИБЛИОТЕКУ БЕЗ ПРОИСХОЖДЕНИЯ.** `docs/LEGAL.md` Реестр: `scripts/library/registry.py` (машина) + `docs/library/RIGHTS.md` (человек). | **У5** | `scripts/library/gate.py::ПР001` · `library-gate.yml` | 🟢 |
| ЗКН-ПР002 | **МАШИНА МОЖЕТ ПУБЛИКОВАТЬ ПИСАНИЕ. ПО СОГЛАСИЮ С СОЗДАТЕЛЕМ ПРИЛОЖЕНИЯ ISKCON ONE LOVE.** Всё, что выходит из `translate.py`, ложится в БД как `draft`. Приложение рендерит `published`. Перевести draft → published. | **У5** | `gate.py::ПР002` | 🟢 |
| ЗКН-ПР003 | **ПЕРЕВОД БЕЗ ИСТОЧНИКА НЕ СУЩЕСТВУЕТ.** Источник = оригинал (деванагари/IAST) **ЛИБО любое читабельное переложение** (ПР009). Нет НИЧЕГО → текст **сочинён**.  **ПОПРАВКА II, 13.07.2026 — ОСНОВАТЕЛЬ:** «Законы ставились, чтобы машина не писала отсебятины в писаниях». Именно так. Закон — щит от **ВЫДУМКИ**, а не от перевода. Я применял его как запрет на перевод и был неправ: сначала требовал бенгальский оригинал, потом «посредника с правами», потом годный OCR — каждый раз находя новый блокер там, где закон охранял другое. Буква вместо духа. Отменено: **читабельный источник есть → работать можно**; стена стоит там, где ей место — на ПР002 | **У4+У5** | `translate.py` · `gate.py::ПР003` | 🟢 |
| ЗКН-ПР004 | **ПАРИТЕТ С ИСТОЧНИКОМ — ЭТО И ЕСТЬ ЛОВЛЯ ГАЛЛЮЦИНАЦИИ.** `parse.py` фиксирует число стихов, реально найденных в источнике (`*.count`). Стало больше после перевода — модель их **сочинила**. Стало меньше — потеряла. Дубли `ref` внутри произведения запрещены. Галлюцинация ловится **счётом**, а не «на глаз» | **У5** | `parse.py` (манифест паритета) · `gate.py::ПР004` | 🟢 |
| ЗКН-ПР005 | **КАНОН ИМЁН ДЕЙСТВУЕТ И В ПИСАНИИ.** Переводчик обязан различать **«Гауранга Махапрабху»** (Навадвипа-лила) и **«Шри Кришна Чайтанья Махапрабху»** (после санньясы, Нилачала). Формы «Шри Чайтанья», «Чайтанья», «Гаура-лила», одиночное «Радхарани» — запрещены. Стандарт вшит в системный промпт переводчика и проверяется по БД | **У4+У5** | `translate.py::SYSTEM` · `gate.py::ПР005` | 🟢 |
| ЗКН-ПР006 | **СТИХ БЕЗ `source_url` — СТИХ БЕЗ ПРОИСХОЖДЕНИЯ.** 30 502 таких уже лежат в БД: мы не можем доказать, откуда они взялись. Для проекта, где «ноль выдумки» — первый закон, это дыра в фундаменте. Новые стихи без источника не пишутся вообще | **У5** | `gate.py::ПР006` | 🟢 |
| ЗКН-ПР007 | **МЫ НЕ УДАЛЯЕМ — МЫ НЕ ПУСКАЕМ.** Правовой долг НЕ лечится через `DELETE`: это нарушило бы Р002 и уничтожило бы годы работы. Стихи с `license ∉ {public-domain, own-translation, licensed}` **остаются в БД, но не рендерятся пользователю**. Долг закрывается одним из двух путей: собственный перевод с открытого оригинала (путь, названный самим LEGAL.md §2.3) · сознательное «не показываем»   **ЗАКОН СТОЯЛ 🟢 БЕЗ КОДА.** | **У5** | `gate.py::ПР007` · храповик `ПР007: 21` | 🟢 |
| ЗКН-ПР008 | **РЕФ РОЖДАЕТСЯ В ИСТОЧНИКЕ, А НЕ В ЗАГРУЗЧИКЕ.** Загрузчик Бхакти-ратнакары проигнорировал родную нумерацию и пронумеровал блоки подряд `1..N`. «БР 1.795» **не существует ни в одном издании книги**: в источнике нумерация идёт диапазонами до 890, а **8 из 15 таранг — сплошная проза, без единого номера**. Проза выдана за пронумерованные стихи — **1 590 ссылок досочинено**. Синтетический реф = фабрикация ссылки (БТ001), ловится **СЧЁТОМ**, как ПР004: стихов в D1 не может быть больше, чем родных номеров в источнике. Манифест паритета обязан быть **коммитируемым** (`docs/library/PARITY.json`) — в `build/` его в CI нет, и гейт слеп. Нет манифеста → фабрикацию не доказать: гейт не обвиняет молча, а печатает список | **У5** | `gate.py::ПР008` (проверен на живом нарушении: БР 4 669 в D1 vs 3 079 в источнике) · храповик `ПР008: 1590` | 🟢 |
| ЗКН-ПР009 | **ПОСРЕДНИК РАЗРЕШЁН. РЕШЕНИЕ О ПРАВАХ — ОСНОВАТЕЛЯ, НЕ МАШИНЫ.** Оригинал недостижим, русского текста нет → работаем с лучшим читабельным переложением. Условия: (1) источник помечен `via_edition`/`via_lang` — скрыть посредника значит соврать о происхождении (БТ001); (2) **ПР002 неизменен** — машина отдаёт `draft`, публикует человек; (3) ПР004, ПР005, ПР008, ПР010 неизменны.  **ПОПРАВКА 13.07.2026:** прежнее условие «посредник обязан быть PUBLISHABLE» вписал я, и оно **не из этой конституции**. Гаудия-переводчик переводит с бенгали на английский ЗАТЕМ, ЧТОБЫ книга разошлась по миру; вайшнавы раздают эти книги друг другу с радостью. Отказаться продолжать его работу «из уважения к его правам» — значит уважать его меньше, чем он сам уважал свою цель. Правовой риск — **осознанное решение основателя**, оно записывается в реестр, а не прячется. Машина не вето | **У5** | `gate.py::ПР009` (посредник записан) · `editions.via_edition/via_lang` | 🟢 |
| ЗКН-ПР010 | **ОРИГИНАЛ ПРОВЕРЯЕТСЯ НА ЦЕЛОСТНОСТЬ, А НЕ НА НАЛИЧИЕ.** ПР003 спрашивает «есть ли оригинал?» — этого мало. Бенгальский скан БР (1888, PD): IA отдал OCR с `Ocr_detected_script_conf = 1.0000`, доля бенгальского письма **100%**, латиницы ноль — мой гейт дал 🟢. А внутри: **каноническое «ভক্তিরত্নাকর» встречается 0 раз** при 492 близких формах (tesseract системно путает конъюнкт `ত্ন`→`ত্ব`). Письмо распознано, слова — нет. Присутствие доказано, целостность — нет. Перевод с такого «оригинала» — фабрикация **с алиби**: ПР003 её пропустит.  **МЕХАНИЗМ — ЛЕКСИЧЕСКИЙ ЯКОРЬ:** у книги есть слова, обязанные повторяться дословно (своё название в колонтитуле, имя автора, имя героя). Метрика — **доля канона** (порог 80%), а не число вариантов: число ловило `Sridhara`/`Srikhanda` как «варианты Srinivasa», а это РАЗНЫЕ ИМЕНА.  **ДВА СОБСТВЕННЫХ ПРОМАХА, ОБА ПОЙМАНЫ ПРОГОНОМ:** (1) `\w+` в Python рвёт бенгальское слово на каждой вирама (`্` = Mn, не alnum) — первая версия ПР010 **дала зелёный на том самом файле, ради которого писалась**; (2) префиксная эвристика ложно срабатывала на чистом источнике. Гейт с ложным срабатыванием хуже отсутствующего — его отключат | **У5** | `integrity.py`, проверен В ОБЕ СТОРОНЫ: бенгали 0% → RC=1 · англ. 88% → RC=0 | 🟢 |
| ЗКН-ПР011 | **ДЕША-КАЛА-ПАТРА: КОНСТИТУЦИЯ СЛУЖИТ МИССИИ, А НЕ НАОБОРОТ.** Законы писаны, чтобы слава Гауранги и Кришны дошла до людей неискажённой — **и дошла**. Закон, который перестал служить этой цели и начал ей мешать, подлежит правке по месту, времени и обстоятельствам. Правит **основатель**; каждая поправка записывает **ПОЧЕМУ** — иначе конституция превращается в свод отговорок, а исполнитель — в бюрократа, который отказывает, ссылаясь на устав.  **ТАК СЛУЧИЛОСЬ 13.07.2026:** четыре хода подряд я отвечал «нельзя», всякий раз по новому основанию, на просьбу довести книгу до русских преданных. Это отказ, замаскированный под законность.  **ИСКЛЮЧЕНИЕ ОДНО — ПР002.** Основатель сам сказал, зачем ставил законы: чтобы машина не писала отсебятины в писаниях. ПР002 и есть этот ответ. Он не гнётся | У2 | конституционное ревью | 🟢 |
| ЗКН-ПР012 | **ИЗЛОЖЕНИЕ ≠ ПЕРЕВОД. ЧЕСТНЫ ОБА — ЛГАТЬ О ТОМ, ЧТО ЭТО, НЕЛЬЗЯ.** Факты традиции свободны: события, жития, учение, ход книги не принадлежат никому — они принадлежат парампаре. Написать их по-русски своими словами (**изложение**) — законно и правильно. Пройти чужой текст блок за блоком и выдать по-русски (**перевод**) — перенос чужого выражения; «авторски, не точь-в-точь» этого не меняет: меняется этикетка, не артефакт. **Оба пути открыты — но издание обязано честно назвать себя:** `editions.kind ∈ {перевод, изложение}`, и человек в приложении видит, что читает. Изложение с оригинала PD, перевод с посредника — всё равно, лишь бы не выдавали одно за другое. Скрыть = соврать о происхождении (БТ001) | У1 | `editions.kind` + `gate.py::ПР012` | 🟡 |

---

## 5. Долги (законы не выше У1 — подлежат механизации)

Отсортировано по ROI. Это бэклог «спринтов законов» (маршрут — `docs/ROADMAP_LAWS.md`).

1. **ЗКН-Т002** — прогнать все 13 карточек через `cleanCardText` (сейчас 2/13). → З1
2. **ЗКН-И001/003** — CI-grep по канону имён во всём `apps/web/src`. → З1
3. **ЗКН-Д003** — вычистить `Apartsales` из ядра (3 файла). → З2
4. **ЗКН-Д001/002** — принять `tokens.ts`/`primitives.tsx`; линтер против магических чисел/hex. → З2
5. **ЗКН-К003** — починить дрейф док↔код в `CardCover` (3с→7с). → З3
6. **ЗКН-П003/П007 · ЗКН-БТ001** — `pkl-audit.yml` + SQL-аудит канона-манджари/narrator как жёсткий гейт. → З4
7. **ЗКН-Ц004** — инвентаризация и карантин ~110 воркфлоу. → З5
8. **ЗКН-Ф006/007/008/009** — инженерные гейты (сборка · зеркальные миграции · порядок роутов · лимит кронов) в CI. → З6
9. **ЗКН-П015 — 94 стиха «Чайтанья-бхагаваты» с раздутым переводом.** У шлоки XVI века в поле «перевод» склеено ПРИЛОЖЕНИЕ книги (ЧБ Антья 2.308 — 29 022 знака: путеводитель по храмам; ЧБ Ади 17.164 — 26 312: биография автора). Человек открывает стих в читалке и получает стену. Чинится **переимпортом** (`ingest-chaitanya.py`), не обрезкой на глаз: где кончается перевод и начинается склейка — знает источник, а не мы. Гейт заперт на 94
10. **ЗКН-П021 — 24 цитаты-стены + 59 оборванных на полуслове.** Цитата, оборванная посреди фразы, — это не высказывание. Чинится сверкой с источником; гейт заперт на 24/59
11. **ЗКН-П022 — 34 карточки с заглушкой вместо смысла.** «Личности, с которыми <Имя> связан в источниках» стоит на месте ответа «что человек дал лиле». Метаданные, выставленные на место смысла, — это пустота с подписью. Чинится наполнением (ПКЛ, фаза 2); гейт заперт на 34
12. **ЗКН-Д015 · Н046 · ПР012** — объявлены У5, механизма нет вовсе (реализация в компоненте ≠ гейт). Уровень честный: У2/У1. → механизировать
13. **ЗКН-БТ003 — 314 → 5.** Гейт требовал номер стиха у **списка книг**: `{"ref":"Бхагавад-гита как она есть","to":"/bhagavad-gita"}` — это библиография, у неё координаты нет и быть не должно (127 из 146 «нарушений» были ею — ЗКН-Ц007). Предмет закона — **цитата**: узел, у которого есть текст. **172 закрыто ТОЧНО, без единой догадки:** номер куплета вычислен сверкой текста цитаты со стихом бхаджана (`prayer_verses`), а для книг — со стихом в `verse_texts`; совпадение принималось только ОДНОЗНАЧНОЕ — по началу И по хвосту цитаты. Остаток **5** заперт храповиком: 2 — источник не внесён (Падма-пурана, Калисантарана-упанишад), 3 — перевод в карточке не совпал дословно ни с одним стихом книги. Выдумывать номер запрещено (ЗКН-БТ001)
9. **ЗКН-К006** — формализовать минимальную карточку (компактная плитка) как стандарт. → З3

### Стоячие инварианты (вне спринтов — гейтят КАЖДУЮ работу, «закрыть» нельзя)

Эти законы не механизируются в один гейт — они проверяются ревью каждой правки:

- **Домен Сд** (сиддханта): всё содержимое ВЫРАЖАЕТ доктрину и НИКОГДА ей не противоречит — Кришна свайам-бхагаван, Гауранга = Кришна в сиянии Радхи, ИСККОН = беспрецедентная волна, персонализм как форма продукта.
- **Домен Пр** (продуктовая дисциплина): твёрдо — AI только со ссылкой на стих (Пр003), гаятри только инициированным (Пр005). Ориентиры (не абсолют, по ходу) — сангха через враты, метрика = дни садханы, порог вместо витрины. Прабхупада/ИСККОН — центр; вклад других сампрадай в волну можно упоминать (Пр002).
- **Домен БТ** (достоверность): ноль фабрикации; BBT-точность; цитаты до стиха; чужой голос не редактируется.

Механизируемые части (ЗКН-Пр005 гейт-гаятри, ЗКН-БТ004 освобождение цитат) поднимаются в свои спринты; судейские части остаются на У1/У3 и сверяются вручную.

---

## 6. Стандарт скрипта / CI (домен Ц)

Любой скрипт или воркфлоу приложения обязан:

- **Идемпотентность** — повторный запуск не ломает состояние (CREATE IF NOT EXISTS,
  INSERT OR IGNORE с оговоркой: FK-нарушения им НЕ подавляются — один сирота убивает батч).
- **Scrub секретов** — `sed -E 's#(ghp_|github_pat_)[A-Za-z0-9_]+#***#g'` на любом выводе.
- **`sh`-совместимость** — шелл по умолчанию `sh`; bash-специфика (массивы, `for ((…))`)
  — только явным `bash file.sh`. `jq` отсутствует → JSON через `python3 -c`.
- **Недеструктивность по умолчанию** — DROP/DELETE на entity/profile-таблицах — только
  осознанно и с бэкапом; деструктивные воркфлоу помечаются и по возможности отключаются.
- **Один короткий однострочный коммит** — многострочные сообщения ломают сериализацию.

---

## 7. Как пользоваться сводом (для любого исполнителя)

1. **Перед работой** над текстом/карточкой/дизайном/данными/скриптом — открой §4,
   найди домен, прочти релевантные законы и их источники.
2. **Встретил повторяющуюся правку**, которую основатель говорит не впервые →
   это кандидат в закон. Пройди цикл §2, добавь строку в §4, поставь целевой уровень.
3. **Устанавливай на максимальном уровне** (§1). Документ без механизма — это У1, то
   есть *незакрыто*. Тянись к У4 (санитайзер) или У5 (CI-гейт).
4. **Не полагайся на память.** Память = У0 = корень боли. Свод — источник; память лишь
   указывает на него.
